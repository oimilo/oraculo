---
phase: 29-integration-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/__tests__/script-v3.test.ts
  - src/services/tts/__tests__/fallback-tts.test.ts
autonomous: true
requirements:
  - INTG-02

must_haves:
  truths:
    - "script-v3.test.ts expectations updated for v4 trimmed segment counts (1-2 for respostas, 1-2 for setups)"
    - "script-v3.test.ts has tests for all 12 new Q2B/Q4B script keys"
    - "fallback-tts.test.ts expects 61 keys (not 49)"
    - "All 34 test suites pass with 0 failures"
  artifacts:
    - path: "src/data/__tests__/script-v3.test.ts"
      provides: "Updated script structure tests for v4"
      contains: "Q2B.*SETUP"
    - path: "src/services/tts/__tests__/fallback-tts.test.ts"
      provides: "Updated FallbackTTS key count test"
      contains: "61"
  key_links:
    - from: "src/data/__tests__/script-v3.test.ts"
      to: "src/data/script.ts"
      via: "import { SCRIPT } from '@/data/script'"
      pattern: "SCRIPT\\.INFERNO_Q2B"
---

<objective>
Fix all 36 failing tests by updating expectations for v4 trimmed script and 61-key count.

Purpose: Phase 25 trimmed segments (respostas now 1-2, not 3-5; intros now 1 segment, not 2-4) and Phase 26 added 12 new Q2B/Q4B keys (total now 61, not 49). Old tests expect pre-v4 values.

Output: All 34 test suites pass, 503+ tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/data/__tests__/script-v3.test.ts
@src/services/tts/__tests__/fallback-tts.test.ts
@src/data/script.ts

Current test results (vitest run):
- 30/34 suites pass, 467/503 tests pass
- 4 failing suites: script-v3.test.ts (35 failures), fallback-tts.test.ts (1 failure)
- Failures: segment count expectations (3-5 → now 1-2), key count (49 → 61), depth ratio
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update script-v3.test.ts segment count expectations for v4 trimming</name>
  <read_first>
    - src/data/__tests__/script-v3.test.ts (full file — understand all assertion patterns)
    - src/data/script.ts (spot-check actual segment counts for INFERNO_INTRO, Q1_RESPOSTA_A, Q2_RESPOSTA_A, Q4_RESPOSTA_A/B)
  </read_first>
  <files>src/data/__tests__/script-v3.test.ts</files>
  <action>
    The test file has assertions from v3.1 that expect pre-trimming segment counts.
    Phase 25 trimmed everything — respostas are now 1-2 segments (was 3-5), intros are 1 segment (was 2-4).

    **Fix approach: Update ALL segment count ranges to match v4 reality.**

    For each failing test, read the actual SCRIPT value and update the assertion:

    1. **INFERNO_INTRO** "has 2-4 segments" → Update to match actual count.
       Check: `SCRIPT.INFERNO_INTRO.length` — if it's 1, change to `toBeGreaterThanOrEqual(1)` and `toBeLessThanOrEqual(2)`

    2. **RESPOSTA_A/B "has 3-5 segments"** — All respostas were trimmed to 1-2 segments.
       Change ALL resposta assertions from `toBeGreaterThanOrEqual(3)` to `toBeGreaterThanOrEqual(1)`
       Change ALL resposta assertions from `toBeLessThanOrEqual(5)` to `toBeLessThanOrEqual(3)`
       This covers: Q1_RESPOSTA_A, Q1_RESPOSTA_B, Q2_RESPOSTA_A, Q3_RESPOSTA_A, Q4_RESPOSTA_A, Q4_RESPOSTA_B

    3. **PURGATORIO_INTRO** "has 2-4 segments" → same as INFERNO_INTRO fix

    4. **Q4_PERGUNTA "has pauseAfter 0 or undefined"** — If the test checks last segment pauseAfter, the v4 pergunta may have a pauseAfter. Update assertion to allow pauseAfter.

    5. **PURGATORIO depth escalation** — "Q3 and Q4 have distinct narrative weight (Q4 deeper)"
       This test checks `q4Total > q3Total * 0.8`. With v4 trimming, the ratios may have shifted.
       Fix: Read actual Q3 and Q4 total text lengths, then update the threshold to match v4 reality.
       If Q4 is now shorter than Q3, reduce threshold (e.g., 0.5) or update description.

    **Also add tests for Q2B and Q4B keys** (12 new keys):
    Add a new `describe('Branch Questions (Q2B, Q4B)')` section with tests for:
    - INFERNO_Q2B_SETUP, INFERNO_Q2B_PERGUNTA, INFERNO_Q2B_RESPOSTA_A, INFERNO_Q2B_RESPOSTA_B
    - PURGATORIO_Q4B_SETUP, PURGATORIO_Q4B_PERGUNTA, PURGATORIO_Q4B_RESPOSTA_A, PURGATORIO_Q4B_RESPOSTA_B
    - FALLBACK_Q2B, FALLBACK_Q4B, TIMEOUT_Q2B, TIMEOUT_Q4B

    Each branch key test: verify `.length >= 1` and first segment has `.text` that's a non-empty string.
  </action>
  <verify>
    <automated>npx vitest run src/data/__tests__/script-v3.test.ts --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - script-v3.test.ts passes all tests (0 failures)
    - Resposta segment expectations updated from 3-5 to 1-3 range
    - Intro segment expectations updated from 2-4 to 1-2 range
    - New describe block for "Branch Questions (Q2B, Q4B)" exists
    - Tests for all 12 Q2B/Q4B keys exist (INFERNO_Q2B_*, PURGATORIO_Q4B_*, FALLBACK_Q2B/Q4B, TIMEOUT_Q2B/Q4B)
  </acceptance_criteria>
  <done>script-v3.test.ts fully updated for v4 trimming and branching</done>
</task>

<task type="auto">
  <name>Task 2: Fix fallback-tts.test.ts key count expectation</name>
  <read_first>
    - src/services/tts/__tests__/fallback-tts.test.ts (find the 49-key assertion)
  </read_first>
  <files>src/services/tts/__tests__/fallback-tts.test.ts</files>
  <action>
    Find the test "should have a URL entry for every key in SCRIPT (49 keys)" and:
    1. Update the test name from "(49 keys)" to "(61 keys)"
    2. Change `expect(scriptKeyCount).toBe(49)` to `expect(scriptKeyCount).toBe(61)`
  </action>
  <verify>
    <automated>npx vitest run src/services/tts/__tests__/fallback-tts.test.ts --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - fallback-tts.test.ts passes all tests
    - Key count assertion is `toBe(61)` not `toBe(49)`
    - Test name references "61 keys"
  </acceptance_criteria>
  <done>FallbackTTS test updated to expect 61 script keys</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and verify zero failures</name>
  <read_first>
    - (no files — this is a verification task)
  </read_first>
  <files>No files modified — verification only</files>
  <action>
    Run the full test suite:
    ```
    npx vitest run
    ```

    Expected: 34/34 suites pass, 503+ tests pass, 0 failures.

    If any tests still fail, fix them in the same commit.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Full vitest run output shows "34 passed" for test suites
    - Zero failures in output
    - All machine tests (117) still pass (no regressions)
  </acceptance_criteria>
  <done>All 34 test suites pass with 0 failures</done>
</task>

</tasks>

<verification>
Phase 29-02 verification:
1. `npx vitest run` shows 34/34 suites pass
2. Zero test failures
3. script-v3.test.ts has Q2B/Q4B branch tests
4. fallback-tts.test.ts expects 61 keys
5. All machine tests (117) still pass
</verification>

<success_criteria>
- INTG-02: All tests passing with v4.0 structure
- Script structure tests validate v4 trimmed segment counts
- Branch question keys (Q2B, Q4B) have test coverage
- FallbackTTS key count matches actual 61 keys
- Zero regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/29-integration-validation/29-02-SUMMARY.md`
</output>
