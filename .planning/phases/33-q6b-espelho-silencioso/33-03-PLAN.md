---
phase: 33-q6b-espelho-silencioso
plan: 03
type: execute
wave: 3
depends_on: [33-01, 33-02]
files_modified:
  - public/audio/prerecorded/paraiso_q6b_setup.mp3
  - public/audio/prerecorded/paraiso_q6b_pergunta.mp3
  - public/audio/prerecorded/paraiso_q6b_resposta_a.mp3
  - public/audio/prerecorded/paraiso_q6b_resposta_b.mp3
  - public/audio/prerecorded/fallback_q6b.mp3
  - public/audio/prerecorded/timeout_q6b.mp3
  - public/audio/prerecorded/devolucao_espelho_silencioso.mp3
  - scripts/validate-timing.ts
  - public/roteiro.html
autonomous: true
requirements: [BR-03, AR-01, POL-01]

must_haves:
  truths:
    - "7 new MP3 files exist in public/audio/prerecorded/: 6 Q6B (setup, pergunta, resposta_a, resposta_b, fallback_q6b, timeout_q6b) + 1 devolucao_espelho_silencioso"
    - "All 7 MP3s are generated from SCRIPT keys added in Phase 33-01 via `npx tsx scripts/generate-audio-v3.ts` (auto-discovery — no manual key list)"
    - "Total MP3 count is 73 (Phase 32 baseline) + 7 (Phase 33) = 80 MP3 files in public/audio/prerecorded/"
    - "All 7 new MP3s use voice ID PznTnBc8X6pvixs9UkQm with model eleven_v3 (matching Phase 31/32)"
    - "scripts/validate-timing.ts ALL_PATHS matrix expanded from 12 paths (Phase 32) to 20 paths (Phase 33)"
    - "20-path matrix correctly enforces Q5B and Q6B mutual exclusion (Q5B requires q4='A' && q5='A'; Q6B requires q5='B' && q6='A' — cannot both fire in same path)"
    - "20-path matrix correctly enforces hasEspelhoSilencioso implies hasQ6B (ESPELHO only reachable via Q6B branch)"
    - "pickLongestDevolucao helper accepts hasEspelhoSilencioso flag and returns DEVOLUCAO_ESPELHO_SILENCIOSO duration when set (overriding archetype lookup)"
    - "validate-timing.ts exits with code 0 (all 20 paths ≤ 450 seconds / 7:30 budget)"
    - "max-path duration documented in script output (expected ≤ 7:00, ideally ≤ 6:30 based on Phase 33-RESEARCH estimates)"
    - "public/roteiro.html Mermaid flowchart has Q6B node + edges + class declaration (q6bBranch class, branch color)"
    - "public/roteiro.html has Q6B branch indicator HTML block (similar to existing Q1B/Q5B indicator blocks)"
    - "public/roteiro.html mentions DEVOLUCAO_ESPELHO_SILENCIOSO in devoluções list (9 total archetypes now: 8 existing + ESPELHO)"
    - "public/roteiro.html 'Ramificações' count updated: was 2 (Q1B, Q5B) → now 3 (Q1B, Q5B, Q6B)"
    - "patternMatching.ts still byte-identical to master (POL-02 invariant — carried forward from Wave 2)"
    - "Full test suite `npm test` passes with zero regressions (246 Phase 32 baseline + Phase 33 additions from Wave 1+2)"
  artifacts:
    - path: public/audio/prerecorded/paraiso_q6b_setup.mp3
      provides: "Q6B_SETUP narration audio"
      min_bytes: 5000
    - path: public/audio/prerecorded/paraiso_q6b_pergunta.mp3
      provides: "Q6B_PERGUNTA narration audio — 'Resposta — ou outra pergunta?'"
      min_bytes: 3000
    - path: public/audio/prerecorded/paraiso_q6b_resposta_a.mp3
      provides: "Q6B_RESPOSTA_A narration audio (closed reading path)"
      min_bytes: 3000
    - path: public/audio/prerecorded/paraiso_q6b_resposta_b.mp3
      provides: "Q6B_RESPOSTA_B narration audio (open question path → ESPELHO)"
      min_bytes: 3000
    - path: public/audio/prerecorded/fallback_q6b.mp3
      provides: "Q6B fallback narration audio"
      min_bytes: 2000
    - path: public/audio/prerecorded/timeout_q6b.mp3
      provides: "Q6B timeout narration audio (silence → Resposta default)"
      min_bytes: 3000
    - path: public/audio/prerecorded/devolucao_espelho_silencioso.mp3
      provides: "DEVOLUCAO_ESPELHO_SILENCIOSO full 6-segment narration (1 MP3 concatenated)"
      min_bytes: 15000
    - path: scripts/validate-timing.ts
      provides: "20-path timing matrix with Q6B + ESPELHO coverage"
      contains: ["hasQ6B", "hasEspelhoSilencioso", "pickLongestDevolucao"]
    - path: public/roteiro.html
      provides: "Client-facing script + decision tree with Q6B branch and ESPELHO_SILENCIOSO archetype"
      contains: ["Q6B", "Espelho Silencioso", "3 ramificações"]
  key_links:
    - from: "validate-timing.ts ALL_PATHS matrix"
      to: "pickLongestDevolucao(hasEspelhoSilencioso)"
      via: "path.hasEspelhoSilencioso flag drives devolução duration choice"
      pattern: "hasEspelhoSilencioso.*pickLongestDevolucao"
    - from: "roteiro.html Mermaid graph"
      to: "Q6B branch node + ESPELHO archetype"
      via: "visual flowchart parity with src/data/script.ts"
      pattern: "Q6B.*Q6B_PERGUNTA.*Q6B_RESPOSTA"
    - from: "scripts/generate-audio-v3.ts"
      to: "SCRIPT keys from src/data/script.ts"
      via: "Object.keys(SCRIPT) auto-discovery"
      pattern: "Object\\.keys\\(SCRIPT\\)"
---

<objective>
Wave 3 of Phase 33: ship the audio artifacts, validate the timing budget across all 20 reachable paths (Q1B×Q5B×Q6B×Q2B×Q4B permutations with mutual-exclusion constraints), and update the client-facing roteiro.html with the new Q6B branch and ESPELHO_SILENCIOSO archetype.

Purpose:
- Satisfy BR-03 at the audio level (6 Q6B MP3s playable from FallbackTTS)
- Satisfy AR-01 at the audio level (1 ESPELHO_SILENCIOSO MP3 playable from FallbackTTS)
- Satisfy POL-01 at the timing level (all 20 paths ≤ 7:30 budget)
- Satisfy POL-03-adjacent at the documentation level (roteiro.html in sync with script.ts and machine)
- Preserve POL-02 invariant: patternMatching.ts still byte-identical to master
- Preserve Phase 31 + Phase 32 + Wave 1 + Wave 2 test baseline (zero regression)

Output:
- 7 new MP3 files in public/audio/prerecorded/ (80 total, up from 73)
- scripts/validate-timing.ts with 20-path matrix, pickLongestDevolucao helper updated, exit 0
- public/roteiro.html with Q6B branch visible in Mermaid flowchart + Q6B indicator block + ESPELHO archetype mention + updated ramificações count
- Final npm test pass (zero regression)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-q6b-espelho-silencioso/33-RESEARCH.md
@.planning/phases/33-q6b-espelho-silencioso/33-VALIDATION.md
@.planning/phases/33-q6b-espelho-silencioso/33-01-PLAN.md
@.planning/phases/33-q6b-espelho-silencioso/33-02-PLAN.md
@.planning/phases/31-q1b-branch-inferno/31-03-SUMMARY.md
@.planning/phases/32-q5b-branch-paraiso-gap-closure/32-03-SUMMARY.md
@src/data/script.ts
@scripts/generate-audio-v3.ts
@scripts/validate-timing.ts
@public/roteiro.html

<interfaces>
<!-- Critical contracts from existing tooling. -->

From scripts/generate-audio-v3.ts (stable Phase 31/32):
- Script auto-discovers keys via `Object.keys(SCRIPT)` from src/data/script.ts
- Voice ID: PznTnBc8X6pvixs9UkQm
- Model: eleven_v3 (with audio tags rendered)
- Output format: mp3_44100_192
- Destination: public/audio/prerecorded/{key_lowercase}.mp3
- Concatenates multi-segment array entries into ONE MP3 per key (e.g., DEVOLUCAO_ESPELHO_SILENCIOSO with 6 segments → 1 MP3)
- Skips existing files unless FORCE=true env var set
- Env vars needed: ELEVENLABS_API_KEY, ELEVENLABS_VOICE_ID=PznTnBc8X6pvixs9UkQm, USE_V3_MODEL=true

From scripts/validate-timing.ts (Phase 32 baseline):
```typescript
// Current Phase 32 shape (12 paths):
interface PathDef {
  name: string;
  keys: string[];          // SCRIPT keys to sum durations
  hasQ1B?: boolean;
  hasQ5B?: boolean;
  hasQ2B?: boolean;
  hasQ4B?: boolean;
  archetype: string;       // which DEVOLUCAO_* to add
}

const ALL_PATHS: PathDef[] = [
  // 12 paths from Phase 32
];

const BUDGET_SECONDS = 450;  // 7:30

function pickLongestDevolucao(path: PathDef): number {
  // Returns duration of the DEVOLUCAO_* archetype for this path
}

// Main loop: sum key durations + devolução, check ≤ BUDGET_SECONDS, exit 1 on failure
```

From public/roteiro.html (Phase 32 baseline):
- Mermaid flowchart block (```mermaid ... ``` inside HTML)
- Existing Q1B and Q5B branch nodes with branchQ1B / branchQ5B classes
- Existing Q1B/Q5B indicator HTML blocks (showing "Ramificação condicional" + trigger)
- Devoluções list section with 8 archetypes
- "Ramificações condicionais" count line near top

**Q6B/ESPELHO mutual-exclusion rules for 20-path matrix:**
1. Q5B requires q4='A' AND q5='A' → Q5B triggers Q5B subflow
2. Q6B requires q5='B' AND q6='A' → Q6B triggers Q6B subflow
3. Q5B and Q6B are MUTUALLY EXCLUSIVE (need different q5 values)
4. ESPELHO requires q6b='B' → implies Q6B fired → implies q5='B' && q6='A'
5. Q2B requires q1='A' AND q2='A' → independent of Q5B/Q6B
6. Q4B requires q3='A' AND q4='A' → independent of Q5B/Q6B

**Path enumeration for 20 paths:**
- Base: 4 paths (Q2B×Q4B combinations)
- + Q1B: add 2 paths (shouldBranchQ1B triggers when q1='B' && q2='B' — note: excludes Q2B since Q2B needs q1='A')
  Wait — Q1B trigger is q1='B' && q2='B'. Q2B trigger is q1='A' && q2='A'. Mutually exclusive.
- Re-enumerate carefully:
  - Path 1: no branches
  - Path 2: Q2B only (q1=A,q2=A,... q3=B,q4=B, q5=A,q6=B ...)
  - Path 3: Q4B only (q1=B,q2=A,q3=A,q4=A,q5=?,q6=?)
  - Path 4: Q2B + Q4B (q1=A,q2=A,q3=A,q4=A)
  - Path 5: Q1B only (q1=B,q2=B,... must avoid Q4B/Q5B/Q6B triggers)
  - Path 6: Q1B + Q4B (q1=B,q2=B,q3=A,q4=A)
  - Path 7: Q5B only (q4=A,q5=A,... must have q1/q2 not both 'B' or not both 'A' based on Q2B)
  - Path 8: Q5B + Q2B (q1=A,q2=A,q4=A,q5=A — q3=?)
  - Path 9: Q5B + Q4B (q3=A,q4=A,q5=A — wait Q4B requires q3=A && q4=A, and Q5B requires q4=A && q5=A. Compatible.)
  - Path 10: Q5B + Q2B + Q4B
  - Path 11: Q5B + Q1B (q1=B,q2=B,q4=A,q5=A)
  - Path 12: Q5B + Q1B + Q4B (q1=B,q2=B,q3=A,q4=A,q5=A)
  - Path 13: Q6B only (q5=B,q6=A — must avoid Q5B which needs q5=A)
  - Path 14: Q6B + Q2B (q1=A,q2=A,q5=B,q6=A)
  - Path 15: Q6B + Q4B (q3=A,q4=A,q5=B,q6=A)
  - Path 16: Q6B + Q2B + Q4B (q1=A,q2=A,q3=A,q4=A,q5=B,q6=A)
  - Path 17: Q6B + Q1B (q1=B,q2=B,q5=B,q6=A)
  - Path 18: Q6B + Q1B + Q4B (q1=B,q2=B,q3=A,q4=A,q5=B,q6=A)
  - Path 19: Q6B + ESPELHO (q5=B,q6=A,q6b=B) — same as Path 13 but with q6b='B' → ESPELHO devolução instead of normal
  - Path 20: Q6B + Q2B + Q4B + ESPELHO (worst case — longest branch stack with ESPELHO) → MAX PATH CANDIDATE

**Critical:** Paths 13-20 are Q6B paths. Paths 19-20 have hasEspelhoSilencioso=true. The goal is to prove max-path stays ≤ 450s even with Q6B + ESPELHO.
</interfaces>

<critical_patterns>
**ANCHOR 1 — Audio Generation is Auto-Discovery**
`scripts/generate-audio-v3.ts` reads `Object.keys(SCRIPT)` and generates any MP3 that doesn't already exist. You do NOT need to edit the script — just ensure Wave 1 added the 7 new SCRIPT keys (PARAISO_Q6B_SETUP, PARAISO_Q6B_PERGUNTA, PARAISO_Q6B_RESPOSTA_A, PARAISO_Q6B_RESPOSTA_B, FALLBACK_Q6B, TIMEOUT_Q6B, DEVOLUCAO_ESPELHO_SILENCIOSO) and run the script.

**ANCHOR 2 — ESPELHO_SILENCIOSO is ONE MP3 (6 segments concatenated)**
The script key `DEVOLUCAO_ESPELHO_SILENCIOSO` is an array of 6 segments (with pauseAfter and inflection tags). generate-audio-v3.ts concatenates these into ONE MP3. This matches the existing archetype packing pattern (e.g., DEVOLUCAO_MIRROR is 1 MP3 containing multiple text segments).

**ANCHOR 3 — Mutual Exclusion Rules in Timing Matrix**
- Q5B and Q6B cannot coexist (need different q5 values)
- ESPELHO implies Q6B (hasEspelhoSilencioso → hasQ6B)
- Q1B and Q2B cannot coexist (q1 must differ: Q1B needs q1='B', Q2B needs q1='A')
- Enforce these in the PathDef construction OR via a validation assertion at the top of the script

**ANCHOR 4 — Budget Ceiling**
BUDGET_SECONDS = 450 (7:30). Max-path = longest sum across all 20 paths. 33-RESEARCH.md estimates ~6:07-6:28 worst case for Q6B paths, well under budget. If a path exceeds 450s, script exits 1 — Phase 33 fails POL-01.

**ANCHOR 5 — pickLongestDevolucao Update**
The helper currently picks DEVOLUCAO duration by archetype. Phase 33 adds ESPELHO_SILENCIOSO which is NOT an archetype (it's a pre-archetype check). Extend the signature:
```typescript
function pickLongestDevolucao(path: PathDef): number {
  if (path.hasEspelhoSilencioso) {
    return getDuration('DEVOLUCAO_ESPELHO_SILENCIOSO');
  }
  // existing archetype switch
}
```

**ANCHOR 6 — Roteiro.html Mermaid Syntax Fragility**
Mermaid is whitespace/syntax sensitive. When adding Q6B nodes, copy an existing branch block (Q1B or Q5B) byte-for-byte and mutate. Specifically:
- Add node: `Q6B_SETUP[Q6B · O Espelho Extra]:::q6bBranch`
- Add edges: `Q6_RESPOSTA_A -->|q5=B && q6=A| Q6B_SETUP` and `Q6B_RESPOSTA_B -->|q6b=B| DEVOLUCAO_ESPELHO_SILENCIOSO`
- Add class declaration: `classDef q6bBranch fill:#... stroke:#...`
- The Mermaid block MUST still render in a browser (manual verification smoke test)
</critical_patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate 7 new MP3s via generate-audio-v3.ts</name>
  <files>public/audio/prerecorded/paraiso_q6b_setup.mp3, public/audio/prerecorded/paraiso_q6b_pergunta.mp3, public/audio/prerecorded/paraiso_q6b_resposta_a.mp3, public/audio/prerecorded/paraiso_q6b_resposta_b.mp3, public/audio/prerecorded/fallback_q6b.mp3, public/audio/prerecorded/timeout_q6b.mp3, public/audio/prerecorded/devolucao_espelho_silencioso.mp3</files>
  <read_first>
    1. Read `scripts/generate-audio-v3.ts` to confirm:
       - It auto-discovers keys via `Object.keys(SCRIPT)` from src/data/script.ts
       - It skips existing MP3 files by default
       - Env vars it expects: ELEVENLABS_API_KEY, ELEVENLABS_VOICE_ID, USE_V3_MODEL
    2. List current MP3 count:
       ```
       ls public/audio/prerecorded/*.mp3 | wc -l
       ```
       Phase 32 baseline = 73. Expected after this task = 80.
    3. Confirm Wave 1 added the 7 SCRIPT keys by grepping:
       ```
       grep -E "(PARAISO_Q6B_|FALLBACK_Q6B|TIMEOUT_Q6B|DEVOLUCAO_ESPELHO_SILENCIOSO)" src/data/script.ts
       ```
       Must return 7 key definitions. If missing, STOP — Wave 1 incomplete.
  </read_first>
  <action>
    1. Confirm environment variables are set:
       ```
       echo %ELEVENLABS_API_KEY% (Windows) or echo $ELEVENLABS_API_KEY (bash)
       ```
       If unset, STOP and request user set ELEVENLABS_API_KEY before proceeding.

    2. Run the generator:
       ```
       npx tsx scripts/generate-audio-v3.ts
       ```
       Expected behavior: script auto-detects the 7 missing MP3s and generates each one via ElevenLabs API. Existing 73 MP3s are skipped.

    3. Verify all 7 new files exist:
       ```
       ls public/audio/prerecorded/paraiso_q6b_setup.mp3
       ls public/audio/prerecorded/paraiso_q6b_pergunta.mp3
       ls public/audio/prerecorded/paraiso_q6b_resposta_a.mp3
       ls public/audio/prerecorded/paraiso_q6b_resposta_b.mp3
       ls public/audio/prerecorded/fallback_q6b.mp3
       ls public/audio/prerecorded/timeout_q6b.mp3
       ls public/audio/prerecorded/devolucao_espelho_silencioso.mp3
       ```
       All 7 must exist.

    4. Verify total MP3 count is 80:
       ```
       ls public/audio/prerecorded/*.mp3 | wc -l
       # Expected: 80
       ```

    5. Verify file sizes are non-trivial (>2KB each, ESPELHO >15KB because 6 segments):
       ```
       ls -la public/audio/prerecorded/paraiso_q6b_*.mp3 public/audio/prerecorded/fallback_q6b.mp3 public/audio/prerecorded/timeout_q6b.mp3 public/audio/prerecorded/devolucao_espelho_silencioso.mp3
       ```

    6. Manual spot-check (record for SUMMARY, non-blocking for this task):
       - Play paraiso_q6b_pergunta.mp3 — should say "Resposta — ou outra pergunta?" with rising question intonation
       - Play devolucao_espelho_silencioso.mp3 — should contain all 6 segments with audible pauses, ending with "Só pergunta."
       - No audible glitches, voice ID consistent with existing Phase 31/32 MP3s

    7. If any MP3 has issues (wrong voice, glitches, missing text), DELETE it and re-run generate-audio-v3.ts with FORCE=true to regenerate.

    DO NOT commit the MP3s yet — Task 4 final regression validates first.
  </action>
  <verify>
    <automated>test -f public/audio/prerecorded/paraiso_q6b_setup.mp3 && test -f public/audio/prerecorded/paraiso_q6b_pergunta.mp3 && test -f public/audio/prerecorded/paraiso_q6b_resposta_a.mp3 && test -f public/audio/prerecorded/paraiso_q6b_resposta_b.mp3 && test -f public/audio/prerecorded/fallback_q6b.mp3 && test -f public/audio/prerecorded/timeout_q6b.mp3 && test -f public/audio/prerecorded/devolucao_espelho_silencioso.mp3</automated>
    <automated>ls public/audio/prerecorded/*.mp3 | wc -l (expect 80)</automated>
  </verify>
  <done>
    - All 7 new MP3s exist in public/audio/prerecorded/
    - Total count is 80 (73 baseline + 7 new)
    - File sizes are non-trivial (spot check via ls -la)
    - Manual spot check logged for SUMMARY (voice consistency, no glitches)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand validate-timing.ts ALL_PATHS matrix from 12 → 20 paths with Q6B + ESPELHO coverage</name>
  <files>scripts/validate-timing.ts</files>
  <read_first>
    1. Read `scripts/validate-timing.ts` end-to-end to understand:
       - Current PathDef interface shape (hasQ1B, hasQ5B, hasQ2B, hasQ4B flags)
       - Current ALL_PATHS array (12 paths from Phase 32)
       - pickLongestDevolucao helper signature
       - BUDGET_SECONDS constant
       - MP3 duration read mechanism (probably uses ffprobe or file-size estimate or hardcoded key durations)
    2. Read the test output from Phase 32's last run (if logged in 32-03-SUMMARY.md) to confirm the baseline max-path was ~6:53.7 min.
  </read_first>
  <behavior>
    Test 1 — ALL_PATHS has exactly 20 entries (no more, no less)

    Test 2 — Mutual exclusion is enforced at the matrix level:
      - No path has both hasQ5B=true AND hasQ6B=true
      - Every path with hasEspelhoSilencioso=true also has hasQ6B=true
      - No path has both hasQ1B=true AND hasQ2B=true

    Test 3 — Q6B paths include required keys:
      - Every path with hasQ6B=true includes PARAISO_Q6B_SETUP, PARAISO_Q6B_PERGUNTA in keys array
      - Every such path includes either PARAISO_Q6B_RESPOSTA_A or PARAISO_Q6B_RESPOSTA_B

    Test 4 — ESPELHO paths use DEVOLUCAO_ESPELHO_SILENCIOSO:
      - Every path with hasEspelhoSilencioso=true: pickLongestDevolucao returns the DEVOLUCAO_ESPELHO_SILENCIOSO duration (NOT an archetype like DEVOLUCAO_MIRROR)

    Test 5 — All 20 paths are under budget:
      - Run the script end-to-end
      - Every path sum (keys + devolução) ≤ 450 seconds
      - Script exits with code 0

    Test 6 — Max-path is identified and logged:
      - Script prints "Max path: {name} — {duration}" in output
      - Max path is one of paths 19-20 (Q6B + ESPELHO, with branch stacking)

    Test 7 — Phase 32 baseline paths still validate:
      - All 12 original Phase 32 paths are still present (renumbered if needed) with same key lists
      - Their max-path (6:53.7 for Q5B+Q1B+Q4B) still holds
  </behavior>
  <action>
    1. Open `scripts/validate-timing.ts`.

    2. Extend the PathDef interface:
    ```typescript
    interface PathDef {
      name: string;
      keys: string[];
      hasQ1B?: boolean;
      hasQ2B?: boolean;
      hasQ4B?: boolean;
      hasQ5B?: boolean;
      hasQ6B?: boolean;              // NEW
      hasEspelhoSilencioso?: boolean; // NEW (implies hasQ6B)
      archetype: string;
    }
    ```

    3. Update `pickLongestDevolucao` to check hasEspelhoSilencioso FIRST:
    ```typescript
    function pickLongestDevolucao(path: PathDef): number {
      // Phase 33 — ESPELHO_SILENCIOSO has HIGHEST priority (mirrors DEVOLUCAO.always[0] in oracleMachine.ts)
      if (path.hasEspelhoSilencioso) {
        return getDuration('DEVOLUCAO_ESPELHO_SILENCIOSO');
      }
      // existing archetype switch (unchanged)
      switch (path.archetype) {
        case 'MIRROR': return getDuration('DEVOLUCAO_MIRROR');
        // ... etc
      }
    }
    ```

    4. Add 8 new Q6B paths to ALL_PATHS (extend from 12 to 20). Use this verbatim structure (adjust key names to match the actual SCRIPT key names in src/data/script.ts):

    ```typescript
    const ALL_PATHS: PathDef[] = [
      // ... 12 existing Phase 32 paths (UNCHANGED) ...

      // ========== Phase 33 — Q6B paths (8 new) ==========

      // Path 13: Q6B only (no other branches, normal devolução)
      {
        name: 'Q6B only → normal devolução',
        keys: [
          'APRESENTACAO',
          'INFERNO_SETUP', 'INFERNO_Q1_SETUP', 'INFERNO_Q1_PERGUNTA', 'INFERNO_Q1_RESPOSTA_A', // q1=A (not-Q1B, not-Q2B via q2=B)
          'INFERNO_Q2_SETUP', 'INFERNO_Q2_PERGUNTA', 'INFERNO_Q2_RESPOSTA_B',
          'PURGATORIO_SETUP',
          'PURGATORIO_Q3_SETUP', 'PURGATORIO_Q3_PERGUNTA', 'PURGATORIO_Q3_RESPOSTA_B', // q3=B (not-Q4B)
          'PURGATORIO_Q4_SETUP', 'PURGATORIO_Q4_PERGUNTA', 'PURGATORIO_Q4_RESPOSTA_B',
          'PARAISO_SETUP',
          'PARAISO_Q5_SETUP', 'PARAISO_Q5_PERGUNTA', 'PARAISO_Q5_RESPOSTA_B', // q5=B (Q6B trigger part 1, not-Q5B)
          'PARAISO_Q6_SETUP', 'PARAISO_Q6_PERGUNTA', 'PARAISO_Q6_RESPOSTA_A', // q6=A (Q6B trigger part 2)
          'PARAISO_Q6B_SETUP', 'PARAISO_Q6B_PERGUNTA', 'PARAISO_Q6B_RESPOSTA_A', // Q6B fires, q6b=A (normal)
          'ENCERRAMENTO',
        ],
        hasQ6B: true,
        archetype: 'SURFACE_KEEPER', // or whichever archetype the pattern q1=A,q2=B,q3=B,q4=B,q5=B,q6=A,q6b=A matches — verify against patternMatching.ts
      },

      // Path 14: Q6B + Q2B (q1=A, q2=A, q5=B, q6=A)
      // ... construct similar ...

      // Path 15: Q6B + Q4B (q3=A, q4=A, q5=B, q6=A)
      // ... construct similar ...

      // Path 16: Q6B + Q2B + Q4B (q1=A,q2=A,q3=A,q4=A,q5=B,q6=A)
      // ... construct similar ...

      // Path 17: Q6B + Q1B (q1=B,q2=B,q5=B,q6=A)
      // ... construct similar ...

      // Path 18: Q6B + Q1B + Q4B (q1=B,q2=B,q3=A,q4=A,q5=B,q6=A)
      // ... construct similar ...

      // Path 19: Q6B + ESPELHO (q6b=B — simplest ESPELHO path)
      {
        name: 'Q6B + ESPELHO_SILENCIOSO',
        keys: [
          // Same as Path 13 but Q6B_RESPOSTA_B instead of _A
          // ... keys through PARAISO_Q6_RESPOSTA_A ...
          'PARAISO_Q6B_SETUP', 'PARAISO_Q6B_PERGUNTA', 'PARAISO_Q6B_RESPOSTA_B',
          'ENCERRAMENTO',
        ],
        hasQ6B: true,
        hasEspelhoSilencioso: true,
        archetype: 'ESPELHO_SILENCIOSO', // unused when hasEspelhoSilencioso=true but keep for docs
      },

      // Path 20: Q6B + Q2B + Q4B + ESPELHO (worst case for ESPELHO path)
      {
        name: 'Q6B + Q2B + Q4B + ESPELHO_SILENCIOSO (MAX-ESPELHO)',
        keys: [
          // q1=A,q2=A,q3=A,q4=A,q5=B,q6=A,q6b=B with Q2B + Q4B branches
          // ...
        ],
        hasQ2B: true,
        hasQ4B: true,
        hasQ6B: true,
        hasEspelhoSilencioso: true,
        archetype: 'ESPELHO_SILENCIOSO',
      },
    ];
    ```

    5. Add assertion helper at top of ALL_PATHS construction (mutual exclusion enforcement):
    ```typescript
    function assertValidPath(p: PathDef): void {
      if (p.hasQ5B && p.hasQ6B) throw new Error(`Path "${p.name}": Q5B and Q6B are mutually exclusive`);
      if (p.hasEspelhoSilencioso && !p.hasQ6B) throw new Error(`Path "${p.name}": ESPELHO requires Q6B`);
      if (p.hasQ1B && p.hasQ2B) throw new Error(`Path "${p.name}": Q1B and Q2B are mutually exclusive`);
    }
    ALL_PATHS.forEach(assertValidPath);
    ```

    6. Verify the key names in every new path match actual SCRIPT keys from src/data/script.ts. Cross-reference with `grep "PARAISO_Q6B\\|DEVOLUCAO_ESPELHO_SILENCIOSO" src/data/script.ts`.

    7. Run:
       ```
       npx tsx scripts/validate-timing.ts
       ```
       Expected output:
       - No assertion errors (mutual exclusion holds)
       - All 20 paths validated
       - Max-path printed (expect ≤ 7:00 min, ideally ≤ 6:30)
       - Exit code 0

    8. If any path exceeds BUDGET_SECONDS (450s), STOP and:
       - Log which path(s) overflow
       - Calculate delta
       - Decide: document overflow as acceptable per v6.0 decision (up to 1-3% visitors acceptable) OR cut a SETUP key per POL-01 mitigation strategy
       - Do NOT cut without user approval — record blocker and return to orchestrator

    Reference: 33-RESEARCH.md Section 5.6 has the 20-path enumeration and duration estimates.
  </action>
  <verify>
    <automated>npx tsx scripts/validate-timing.ts</automated>
    <automated>grep -c "hasQ6B" scripts/validate-timing.ts (expect ≥8 — flag definition + 8 paths)</automated>
    <automated>grep -c "hasEspelhoSilencioso" scripts/validate-timing.ts (expect ≥3 — flag definition + 2 paths + pickLongestDevolucao branch)</automated>
  </verify>
  <done>
    - ALL_PATHS has 20 entries (12 Phase 32 + 8 Phase 33)
    - PathDef interface extended with hasQ6B and hasEspelhoSilencioso flags
    - pickLongestDevolucao handles hasEspelhoSilencioso (returns DEVOLUCAO_ESPELHO_SILENCIOSO duration)
    - Mutual exclusion assertions in place (Q5B/Q6B, Q1B/Q2B, ESPELHO requires Q6B)
    - Script exits 0 (all paths ≤ 450s)
    - Max-path logged in output and recorded for SUMMARY
  </done>
</task>

<task type="auto">
  <name>Task 3: Update public/roteiro.html — Mermaid Q6B branch + ESPELHO archetype + ramificações count</name>
  <files>public/roteiro.html</files>
  <read_first>
    1. Read `public/roteiro.html` top-to-bottom to understand:
       - Mermaid flowchart structure
       - Where Q1B (Phase 31) and Q5B (Phase 32) branch nodes and edges are defined
       - The existing q1bBranch and q5bBranch classDef declarations
       - Where the "Ramificações condicionais" count is stated (likely near top of page)
       - Where the devoluções archetype list is (probably 8 items currently)
       - Where the Q1B/Q5B indicator HTML blocks live (outside the Mermaid code block)
    2. Note: Mermaid is whitespace/syntax sensitive — any typo breaks rendering. Copy Q5B patterns byte-for-byte and mutate.
  </read_first>
  <action>
    1. **Update Mermaid flowchart — add Q6B nodes and edges:**

    Find the existing Q5B branch block in the Mermaid section. Add immediately after it (or near the Q6 states):

    ```mermaid
    %% Phase 33 — Q6B branch: "O Espelho Extra"
    Q6B_SETUP[Q6B · O Espelho Extra]:::q6bBranch
    Q6B_PERGUNTA[Resposta — ou outra pergunta?]:::q6bBranch
    Q6B_RESPOSTA_A[Resposta fechada]:::q6bBranch
    Q6B_RESPOSTA_B[Outra pergunta — abertura]:::q6bBranch

    Q6_RESPOSTA_A -->|q5=B && q6=A| Q6B_SETUP
    Q6B_SETUP --> Q6B_PERGUNTA
    Q6B_PERGUNTA --> Q6B_RESPOSTA_A
    Q6B_PERGUNTA --> Q6B_RESPOSTA_B
    Q6B_RESPOSTA_A --> DEVOLUCAO
    Q6B_RESPOSTA_B -->|q6b=B| DEVOLUCAO_ESPELHO_SILENCIOSO

    DEVOLUCAO_ESPELHO_SILENCIOSO[Devolução · Espelho Silencioso]:::devolucao
    DEVOLUCAO_ESPELHO_SILENCIOSO --> ENCERRAMENTO
    ```

    2. **Add q6bBranch classDef:**

    Find the existing `classDef q5bBranch` line. Immediately after, add:
    ```mermaid
    classDef q6bBranch fill:#3d2a4d,stroke:#b794f4,stroke-width:2px,color:#f5f0ff
    ```
    (Adjust colors to match the visual style of q1bBranch/q5bBranch classDefs — use a purple/lavender tone to differentiate from Q1B and Q5B.)

    3. **Update "Ramificações condicionais" count:**

    Find the line stating "2 ramificações" (or similar — Phase 31 set it to 1, Phase 32 bumped to 2, Phase 33 bumps to 3):
    ```html
    <!-- BEFORE: -->
    <p>2 ramificações condicionais (Q1B, Q5B)</p>
    <!-- AFTER: -->
    <p>3 ramificações condicionais (Q1B, Q5B, Q6B)</p>
    ```

    4. **Add Q6B indicator HTML block:**

    Find the existing Q1B and Q5B indicator blocks (HTML cards/sections outside Mermaid). Add a new Q6B block with the same structure:

    ```html
    <section class="branch-indicator q6b">
      <h3>Q6B · O Espelho Extra</h3>
      <p class="trigger">Dispara quando: q5='B' && q6='A' (dissolveu pergunta MAS pediu leitura)</p>
      <p class="narrative">
        "Antes de eu começar. Você lembra do que você fez lá atrás? Você deixou uma pergunta dissolver...
        Eu posso te dar uma resposta — fechada, definitiva — ou eu posso te dar outra pergunta. Aberta."
      </p>
      <p class="choice">
        <strong>Resposta</strong> (q6b='A') → devolução normal por arquétipo<br>
        <strong>Pergunta</strong> (q6b='B') → Devolução · Espelho Silencioso
      </p>
    </section>
    ```

    5. **Update devoluções archetype list:**

    Find the existing list of 8 devoluções. Add ESPELHO_SILENCIOSO at the TOP (matching DEVOLUCAO.always[0] priority order):

    ```html
    <ul class="devolucoes">
      <li><strong>Espelho Silencioso</strong> — devolve forma em vez de conteúdo (trigger: q6b='B', HIGHEST priority)</li>
      <li>Mirror — ...</li>
      <li>Seeker — ...</li>
      <!-- ... 7 more existing ... -->
    </ul>
    ```

    6. **Update the devoluções count if one is stated:**

    If the page states "8 devoluções arquetípicas", update to "9 devoluções (8 arquétipos + Espelho Silencioso)".

    7. **Add DEVOLUCAO_ESPELHO_SILENCIOSO narrative excerpt** (optional but recommended for client review):

    Add to the devoluções section:
    ```html
    <blockquote class="devolucao-espelho">
      <p>"Tudo bem. Você não vai sair daqui com um nome. Você vai sair com um silêncio que tem forma..."</p>
      <p>"O que você nunca pediu — mas que mesmo assim te falta?"</p>
      <p>"Esse é o seu espelho. Não tem moldura. Não tem reflexo. Só pergunta."</p>
    </blockquote>
    ```

    8. **Verification — open in browser (manual smoke):**

    The task is structurally complete when:
    - Mermaid block contains Q6B nodes and edges (grep check below)
    - Classdef q6bBranch exists
    - Ramificações count says "3"
    - ESPELHO mentioned in devoluções list

    Manual smoke (recorded for SUMMARY): Open `public/roteiro.html` in Chrome/Firefox, verify the Mermaid diagram renders without syntax errors and the new Q6B branch is visually visible with its own color. If Mermaid shows a parse error, fix the typo and retry.

    Reference: Phase 32's 32-03-SUMMARY.md has the Q5B roteiro diff as a template.
  </action>
  <verify>
    <automated>grep -c "Q6B" public/roteiro.html (expect ≥5 — multiple Q6B references)</automated>
    <automated>grep -ci "espelho" public/roteiro.html (expect ≥3 — multiple ESPELHO references)</automated>
    <automated>grep -c "q6bBranch" public/roteiro.html (expect ≥2 — classDef + at least one usage)</automated>
    <automated>grep -c "3 ramifica" public/roteiro.html (expect ≥1 — count update)</automated>
    <automated>grep -c "DEVOLUCAO_ESPELHO_SILENCIOSO" public/roteiro.html (expect ≥1 — Mermaid node)</automated>
  </verify>
  <done>
    - Mermaid flowchart has Q6B_SETUP, Q6B_PERGUNTA, Q6B_RESPOSTA_A, Q6B_RESPOSTA_B nodes
    - Mermaid has q6bBranch classDef declaration
    - Ramificações count updated to 3 (Q1B, Q5B, Q6B)
    - Q6B indicator HTML block added outside Mermaid
    - Devoluções list includes Espelho Silencioso at the top
    - ESPELHO narrative excerpt block (optional) added
    - Manual browser smoke check logged for SUMMARY (Mermaid renders, Q6B visible)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Phase 33 final regression — full test suite + timing + POL-02 + smoke</name>
  <files>src/machines/__tests__/oracleMachine.test.ts, src/components/experience/__tests__/OracleExperience-helpers.test.ts, src/data/__tests__/script.test.ts, src/types/__tests__/index.test.ts, scripts/validate-timing.ts, src/machines/guards/patternMatching.ts</files>
  <read_first>
    1. No new reads — all files are in working memory from Tasks 1-3 of this plan and Waves 1-2.
  </read_first>
  <behavior>
    Test 1 — Full npm test suite passes:
      - All Phase 31 (Q1B) tests green
      - All Phase 32 (Q5B) tests green
      - All Phase 33 Wave 1 (script + QUESTION_META[11]) tests green
      - All Phase 33 Wave 2 (machine + helpers) tests green
      - Zero regressions on pre-existing v4.0/v5.0 tests

    Test 2 — scripts/validate-timing.ts exits 0:
      - All 20 paths ≤ 450 seconds
      - Max-path logged

    Test 3 — POL-02 invariant intact:
      - git diff master -- src/machines/guards/patternMatching.ts is empty

    Test 4 — MP3 count is 80:
      - ls public/audio/prerecorded/*.mp3 | wc -l = 80

    Test 5 — roteiro.html has Q6B + ESPELHO references:
      - grep checks from Task 3 all pass

    Test 6 — Source-text structural invariants:
      - `grep "DEVOLUCAO_ESPELHO_SILENCIOSO" src/machines/oracleMachine.ts` returns ≥2 (always[0] entry + top-level state)
      - `grep "shouldBranchQ6B" src/machines/oracleMachine.ts` returns ≥2 (guard definition + Q6_RESPOSTA_A usage)
      - `grep "isEspelhoSilencioso" src/machines/oracleMachine.ts` returns ≥2 (guard definition + always[0] usage)
      - `grep "isEspelhoSilencioso\\|shouldBranchQ6B" src/machines/guards/patternMatching.ts` returns 0 (POL-02)

    Test 7 — Phase 33 requirements all demonstrably covered:
      - BR-03: Q6B SCRIPT keys exist (Wave 1) + guard + 6 states (Wave 2) + 6 MP3s (Wave 3) + roteiro.html branch (Wave 3)
      - AR-01: ESPELHO SCRIPT key exists (Wave 1) + guard + top-level state + always[0] (Wave 2) + 1 MP3 (Wave 3) + roteiro.html mention (Wave 3)
      - POL-01: validate-timing.ts 20-path matrix, max-path under budget
      - POL-02: patternMatching.ts byte-identical to master (verified throughout)
  </behavior>
  <action>
    1. Run FULL test suite:
       ```
       npm test
       ```
       Expected: ALL GREEN. Phase 32 baseline was 246 tests. Phase 33 adds:
       - Wave 1: ~18 new tests (script + types)
       - Wave 2: ~40 new tests (machine guards + Q6B sequence + ESPELHO routing + helpers)
       - Wave 3: 0 new tests
       - Total expected: ~304 tests green

       If ANYTHING is red, STOP and diagnose:
       - If a pre-existing test fails → check if Wave 2 broke it accidentally (regression)
       - If a new Phase 33 test fails → check the implementation matches the behavior spec
       - Do NOT ship with red tests

    2. Run timing validator:
       ```
       npx tsx scripts/validate-timing.ts
       ```
       Expected:
       - Exit code 0
       - 20 paths validated
       - Max-path printed (record for SUMMARY)

    3. POL-02 final check:
       ```
       git diff master -- src/machines/guards/patternMatching.ts
       ```
       Output MUST be empty. If ANY diff appears, REVERT it.

    4. MP3 count final check:
       ```
       ls public/audio/prerecorded/*.mp3 | wc -l
       ```
       Must be 80.

    5. Source-text invariant grep checks (all from behavior Test 6):
       ```
       grep -c "DEVOLUCAO_ESPELHO_SILENCIOSO" src/machines/oracleMachine.ts  # ≥2
       grep -c "shouldBranchQ6B" src/machines/oracleMachine.ts               # ≥2
       grep -c "isEspelhoSilencioso" src/machines/oracleMachine.ts           # ≥2
       grep -cE "isEspelhoSilencioso|shouldBranchQ6B" src/machines/guards/patternMatching.ts  # 0
       ```

    6. Roteiro final check (from Task 3 verify commands):
       ```
       grep -c "Q6B" public/roteiro.html                       # ≥5
       grep -ci "espelho" public/roteiro.html                  # ≥3
       grep -c "3 ramifica" public/roteiro.html                # ≥1
       ```

    7. Write a FINAL REGRESSION REPORT block for the Wave 3 SUMMARY with:
       - Full npm test count (before → after Phase 33)
       - validate-timing.ts output (20 paths, max-path, budget headroom)
       - POL-02 verification command + empty output confirmation
       - MP3 count (73 → 80)
       - All grep checks with counts

    DO NOT modify any production files. This task is verification + reporting only.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>npx tsx scripts/validate-timing.ts</automated>
    <automated>git diff master -- src/machines/guards/patternMatching.ts | wc -l (must be 0)</automated>
    <automated>ls public/audio/prerecorded/*.mp3 | wc -l (expect 80)</automated>
    <automated>grep -cE "isEspelhoSilencioso|shouldBranchQ6B" src/machines/guards/patternMatching.ts (must be 0)</automated>
    <automated>grep -c "DEVOLUCAO_ESPELHO_SILENCIOSO" src/machines/oracleMachine.ts (expect ≥2)</automated>
    <automated>grep -c "Q6B" public/roteiro.html (expect ≥5)</automated>
  </verify>
  <done>
    - npm test full suite GREEN (zero regressions on Phase 31/32/v4.0 baseline)
    - validate-timing.ts exits 0 with 20 paths under budget
    - Max-path documented (≤ 450s confirmed)
    - POL-02 invariant intact (patternMatching.ts byte-identical to master)
    - 80 MP3s in public/audio/prerecorded/
    - Roteiro.html has all Q6B + ESPELHO references
    - Source-text grep invariants all pass
    - Final regression report block drafted for SUMMARY
    - Phase 33 implementation COMPLETE — ready for /gsd:verify-work
  </done>
</task>

</tasks>

<verification>
**Phase-level verification (run after all 4 tasks complete):**

1. Full regression suite:
   ```
   npm test
   ```
   All GREEN. Zero regressions.

2. Timing budget:
   ```
   npx tsx scripts/validate-timing.ts
   ```
   Exit code 0. Max-path ≤ 450 seconds. 20 paths validated.

3. POL-02 invariant:
   ```
   git diff master -- src/machines/guards/patternMatching.ts
   ```
   Empty output.

4. MP3 manifest:
   ```
   ls public/audio/prerecorded/*.mp3 | wc -l
   ```
   Must be 80 (Phase 32 baseline 73 + Phase 33 new 7).

5. Requirements coverage summary:
   - BR-03: Q6B branch — data (33-01) + machine (33-02) + audio (33-03) + roteiro (33-03) — COMPLETE
   - AR-01: ESPELHO_SILENCIOSO archetype — data (33-01) + guard + state + always[0] (33-02) + audio (33-03) + roteiro (33-03) — COMPLETE
   - POL-01: 20-path timing matrix (33-03) — COMPLETE (subject to max-path verification)
   - POL-02: patternMatching.ts untouched throughout — CARRIED FORWARD from Wave 2

6. Manual smoke items for SUMMARY log (non-blocking for task):
   - Play 7 new MP3s, confirm voice ID PznTnBc8X6pvixs9UkQm and no glitches
   - Open public/roteiro.html in browser, confirm Mermaid renders with Q6B branch visible
</verification>

<success_criteria>
Phase 33 Wave 3 is complete when:

- [ ] 7 new MP3s exist in public/audio/prerecorded/ (Task 1)
- [ ] Total MP3 count = 80 (Task 1)
- [ ] scripts/validate-timing.ts has 20-path ALL_PATHS matrix (Task 2)
- [ ] PathDef interface extended with hasQ6B + hasEspelhoSilencioso (Task 2)
- [ ] pickLongestDevolucao handles hasEspelhoSilencioso (Task 2)
- [ ] validate-timing.ts exits 0 with all 20 paths ≤ 450s (Task 2)
- [ ] Max-path identified, logged, under budget (Task 2)
- [ ] public/roteiro.html has Q6B Mermaid nodes + edges + classDef (Task 3)
- [ ] public/roteiro.html has Q6B indicator HTML block (Task 3)
- [ ] public/roteiro.html mentions DEVOLUCAO_ESPELHO_SILENCIOSO in devoluções list (Task 3)
- [ ] public/roteiro.html ramificações count updated to 3 (Task 3)
- [ ] Full npm test suite GREEN (Task 4)
- [ ] POL-02 invariant intact (patternMatching.ts byte-identical) (Task 4)
- [ ] Source-text grep invariants all pass (Task 4)
- [ ] Manual spot-checks logged for Phase 33 SUMMARY (voice + Mermaid)
- [ ] Phase 33 ready for /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/33-q6b-espelho-silencioso/33-03-SUMMARY.md` documenting:

**Files modified:**
- 7 new MP3 files (list with sizes)
- scripts/validate-timing.ts (ALL_PATHS 12 → 20, PathDef extension, pickLongestDevolucao update)
- public/roteiro.html (Mermaid + indicator + devoluções list + ramificações count)

**Test results:**
- Full npm test count (before → after Phase 33)
- validate-timing.ts output (20 paths, max-path name, max-path duration, budget headroom)
- POL-02 verification: empty git diff
- Grep invariant checks with counts

**Audio verification:**
- 7 MP3s listed with file sizes and estimated durations
- Voice ID confirmation (PznTnBc8X6pvixs9UkQm)
- Model confirmation (eleven_v3)
- Manual spot-check notes (voice consistency, no glitches, ESPELHO 6-segment intact)

**Timing breakdown:**
- 20 paths enumerated with duration per path
- Max-path highlighted (expected: Q6B + Q2B + Q4B + ESPELHO worst case, or Q5B + Q1B + Q4B carry-over from Phase 32)
- Budget headroom in seconds

**Roteiro verification:**
- Mermaid render check (browser smoke result)
- Ramificações count update noted
- ESPELHO archetype placement in devoluções list

**Requirements closeout:**
- BR-03 complete (audio + timing + roteiro)
- AR-01 complete (audio + timing + roteiro)
- POL-01 complete (20-path matrix under budget)
- POL-02 carry-forward (invariant intact from Wave 2)

**Next steps:**
- Phase 33 complete — ready for `/gsd:verify-work`
- After verify: ready for Phase 34 (CONTRA_FOBICO + PORTADOR archetypes, AR-02 + AR-03)
- Browser UAT deferred to Phase 35 (same as Phase 31/32)
</output>
