---
phase: 35-timing-mitigation-browser-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/roteiro.html
  - CLAUDE.md
  - .planning/phases/35-timing-mitigation-browser-uat/35-01-SUMMARY.md
autonomous: true
requirements: [POL-01, POL-03]

must_haves:
  truths:
    - "scripts/validate-timing.ts still exits 0 with the existing 24-path matrix and prints 'All 24 paths fall within acceptable range.' (max-path 7:11.2 min, 18.8s headroom under 7:30 budget)"
    - "POL-01 reconciliation is documented in 35-01-SUMMARY.md: the ROADMAP.md '96 path permutations' phrasing is a stale combinatorial estimate; mutual exclusion rules (Q1B XOR Q2B, Q5B XOR Q6B, CONTRA_FOBICO implies Q1B, PORTADOR implies Q5B, ESPELHO implies Q6B) reduce the valid path count to ~32, of which the 24-path matrix in validate-timing.ts covers all branch combinations + worst-case stacking + Phase 34 archetype triggers. No validator extension is required."
    - "public/roteiro.html line 377 area no longer claims '9 archetypes' — it states 11 archetypes coherent with the priority list at line 959"
    - "public/roteiro.html overview Ramificações section (line 376 area) explicitly mentions both CONTRA_FOBICO and PORTADOR archetype triggers (in addition to Q1B/Q5B branch descriptions already present)"
    - "public/roteiro.html Mermaid flowchart (lines 385-452) is extended so that the rendered diagram shows CONTRA_FOBICO and PORTADOR as devolução targets reachable from Q1B and Q5B paths — NOT only via the existing single 'DEV' node"
    - "public/roteiro.html Mermaid central DEV node label is updated from '1 de 8 arquétipos' to '1 de 8 arquétipos baseline' (or equivalent phrasing) so the diagram is internally consistent with the priority list at line 959"
    - "CLAUDE.md status banner reads 'v6.0 Deep Branching complete' with 82 MP3s mentioned (NOT 'v4.0 Game Flow complete' / 61 MP3s)"
    - "CLAUDE.md state count says ~78 states, decision count says 11 max decisions, 5 conditional branches (Q1B/Q2B/Q4B/Q5B/Q6B) named explicitly"
    - "CLAUDE.md devolução archetype list contains all 11 archetypes including ESPELHO_SILENCIOSO, CONTRA_FOBICO, PORTADOR"
    - "CLAUDE.md MP3 count is 82 in BOTH the Service Pattern table AND the Script & Audio section — no '61 MP3s' / '61 keys' / '~54 states' / '8 questions' / '8 branching voice decisions' remnants exist anywhere"
    - "CLAUDE.md Project Structure 'public/audio/oracle/' line shows the actual folder size (run du -sh) instead of the stale ~17MB string"
    - "Full vitest run baseline preserved: matches Phase 34 closeout (≥733 passing / 16 failing / 3 skipped) — no new regressions introduced by documentation edits"
    - "35-01-SUMMARY.md exists, mentions POL-01 + POL-03, lists exact text replacements applied to CLAUDE.md and roteiro.html, and records the timing validator output"
  artifacts:
    - path: public/roteiro.html
      provides: "Client-facing script + decision tree updated for v6.0: 11 archetypes called out in overview, Mermaid flowchart shows CONTRA_FOBICO + PORTADOR routing alongside ESPELHO_SILENCIOSO, priority list parity confirmed"
      contains: ["11 arqu", "CONTRA_FOBICO", "PORTADOR"]
    - path: CLAUDE.md
      provides: "Developer guide reflecting v6.0 Deep Branching complete state: 82 MP3s, ~78 states, 11 max decisions, 11 archetypes, 5 conditional branches"
      contains: ["v6.0 Deep Branching complete", "82 MP3s", "~78 states", "11 decision points", "11 devolucao archetypes", "Q1B, Q2B, Q4B, Q5B, Q6B"]
    - path: .planning/phases/35-timing-mitigation-browser-uat/35-01-SUMMARY.md
      provides: "Wave 1 completion summary documenting POL-01 reconciliation and POL-03 documentation sync — input for Phase 35 verification"
      contains: ["POL-01", "POL-03", "24 paths"]
  key_links:
    - from: "scripts/validate-timing.ts (existing, unchanged)"
      to: "35-01-SUMMARY.md POL-01 reconciliation paragraph"
      via: "documented evidence that 24 paths covers the valid permutation space"
      pattern: "All 24 paths fall within acceptable range"
    - from: "public/roteiro.html overview line 377"
      to: "public/roteiro.html priority list line 959"
      via: "matching 11-archetype claim across both sections"
      pattern: "11 arqu"
    - from: "public/roteiro.html Mermaid flowchart"
      to: "DEVOLUCAO_CONTRA_FOBICO + DEVOLUCAO_PORTADOR nodes"
      via: "new flowchart edges from Q1B/Q5B branches into archetype-specific devolução targets"
      pattern: "CONTRA_FOBICO|PORTADOR"
    - from: "CLAUDE.md status banner + Architecture section + Service Pattern table"
      to: "internally consistent v6.0 stats (82 MP3s, ~78 states, 11 archetypes, 11 decisions)"
      via: "single-pass edit covering all 7 stale stat locations"
      pattern: "82 MP3s|~78 states|11 decision|11 devolucao archetypes|v6\\.0 Deep Branching complete"
---

<objective>
Wave 1 of Phase 35: reconcile POL-01 (no validator changes — document why 24 paths is sufficient) and satisfy POL-03 (update public/roteiro.html and CLAUDE.md so client + developer documentation matches the v6.0 runtime). This is a DOCUMENTATION-ONLY plan: zero TypeScript changes, zero new tests, zero new MP3s.

Purpose:
- Record the resolution of POL-01 (the "96 path permutations" claim in ROADMAP.md was a stale combinatorial estimate; mutual exclusion rules collapse the valid space to ~32; the existing 24-path matrix already covers all branches + worst cases with 18.8s timing headroom)
- Bring public/roteiro.html into parity with src/data/script.ts (4 surgical edits: line 377 archetype count, overview Ramificações mentions for CONTRA_FOBICO + PORTADOR, Mermaid flowchart routing edges, central DEV node label tweak)
- Bring CLAUDE.md into parity with the v6.0 runtime (7 stale stat locations: status banner, state count, decision count, archetype count, MP3 count ×2, question count, plus Project Structure folder size)
- Leave the codebase ready for Wave 2 (UAT scenario document) with the regression baseline intact (≥733/16/3)

Output:
- public/roteiro.html with 4 surgical updates (overview line 377, overview Ramificações mention, Mermaid flowchart edges, central DEV node label)
- CLAUDE.md with v6.0 stats updated in 7+ locations
- .planning/phases/35-timing-mitigation-browser-uat/35-01-SUMMARY.md documenting all changes + POL-01 reconciliation rationale + grep verification results
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-timing-mitigation-browser-uat/35-RESEARCH.md
@.planning/phases/35-timing-mitigation-browser-uat/35-VALIDATION.md
@CLAUDE.md
@public/roteiro.html
@scripts/validate-timing.ts

<interfaces>
<!-- These are the EXACT current strings in CLAUDE.md and roteiro.html that this plan replaces. -->
<!-- Read these verbatim — each task below provides the literal new text to substitute. -->

CLAUDE.md current state (verified during planning):

Line 5 (current):
"Interactive voice art installation for the VII Bienal de Psicanalise e Cultura (SBPRP 2026, May 29-30). A visitor puts on headphones, the Oracle guides them through a Dante-inspired journey (Inferno → Purgatorio → Paraiso) with 8 branching voice decisions in 5-7 minutes, then delivers a personalized devolucao based on their choice pattern."

Line 7 (current):
"**Status:** v4.0 Game Flow complete. All code, script, machine, and 61 MP3s shipped. Pending: browser UAT + event deploy."

Line 13 (current):
"- **XState v5** state machine (~54 states, 8 decision points, branching)"

Line 26 (current):
"- **6 base questions** (Q1-Q6) + **2 conditional branches** (Q2B, Q4B) = 8 max decisions"

Line 27 (current):
"- Q2B triggers when Q1=A AND Q2=A (both cautious choices in Inferno)"

Line 28 (current):
"- Q4B triggers when Q3=A AND Q4=A (both engaged choices in Purgatorio)"

Line 29 (current):
"- 8 devolucao archetypes: SEEKER, GUARDIAN, CONTRADICTED, PIVOT_EARLY, PIVOT_LATE, DEPTH_SEEKER, SURFACE_KEEPER, MIRROR"

Line 40 (current — Service Pattern table):
"| TTS | ElevenLabs v3 + FallbackTTS (61 MP3s) | `createTTSService()` | `NEXT_PUBLIC_USE_REAL_APIS` |"

Line 58 (current — Script & Audio):
"- **Script:** `src/data/script.ts` — 61 keys, all PT-BR, with `pauseAfter` timing and `inflection` tags"

Line 59 (current):
"- **NLU metadata:** `QUESTION_META` in `src/types/index.ts` — 8 questions with keywords, labels, context"

Line 60 (current):
"- **MP3s:** `public/audio/oracle/` — 61 files, ~17MB total, mp3_44100_192 via ElevenLabs v3"

Line 62 (current):
"- **Timing validation:** `scripts/validate-timing.ts` checks all 4 branch permutations (max-path: 5:57 min)"

Line 71 (current):
"- 8 `buildChoiceConfig(N)` instances from `QUESTION_META`"

Line 101 (current):
"npm test              # all tests (32/34 suites, 496 tests)"

Line 138 (current):
"    oracleMachine.ts            # XState v5 machine (~54 states)"

Line 141 (current):
"    script.ts                   # Full PT-BR script (61 keys)"

Line 158 (current):
"  validate-timing.ts           # Duration validation (4 branch paths)"

Line 159 (current):
"public/audio/oracle/           # 61 pre-recorded MP3s (~17MB)"

---

public/roteiro.html current state (verified during planning):

Line 376 (current — Ramificações):
"<li><strong>Ramifica&ccedil;&otilde;es:</strong> Q1B aparece se Q1=B e Q2=B (perfil contra-f&oacute;bico). Q2B aparece se Q1=A e Q2=A. Q4B aparece se Q3=A e Q4=A. Q5B aparece se Q4=A e Q5=A (perfil PORTADOR). Q6B aparece se Q5=B e Q6=A (dissolveu pergunta MAS abriu para ser visto).</li>"

Line 377 (current — Devolução):
"<li><strong>Devolu&ccedil;&atilde;o:</strong> 9 arqu&eacute;tipos poss&iacute;veis (8 padr&otilde;es + Espelho Silencioso quando Q6B=B), derivados do <em>padr&atilde;o</em> de escolhas (n&atilde;o de cada escolha individual)</li>"

Line 431 (current — central DEV node in Mermaid):
'    DEV["<strong>DEVOLU&Ccedil;&Atilde;O</strong><br/><em>1 de 8 arqu&eacute;tipos<br/>baseado no padr&atilde;o de escolhas</em>"]'

Line 425-429 (current — Q6B routing in Mermaid):
"    Q6 -->|\"A: Ouvir<br/>(se Q5=B)\"| Q6B{...}
    Q6 -->|\"A: Ouvir<br/>(se Q5=A)\"| DEV
    Q6 -->|\"B: J&aacute; sei\"| DEV
    Q6B -->|\"A: Resposta\"| DEV
    Q6B -->|\"B: Pergunta\"| ESPELHO[...]"

Note: line 397 has Q1B routing (Q2 → Q1B). Line 421 has Q5B routing (Q5B → Q6).
Currently Q1B and Q5B route INTO subsequent questions, then ultimately reach DEV.
The Mermaid flowchart already has the BRANCH nodes — what it lacks is dedicated
CONTRA_FOBICO and PORTADOR devolução target nodes downstream of DEV.
The cleanest insertion is to add two new dashed edges from DEV (or from Q1B/Q5B
exit points) into two new archetype boxes labeled CONTRA_FOBICO and PORTADOR,
mirroring the ESPELHO node convention introduced in Phase 33.

Existing ESPELHO node template (line 429):
'ESPELHO["<strong>DEVOLU&Ccedil;&Atilde;O</strong><br/><strong>Espelho Silencioso</strong><br/><em>Estrutura aberta, n&atilde;o diagn&oacute;stico</em>"]'

---

scripts/validate-timing.ts (read-only — DO NOT MODIFY):

Lines 230-281 contain ALL_PATHS array with 24 entries.
Lines 225-230 contain the mutual exclusion comment block:
"// NOTE: Q1B and Q2B are mutually exclusive (q1 can't be both A and B).
// Q5B and Q6B are mutually exclusive (q5 can't be both A and B).
// ESPELHO_SILENCIOSO implies Q6B (only reachable via Q6B branch).
// CONTRA_FOBICO implies Q1B (Phase 34 — only reachable via Q1B branch).
// PORTADOR implies Q5B (Phase 34 — only reachable via Q5B branch).
// The matrix has 24 entries (Phase 33 had 20; Phase 34 adds 4 new permutations)."

This comment IS the POL-01 reconciliation evidence — it documents why 24 is sufficient.
Plan 01 simply confirms exit-0 status and references this evidence in 35-01-SUMMARY.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Confirm validate-timing.ts exits 0 with 24 paths (POL-01 baseline check)</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - scripts/validate-timing.ts (full file — confirm ALL_PATHS has 24 entries and the mutual-exclusion comment block lines 225-230)
    - .planning/phases/35-timing-mitigation-browser-uat/35-RESEARCH.md (Pitfall 1 + Pitfall 5 — explain why no extension is needed)
  </read_first>
  <action>
Run the timing validator and capture its full output. This is a pre-flight check confirming POL-01's "Complete" status from REQUIREMENTS.md line 86 still holds before Wave 1 touches any documentation.

Command:
```
npx tsx scripts/validate-timing.ts 2>&1 | tee /tmp/validate-timing-35-01.log
echo "exit=$?"
```

Expected:
- `exit=0` printed at end
- Output header line contains "PHASE 34 (24 PATHS)" or equivalent v6.0 phrasing
- Output last line contains the literal string "All 24 paths fall within acceptable range."
- Output shows 24 path blocks processed (count `^\[` lines or similar)
- Output contains a max-path line showing ≤ 450.0 seconds (currently 7:11.2 = 431.2s, leaving 18.8s headroom)

Save the captured log path (/tmp/validate-timing-35-01.log) for Task 12 to reference when writing the SUMMARY.

If exit code is NOT 0, STOP. Phase 35 cannot start while POL-01 is broken. Investigate (likely cause: an unrelated commit broke the validator), fix, re-run before continuing. The remainder of Wave 1 assumes the validator is green at baseline.

DO NOT modify scripts/validate-timing.ts under any circumstances. The 24-path matrix is the canonical POL-01 deliverable per Phase 34 closeout.
  </action>
  <verify>
    <automated>npx tsx scripts/validate-timing.ts</automated>
  </verify>
  <acceptance_criteria>
    - Command `npx tsx scripts/validate-timing.ts; echo "exit=$?"` outputs `exit=0` on the final line
    - The captured stdout contains the literal string `All 24 paths fall within acceptable range.`
    - The captured stdout contains a header line referencing "24 PATHS" (e.g., "PHASE 34 (24 PATHS)" or "V6.0 TIMING VALIDATION ... 24 PATHS")
    - scripts/validate-timing.ts was NOT modified by this task (confirm `git diff scripts/validate-timing.ts` is empty)
    - Captured log is available at /tmp/validate-timing-35-01.log for Task 12 reference
  </acceptance_criteria>
  <done>
    Validator baseline confirmed green: 24 paths, exit 0, max-path ≤ 7:30. POL-01 evidence captured for SUMMARY documentation. No code changes made. Wave 1 documentation tasks can now proceed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update public/roteiro.html line 377 — replace "9 arquétipos" wording with "11 arquétipos" (POL-03 part 1/4)</name>
  <files>public/roteiro.html</files>
  <read_first>
    - public/roteiro.html (lines 370-385 area — confirm exact text match before editing)
    - .planning/phases/35-timing-mitigation-browser-uat/35-RESEARCH.md §POL-03 gaps
  </read_first>
  <action>
Open public/roteiro.html. Locate line 377 (the Devolução overview bullet inside the `<ul>` block at lines 371-380).

CURRENT exact text on line 377 (verified during planning):
```
    <li><strong>Devolu&ccedil;&atilde;o:</strong> 9 arqu&eacute;tipos poss&iacute;veis (8 padr&otilde;es + Espelho Silencioso quando Q6B=B), derivados do <em>padr&atilde;o</em> de escolhas (n&atilde;o de cada escolha individual)</li>
```

REPLACE with this NEW exact text:
```
    <li><strong>Devolu&ccedil;&atilde;o:</strong> 11 arqu&eacute;tipos poss&iacute;veis (8 padr&otilde;es base + Espelho Silencioso quando Q6B=B + Contra-F&oacute;bico quando Q1B=A + Portador quando Q5B=A), derivados do <em>padr&atilde;o</em> de escolhas (n&atilde;o de cada escolha individual)</li>
```

Use the Edit tool with the old_string being the entire current line (including leading whitespace) and the new_string being the entire new line. Do NOT touch any other line in this section. Do NOT change Mermaid yet (Task 4 handles that).

Verification grep after save:
```
grep -c "11 arqu" public/roteiro.html
```
Expected: at least 2 (the new line 377 + the existing line 959 reference). The number was 1 before this edit.

Also confirm the old "9 arquétipos" string is gone:
```
grep -c "9 arqu" public/roteiro.html
```
Expected: 0 (assuming no other "9 arqu" string exists in the file — verified during planning).
  </action>
  <verify>
    <automated>grep -c "11 arqu" public/roteiro.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "11 arqu" public/roteiro.html` returns ≥ 2 (line 377 new + line 959 existing)
    - `grep -c "9 arqu" public/roteiro.html` returns 0 (old text removed)
    - `grep -q "Contra-F.*bico quando Q1B=A" public/roteiro.html` exits 0
    - `grep -q "Portador quando Q5B=A" public/roteiro.html` exits 0
    - `wc -l public/roteiro.html` returns the same line count as before this edit (line 377 was replaced in place, no insertions)
  </acceptance_criteria>
  <done>
    Line 377 of public/roteiro.html now states 11 archetypes (was 9), with explicit mention of all three new triggers (ESPELHO Q6B=B, CONTRA_FOBICO Q1B=A, PORTADOR Q5B=A). No other lines touched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify public/roteiro.html overview already mentions CONTRA_FOBICO + PORTADOR sufficiently (POL-03 part 2/4)</name>
  <files>(no files modified — verification only; if check fails, edit line 376 inline)</files>
  <read_first>
    - public/roteiro.html (line 376 area — Ramificações bullet)
    - .planning/phases/35-timing-mitigation-browser-uat/35-RESEARCH.md §POL-03
  </read_first>
  <action>
This task is a guarded verification. The Ramificações bullet on line 376 was already updated during a prior phase to mention "perfil contra-f&oacute;bico" (Q1B) and "perfil PORTADOR" (Q5B). The research finding is that this line is already adequate.

CURRENT text on line 376 (verified during planning):
```
    <li><strong>Ramifica&ccedil;&otilde;es:</strong> Q1B aparece se Q1=B e Q2=B (perfil contra-f&oacute;bico). Q2B aparece se Q1=A e Q2=A. Q4B aparece se Q3=A e Q4=A. Q5B aparece se Q4=A e Q5=A (perfil PORTADOR). Q6B aparece se Q5=B e Q6=A (dissolveu pergunta MAS abriu para ser visto).</li>
```

Step 1 — Run these greps to confirm both archetype names are present in the file at large (cards + ramifications + priority list):
```
grep -c "CONTRA_FOBICO\|contra-f.bico\|Contra-F.bico" public/roteiro.html
grep -c "PORTADOR\|Portador" public/roteiro.html
```

Expected:
- "CONTRA_FOBICO" / "contra-fóbico" / "Contra-Fóbico" pattern: ≥ 3 occurrences (existing card line 992 + existing priority list line 962 + existing ramificações line 376 + new line 377 from Task 2)
- "PORTADOR" / "Portador" pattern: ≥ 3 occurrences (existing card line 1006 + existing priority list line 963 + existing ramificações line 376 + line 831 + new line 377)

Step 2 — If EITHER count is below 3, the overview is missing a mention. Extend line 376 by appending this sentence at the end (before `</li>`):

OLD line 376 ending: `... (dissolveu pergunta MAS abriu para ser visto).</li>`
NEW line 376 ending: `... (dissolveu pergunta MAS abriu para ser visto). Quando o visitante segue por Q1B=A surge o arqu&eacute;tipo <strong>CONTRA_FOBICO</strong>; por Q5B=A surge o arqu&eacute;tipo <strong>PORTADOR</strong>.</li>`

Step 3 — If both counts are ≥ 3, NO EDIT IS NEEDED. Skip the modification and rely on existing text.

After running greps (and any conditional edit), verify both pattern counts are ≥ 3.
  </action>
  <verify>
    <automated>grep -c "CONTRA_FOBICO\|contra-f.bico\|Contra-F.bico" public/roteiro.html | awk '{ if ($1 >= 3) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CONTRA_FOBICO\|contra-f.bico\|Contra-F.bico" public/roteiro.html` returns ≥ 3
    - `grep -c "PORTADOR\|Portador" public/roteiro.html` returns ≥ 3
    - If line 376 was modified: it now contains both "CONTRA_FOBICO" and "PORTADOR" as standalone tokens (not just archetype card references)
    - If line 376 was NOT modified: the SUMMARY notes that overview was already adequate from prior phases (Task 12 documents this)
  </acceptance_criteria>
  <done>
    Overview Ramificações coverage of CONTRA_FOBICO and PORTADOR confirmed sufficient (≥ 3 mentions of each across the file). Line 376 may or may not have been modified — both outcomes are acceptable per the conditional logic.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Extend Mermaid flowchart with CONTRA_FOBICO + PORTADOR devolução nodes + update central DEV label (POL-03 part 3/4)</name>
  <files>public/roteiro.html</files>
  <read_first>
    - public/roteiro.html (lines 385-452 — full Mermaid block; identify DEV node line 431, ESPELHO node line 429, classDef phase line 449)
    - .planning/phases/35-timing-mitigation-browser-uat/35-RESEARCH.md §POL-03 + §Manual-Only Verifications
  </read_first>
  <action>
Open public/roteiro.html and locate the Mermaid block (lines 385-452). The current diagram routes ALL devoluções through a single DEV node labeled "1 de 8 arquétipos" with one exception: ESPELHO_SILENCIOSO has its own dedicated node (introduced in Phase 33). Phase 35 extends this with two more dedicated nodes for CONTRA_FOBICO and PORTADOR.

Make TWO precise edits:

**Edit A: Update the central DEV node label on line 431.**

CURRENT (line 431):
```
    DEV["<strong>DEVOLU&Ccedil;&Atilde;O</strong><br/><em>1 de 8 arqu&eacute;tipos<br/>baseado no padr&atilde;o de escolhas</em>"]
```

REPLACE with:
```
    DEV["<strong>DEVOLU&Ccedil;&Atilde;O</strong><br/><em>1 de 8 arqu&eacute;tipos baseline<br/>baseado no padr&atilde;o de escolhas</em>"]
```

This makes the diagram internally consistent: 8 baseline + 1 ESPELHO + 1 CONTRA_FOBICO + 1 PORTADOR = 11 archetypes total, matching the priority list at line 959.

**Edit B: Insert two new devolução nodes immediately after the ESPELHO node line, before the `ESPELHO --> FIM` line (around line 432-433).**

Find this current block (lines 429-433):
```
    Q6B -->|"B: Pergunta"| ESPELHO["<strong>DEVOLU&Ccedil;&Atilde;O</strong><br/><strong>Espelho Silencioso</strong><br/><em>Estrutura aberta, n&atilde;o diagn&oacute;stico</em>"]

    DEV["<strong>DEVOLU&Ccedil;&Atilde;O</strong><br/><em>1 de 8 arqu&eacute;tipos baseline<br/>baseado no padr&atilde;o de escolhas</em>"]

    ESPELHO --> FIM
```
(Note: line 431 should already be the updated version from Edit A. If you applied Edit A first, the Edit B match must include the NEW line 431 text.)

REPLACE with:
```
    Q6B -->|"B: Pergunta"| ESPELHO["<strong>DEVOLU&Ccedil;&Atilde;O</strong><br/><strong>Espelho Silencioso</strong><br/><em>Estrutura aberta, n&atilde;o diagn&oacute;stico</em>"]

    Q1B -.->|"A: Atravessar (perfil)"| CONTRA_FOBICO["<strong>DEVOLU&Ccedil;&Atilde;O</strong><br/><strong>Contra-F&oacute;bico</strong><br/><em>Atravessou o vazio sem nome de coragem</em>"]
    Q5B -.->|"A: Carregar (perfil)"| PORTADOR_NODE["<strong>DEVOLU&Ccedil;&Atilde;O</strong><br/><strong>Portador</strong><br/><em>Carrega a pergunta como tesouro herdado</em>"]

    DEV["<strong>DEVOLU&Ccedil;&Atilde;O</strong><br/><em>1 de 8 arqu&eacute;tipos baseline<br/>baseado no padr&atilde;o de escolhas</em>"]

    ESPELHO --> FIM
    CONTRA_FOBICO --> FIM
    PORTADOR_NODE --> FIM
```

Note the dashed arrows (`-.->`) indicating these are conditional/perfil-based routes, distinguishing them visually from the main flow's solid arrows. The PORTADOR_NODE name avoids collision with any existing PORTADOR string in the diagram (none currently exists, but the suffix is a defensive measure).

**Edit C: Update the classDef phase assignment on line 449.**

CURRENT:
```
    class DEV,ESPELHO phase
```

REPLACE with:
```
    class DEV,ESPELHO,CONTRA_FOBICO,PORTADOR_NODE phase
```

This ensures the new nodes inherit the same dark phase styling as DEV and ESPELHO (consistent visual weight for all 4 devolução targets).

Save public/roteiro.html.

**Manual visual QA (mandatory):**
After saving, open public/roteiro.html in Chrome (or run `start public/roteiro.html` on Windows). The Mermaid diagram should now render with:
- Q1B branch leading (via dashed edge labeled "A: Atravessar (perfil)") to a new dark CONTRA_FOBICO box
- Q5B branch leading (via dashed edge labeled "A: Carregar (perfil)") to a new dark PORTADOR_NODE box
- ESPELHO unchanged (still reachable from Q6B "B: Pergunta")
- All 4 dark boxes (DEV, ESPELHO, CONTRA_FOBICO, PORTADOR_NODE) eventually connect to FIM
- The central DEV box label now reads "1 de 8 arquétipos baseline" instead of just "1 de 8 arquétipos"

If the Mermaid diagram fails to render (syntax error), the Mermaid CDN will show an error message. Inspect the error, fix the syntax (likely a missing newline or unbalanced bracket), re-save, refresh.
  </action>
  <verify>
    <automated>grep -A 200 'class="mermaid"' public/roteiro.html | grep -c "CONTRA_FOBICO\|PORTADOR_NODE"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 200 'class="mermaid"' public/roteiro.html | grep -c "CONTRA_FOBICO\|PORTADOR_NODE"` returns ≥ 4 (CONTRA_FOBICO node definition + edge + classDef + arrow target = at least 4 occurrences)
    - `grep -q "1 de 8 arqu.*tipos baseline" public/roteiro.html` exits 0 (DEV label updated)
    - `grep -q "Q1B -\.->" public/roteiro.html` exits 0 (dashed edge from Q1B to CONTRA_FOBICO present)
    - `grep -q "Q5B -\.->" public/roteiro.html` exits 0 (dashed edge from Q5B to PORTADOR_NODE present)
    - `grep -q "CONTRA_FOBICO --> FIM" public/roteiro.html` exits 0
    - `grep -q "PORTADOR_NODE --> FIM" public/roteiro.html` exits 0
    - `grep -q "class DEV,ESPELHO,CONTRA_FOBICO,PORTADOR_NODE phase" public/roteiro.html` exits 0
    - Manual visual QA: open public/roteiro.html in Chrome, scroll to "Árvore de Decisões" section, confirm Mermaid renders without error AND shows 4 distinct dark devolução boxes (DEV, ESPELHO, CONTRA_FOBICO, PORTADOR_NODE) all reaching FIM
  </acceptance_criteria>
  <done>
    Mermaid flowchart now visually shows all 11 archetype targets (8 baseline grouped under DEV + 3 dedicated nodes ESPELHO, CONTRA_FOBICO, PORTADOR_NODE), with conditional dashed edges from Q1B and Q5B branches indicating perfil-based routing. Diagram renders correctly in Chrome.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Update CLAUDE.md line 5 — replace "8 branching voice decisions" with "11 max decisions"</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (lines 1-15)
  </read_first>
  <action>
Open CLAUDE.md. Locate line 5 (the "What This Is" paragraph).

CURRENT exact text on line 5:
```
Interactive voice art installation for the VII Bienal de Psicanalise e Cultura (SBPRP 2026, May 29-30). A visitor puts on headphones, the Oracle guides them through a Dante-inspired journey (Inferno → Purgatorio → Paraiso) with 8 branching voice decisions in 5-7 minutes, then delivers a personalized devolucao based on their choice pattern.
```

REPLACE with:
```
Interactive voice art installation for the VII Bienal de Psicanalise e Cultura (SBPRP 2026, May 29-30). A visitor puts on headphones, the Oracle guides them through a Dante-inspired journey (Inferno → Purgatorio → Paraiso) with up to 11 max decisions (6 base + 5 conditional branches) in 5-7 minutes, then delivers a personalized devolucao based on their choice pattern.
```

Use the Edit tool with old_string = the entire current line and new_string = the entire new line.
  </action>
  <verify>
    <automated>grep -q "11 max decisions" CLAUDE.md && ! grep -q "8 branching voice decisions" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "11 max decisions" CLAUDE.md` exits 0
    - `grep -q "5 conditional branches" CLAUDE.md` exits 0 (line 5 contains the parenthetical)
    - `grep -q "8 branching voice decisions" CLAUDE.md` exits 1 (old text gone)
    - `wc -l CLAUDE.md` returns the same line count as before (in-place line replacement)
  </acceptance_criteria>
  <done>
    CLAUDE.md line 5 updated: status sentence now says "up to 11 max decisions (6 base + 5 conditional branches)" instead of "8 branching voice decisions".
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Update CLAUDE.md line 7 — status banner v4.0 → v6.0, 61 MP3s → 82 MP3s</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (lines 5-10)
  </read_first>
  <action>
Open CLAUDE.md. Locate line 7 (the "**Status:**" banner).

CURRENT exact text on line 7:
```
**Status:** v4.0 Game Flow complete. All code, script, machine, and 61 MP3s shipped. Pending: browser UAT + event deploy.
```

REPLACE with:
```
**Status:** v6.0 Deep Branching complete. All code, script, machine, and 82 MP3s shipped. Pending: browser UAT + event deploy.
```

Use the Edit tool. The two changes in this single line are: "v4.0 Game Flow" → "v6.0 Deep Branching", and "61 MP3s" → "82 MP3s".
  </action>
  <verify>
    <automated>grep -q "v6\.0 Deep Branching complete" CLAUDE.md && grep -q "82 MP3s shipped" CLAUDE.md && ! grep -q "v4\.0 Game Flow complete" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "v6\.0 Deep Branching complete" CLAUDE.md` exits 0
    - `grep -q "82 MP3s shipped" CLAUDE.md` exits 0
    - `grep -q "v4\.0 Game Flow complete" CLAUDE.md` exits 1 (old text gone)
    - `grep -c "61 MP3s shipped" CLAUDE.md` returns 0
  </acceptance_criteria>
  <done>
    CLAUDE.md line 7 status banner now reads "v6.0 Deep Branching complete. All code, script, machine, and 82 MP3s shipped. Pending: browser UAT + event deploy."
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Update CLAUDE.md line 13 — XState bullet ~54 states → ~78 states, 8 → 11 decision points</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (lines 10-20)
  </read_first>
  <action>
Open CLAUDE.md. Locate line 13 (the XState bullet inside the Stack list).

CURRENT exact text on line 13:
```
- **XState v5** state machine (~54 states, 8 decision points, branching)
```

REPLACE with:
```
- **XState v5** state machine (~78 states, 11 decision points, 5 conditional branches)
```
  </action>
  <verify>
    <automated>grep -q "~78 states" CLAUDE.md && grep -q "11 decision points" CLAUDE.md && ! grep -q "~54 states" CLAUDE.md