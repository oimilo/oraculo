---
phase: 23-devolucoes-bookends
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/script.ts
  - src/data/__tests__/script-v3.test.ts
autonomous: true
requirements: [SCR-04, SCR-05, SCR-08]
must_haves:
  truths:
    - "Each devolucao names the PATTERN (shape across 6 choices), never lists individual scenarios"
    - "Each devolucao has 3-layer structure: Winnicott pattern + Lacan structure + Bion transformation"
    - "Each devolucao ends with a question OR persistent image (not both)"
    - "Each devolucao makes visitor feel SEEN, not analyzed"
    - "Anchor lines from D-07 preserved exactly"
    - "Oracle voice in devoluces is direct, intimate, mirror-like"
  artifacts:
    - path: "src/data/script.ts"
      provides: "8 rewritten devolucao sections"
      contains: "DEVOLUCAO_SEEKER"
    - path: "src/data/__tests__/script-v3.test.ts"
      provides: "Fixed test assertions for Phase 22 expansions"
      contains: "PURGATORIO_INTRO"
  key_links:
    - from: "src/data/script.ts"
      to: "src/machines/guards/patternMatching.ts"
      via: "8 archetype keys match"
      pattern: "DEVOLUCAO_(SEEKER|GUARDIAN|CONTRADICTED|PIVOT_EARLY|PIVOT_LATE|DEPTH_SEEKER|SURFACE_KEEPER|MIRROR)"
---

<objective>
Rewrite all 8 devolucoes in src/data/script.ts with 3-layer psychoanalytic structure (Winnicott pattern + Lacan structure + Bion transformation) per D-01, raising depth from 2-3 to 4-5. Also fix 2 failing test assertions from Phase 22 expansions.

Purpose: Devolucoes are THE PAYOFF — visitor played 6 choices to receive this pattern reading. Currently they describe patterns but don't perform the analytic act. This plan makes each devolucao function as a genuine mirror.

Output: 8 rewritten devolucao sections in script.ts + fixed test file
</objective>

<execution_context>
@C:/Users/USER/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/USER/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v3.1-ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-devolucoes-bookends/23-CONTEXT.md
@.planning/phases/23-devolucoes-bookends/23-RESEARCH.md

<interfaces>
<!-- SpeechSegment interface from src/types/index.ts -->
From src/types/index.ts:
```typescript
export interface SpeechSegment {
  text: string;
  pauseAfter?: number;
  inflection?: string[];
}
```

<!-- Archetype keys that devolucoes must match -->
From src/machines/guards/patternMatching.ts:
```typescript
export type DevolucaoArchetype = 'SEEKER' | 'GUARDIAN' | 'CONTRADICTED' | 'PIVOT_EARLY' | 'PIVOT_LATE' | 'DEPTH_SEEKER' | 'SURFACE_KEEPER' | 'MIRROR';
```

<!-- ScriptDataV3 devolucao keys -->
From src/data/script.ts:
```typescript
DEVOLUCAO_SEEKER: SpeechSegment[];
DEVOLUCAO_GUARDIAN: SpeechSegment[];
DEVOLUCAO_CONTRADICTED: SpeechSegment[];
DEVOLUCAO_PIVOT_EARLY: SpeechSegment[];
DEVOLUCAO_PIVOT_LATE: SpeechSegment[];
DEVOLUCAO_DEPTH_SEEKER: SpeechSegment[];
DEVOLUCAO_SURFACE_KEEPER: SpeechSegment[];
DEVOLUCAO_MIRROR: SpeechSegment[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix failing test assertions from Phase 22 expansions</name>
  <files>src/data/__tests__/script-v3.test.ts</files>
  <read_first>
    - src/data/__tests__/script-v3.test.ts (current test file — see lines 311-313 and 377-379)
    - src/data/script.ts (current script — PURGATORIO_INTRO has 4 segments, Q4_SETUP has 5 segments)
  </read_first>
  <action>
Fix 2 failing test assertions caused by Phase 22 expansions (these are expected changes, not bugs):

1. **PURGATORIO_INTRO segment count (line 311-313):**
   - Current assertion: `toBeLessThanOrEqual(3)` — fails because Phase 22 expanded PURGATORIO_INTRO to 4 segments
   - Change to: `toBeLessThanOrEqual(4)` — accept 2-4 segments
   - The test at line 312 (`toBeGreaterThanOrEqual(2)`) stays as is

2. **PURGATORIO_Q4_SETUP segment count (line 377-379):**
   - Current assertion: `toBeLessThanOrEqual(4)` — fails because Phase 22 expanded Q4_SETUP to 5 segments to fix Q3-Q4 escalation flattening
   - Change to: `toBeLessThanOrEqual(5)` — accept 2-5 segments
   - The test at line 378 (`toBeGreaterThanOrEqual(2)`) stays as is

No other test changes needed. These are the only 2 failures (49 passing, 2 failing).
  </action>
  <verify>
    <automated>npx vitest run src/data/__tests__/script-v3.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - script-v3.test.ts line ~313 contains `toBeLessThanOrEqual(4)` (was 3)
    - script-v3.test.ts line ~379 contains `toBeLessThanOrEqual(5)` (was 4)
    - `npx vitest run src/data/__tests__/script-v3.test.ts` exits 0 with 51 tests passing, 0 failing
  </acceptance_criteria>
  <done>All 51 script-v3 tests pass (0 failures). Assertions updated to match Phase 22 expansions.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite 8 devolucoes with 3-layer psychoanalytic structure</name>
  <files>src/data/script.ts</files>
  <read_first>
    - src/data/script.ts (current script — lines 347-420 are current devolucoes)
    - src/machines/guards/patternMatching.ts (what each archetype MEANS — determines what pattern to read)
    - .planning/phases/23-devolucoes-bookends/23-CONTEXT.md (locked decisions D-01 through D-07)
    - .planning/phases/23-devolucoes-bookends/23-RESEARCH.md (code examples with current vs target for SEEKER, lines 220-258)
    - .planning/phases/21-script-audit-framework-integration/21-02-gold-phrases-and-final-audit.md (lines 269-288 — per-archetype rewrite recommendations)
  </read_first>
  <action>
Rewrite all 8 DEVOLUCAO sections in src/data/script.ts (lines 347-420), applying the 3-layer structure per D-01.

**Structure for EACH devolucao (per D-01):**
- Layer 1 (Winnicott): Name the PATTERN across 6 choices — recognition without judgment
- Layer 2 (Lacan): Read the STRUCTURE — what pattern reveals about desire/defense (analytic act as surprise)
- Layer 3 (Bion): Name the TRANSFORMATION — what was metabolized or evacuated (container reading)

**Constraints (from CONTEXT.md decisions):**
- Per D-02: Keep 4-5 segments per devolucao. Depth through REPLACEMENT, not addition.
- Per D-03: End with question OR persistent image (pick what fits archetype, not both)
- Per D-04: NO gold phrase forced integration (devolucoes are pattern readings, not scenario-based)
- Per D-05: Voice is direct, intimate, mirror-like. NOT realm-specific. Oracle at its most personal.
- Per D-06: Visitor must feel SEEN, not analyzed. Espelho, nao laudo.
- Per D-07: PRESERVE these exact anchor lines:
  - CONTRADICTED: "Quem e coerente demais ja decidiu parar de sentir"
  - SURFACE_KEEPER: "O jardim que voce nao entrou continua pegando fogo na sua cabeca"

**Per-archetype rewrite targets (from Phase 21 audit lines 277-288 + RESEARCH.md examples):**

**DEVOLUCAO_SEEKER** (depth 2 -> 4-5):
Replace segments 2 and 4. Keep "Voce entrou em tudo" (Layer 1). Keep "fome" (Layer 2 start). Add:
```
Layer 1: "Voce entrou em tudo." + "Nada te parou. Voce nao recua."
Layer 2: "Tem gente que chama isso de coragem. Eu chamo de fome." + "Mas fome de que? Percebe -- voce nunca ficou parado. O movimento pode ser a ultima forma de fuga."
Layer 3: End with Bionian transformation question: "A pergunta que fica: voce transformou o que tocou -- ou so acumulou peso?"
```
5 segments. End with question (per D-03).

**DEVOLUCAO_GUARDIAN** (depth 2 -> 4-5):
Replace scenario listing (segment 2). Keep "Voce se protegeu em cada encruzilhada" (Layer 1). Add:
```
Layer 1: "Voce se protegeu em cada encruzilhada." + "Em cada encontro, sua primeira resposta foi preservar. Nao e fraqueza. E sofisticacao."
Layer 2 (Winnicott false self + Lacan): "Mas voce veio ate aqui. Pra esse escuro, pra essa voz. Algo em voce sabe: o que voce protege pode ser o que voce nunca deixou nascer."
Layer 3 (Bion): "Toda muralha tem dois lados. O que ela impede de entrar -- e o que ela impede de sair."
End: "Fica com isso." (persistent image of the wall, per D-03)
```
5 segments.

**DEVOLUCAO_CONTRADICTED** (depth 3 -> 4-5):
PRESERVE anchor: "Quem e coerente demais ja decidiu parar de sentir" (D-07). Replace thin segments:
```
Layer 1 (Winnicott): "Voce foi e voltou. Segurou e soltou. Nenhuma direcao fixa."
Layer 2 (Lacan split subject): "A maioria ia achar que isso e indecisao. Nao e. E respiracao. Voce quer as duas coisas porque desejo nunca e simples. Nao e falha -- e estrutura."
ANCHOR: "Quem e coerente demais ja decidiu parar de sentir."
Layer 3 (Bion): "Guarda essa oscilacao. Ela e mais sua do que qualquer certeza."
```
4-5 segments. End with persistent image (oscilacao as possession, per D-03).

**DEVOLUCAO_PIVOT_EARLY** (depth 2 -> 4-5):
Replace thin "instinto" description:
```
Layer 1 (Winnicott): "Alguma coisa mudou em voce no comeco." + "Voce comecou num caminho e virou antes da terceira pergunta."
Layer 2 (Lacan): "A virada aconteceu antes de voce perceber. O corpo se moveu antes da mente entender. E quando o corpo lidera, o que aparece nao e reacao -- e verdade."
Layer 3 (Bion): "Os primeiros encontros sacudiram o plano. E em vez de resistir, voce deixou o plano cair. Isso e raro. A maioria segura o plano ate o fim -- mesmo quando ele ja nao serve."
End: "Presta atencao nesse instinto. Ele sabe coisas que voce ainda nao sabe." (persistent image, per D-03)
```
5 segments.

**DEVOLUCAO_PIVOT_LATE** (depth 3 -> 4-5):
Keep "dobradica". Add Lacanian desire curve:
```
Layer 1 (Winnicott): "Alguma coisa mudou em voce entre a terceira e a quarta pergunta." + "Voce comecou de um jeito e terminou de outro. A virada veio na metade mais funda."
Layer 2 (Lacan): "Quem muda tarde muda de verdade. Porque ja nao e reacao -- e decisao. Desejo nao e linha reta. E curva."
Layer 3 (Bion): "Essa dobradica entre quem voce era no inicio e quem voce e agora -- esse e o lugar onde algo foi transformado. Nao so sentido. Atravessado."
End: "Nao esquece desse ponto. Ele e mais importante do que qualquer resposta que voce deu." (persistent image, per D-03)
```
5 segments.

**DEVOLUCAO_DEPTH_SEEKER** (depth 3 -> 4-5):
Keep "coragem vs compulsao". Add Bionian depth:
```
Layer 1 (Winnicott): "Voce foi em direcao a cada fogo." + "Nao recuou. Em nenhum."
Layer 2 (Lacan): "A questao e se voce sabe a diferenca entre coragem e compulsao. Porque os dois se parecem -- ate o preco chegar."
Layer 3 (Bion): "Voce recebeu tudo. Mas receber nao e o mesmo que digerir. Fundo e bom. Mas fundo sem ar mata igual."
End: persistent image (per D-03)
```
4-5 segments.

**DEVOLUCAO_SURFACE_KEEPER** (depth 3 -> 4-5):
PRESERVE anchor: "O jardim que voce nao entrou continua pegando fogo na sua cabeca" (D-07). Add Bionian beta:
```
Layer 1 (Winnicott): "Voce se manteve inteiro." + "Em cada encruzilhada, voce escolheu o caminho que preserva. Nao e fraqueza -- e sofisticacao."
Layer 2 (Lacan): "Mas a protecao tem um custo que nao aparece na hora. Aparece depois, no que voce nunca se permitiu ver."
ANCHOR: "O jardim que voce nao entrou continua pegando fogo na sua cabeca."
Layer 3 (Bion): "Nao como memoria -- como coisa que nunca foi recebida. E o que nao e recebido nao some. Insiste."
```
5 segments. End with persistent image ("insiste", per D-03).

**DEVOLUCAO_MIRROR** (depth 3 -> 4-5):
Keep excellent ambiguity. Add Lacanian desire reading:
```
Layer 1 (Winnicott): "Equilibrio perfeito." + "Voce nunca repetiu a mesma direcao duas vezes seguidas. A maioria das pessoas pende pra um lado. Voce segurou o centro."
Layer 2 (Lacan): "O equilibrio perfeito pode ser sabedoria -- sentir os dois lados sem perder o eixo. Ou pode ser a ultima forma de nunca se comprometer de verdade com nenhum desejo."
Layer 3 (Bion): "So voce sabe se o centro e uma conquista ou um refugio."
End: "Fica com essa pergunta." (question per D-03)
```
4-5 segments.

**Voice rules for ALL 8 devolucoes (per D-05, D-06):**
- 2nd person, short percussive sentences
- No sentence over 25 words (mirror voice is tighter than narrative)
- Max 1 inflection tag per segment, sparse (~2-3 tags per devolucao total)
- pauseAfter range: 1500-2500ms, 0 for last segment
- NO scenario names (sala, coisa, jardim, agua) — pattern language only
- NO listing individual choices — read the SHAPE

**What NOT to do:**
- Do NOT list individual scenario names (Pitfall 1 from RESEARCH.md)
- Do NOT explain psychology — mirror, don't lecture (Pitfall 2)
- Do NOT break anchor lines from D-07 (Pitfall 3)
- Do NOT add segments beyond 5 per devolucao (Pitfall 4 — depth through replacement per D-02)
  </action>
  <verify>
    <automated>npx vitest run src/data/__tests__/script-v3.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - script.ts contains "DEVOLUCAO_SEEKER" with 4-5 SpeechSegment objects
    - script.ts contains "DEVOLUCAO_GUARDIAN" with 4-5 SpeechSegment objects
    - script.ts contains "DEVOLUCAO_CONTRADICTED" with 4-5 SpeechSegment objects containing "Quem e coerente demais ja decidiu parar de sentir" (D-07 anchor preserved)
    - script.ts contains "DEVOLUCAO_PIVOT_EARLY" with 4-5 SpeechSegment objects
    - script.ts contains "DEVOLUCAO_PIVOT_LATE" with 4-5 SpeechSegment objects
    - script.ts contains "DEVOLUCAO_DEPTH_SEEKER" with 4-5 SpeechSegment objects
    - script.ts contains "DEVOLUCAO_SURFACE_KEEPER" with 4-5 SpeechSegment objects containing "O jardim que voce nao entrou continua pegando fogo na sua cabeca" (D-07 anchor preserved)
    - script.ts contains "DEVOLUCAO_MIRROR" with 4-5 SpeechSegment objects
    - NO devolucao segment contains individual scenario names: grep for "sala sem porta", "coisa na parede", "jardim pegando fogo" inside DEVOLUCAO sections returns 0 matches (except SURFACE_KEEPER anchor which uses "jardim" in abstract sense)
    - Each devolucao has max 1 inflection tag per segment
    - `npx vitest run src/data/__tests__/script-v3.test.ts` exits 0
    - `npx tsc --noEmit` exits 0 (TypeScript compiles)
  </acceptance_criteria>
  <done>All 8 devolucoes rewritten with 3-layer structure (Winnicott + Lacan + Bion), depth raised from 2-3 to 4-5, anchor lines preserved, no scenario listing, 4-5 segments each, all tests pass, TypeScript compiles.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/data/__tests__/script-v3.test.ts` — all 51 tests pass
2. `npx tsc --noEmit` — TypeScript compilation succeeds
3. Each DEVOLUCAO section has exactly 4-5 segments (grep count)
4. D-07 anchor lines present: "Quem e coerente demais ja decidiu parar de sentir" in CONTRADICTED, "O jardim que voce nao entrou continua pegando fogo na sua cabeca" in SURFACE_KEEPER
5. No devolucao lists individual scenario names (pattern reading, not recap)
</verification>

<success_criteria>
- 8 devolucoes rewritten with 3-layer psychoanalytic depth (Winnicott pattern + Lacan structure + Bion transformation)
- Each devolucao: 4-5 segments, ends with question OR persistent image
- Anchor lines from D-07 preserved exactly
- No scenario listing in any devolucao
- Oracle voice: direct, intimate, mirror-like, short sentences
- All 51 script-v3 tests pass (including 2 fixed from Phase 22)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-devolucoes-bookends/23-01-SUMMARY.md`
</output>
