---
phase: 05-real-voice-services
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/services/tts/index.ts
  - src/services/stt/index.ts
  - src/services/nlu/index.ts
autonomous: true
requirements: [RTTS-01, RSTT-01, RNLU-01]

must_haves:
  truths:
    - "Factory function createTTSService() returns ElevenLabsTTSService when NEXT_PUBLIC_USE_REAL_APIS=true"
    - "Factory function createSTTService() returns WhisperSTTService when NEXT_PUBLIC_USE_REAL_APIS=true"
    - "Factory function createNLUService() returns ClaudeNLUService when NEXT_PUBLIC_USE_REAL_APIS=true"
    - "Factory functions still return mock services when NEXT_PUBLIC_USE_REAL_APIS is not true"
    - "Full test suite passes (all existing + all new tests)"
  artifacts:
    - path: "src/services/tts/index.ts"
      provides: "Updated TTS factory with ElevenLabsTTSService import"
      contains: "ElevenLabsTTSService"
    - path: "src/services/stt/index.ts"
      provides: "Updated STT factory with WhisperSTTService import"
      contains: "WhisperSTTService"
    - path: "src/services/nlu/index.ts"
      provides: "Updated NLU factory with ClaudeNLUService import"
      contains: "ClaudeNLUService"
  key_links:
    - from: "src/services/tts/index.ts"
      to: "src/services/tts/elevenlabs.ts"
      via: "import { ElevenLabsTTSService } from './elevenlabs'"
      pattern: "import.*ElevenLabsTTSService.*from.*elevenlabs"
    - from: "src/services/stt/index.ts"
      to: "src/services/stt/whisper.ts"
      via: "import { WhisperSTTService } from './whisper'"
      pattern: "import.*WhisperSTTService.*from.*whisper"
    - from: "src/services/nlu/index.ts"
      to: "src/services/nlu/claude.ts"
      via: "import { ClaudeNLUService } from './claude'"
      pattern: "import.*ClaudeNLUService.*from.*claude"
---

<objective>
Wire the three real service implementations into factory functions, replacing TODO(Phase 5) placeholders so that setting NEXT_PUBLIC_USE_REAL_APIS=true returns real AI-powered services.

Purpose: Complete the service toggle mechanism so the application can switch between mock and real AI services via environment variable, fulfilling the factory pattern established in v1.0.

Output: Updated factory functions in all three service index files, verified with full test suite.
</objective>

<execution_context>
@C:\Users\USER\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\USER\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-real-voice-services/05-01-SUMMARY.md

<interfaces>
<!-- Current state of factory functions (with TODO placeholders to replace) -->

From src/services/tts/index.ts (current):
```typescript
export function createTTSService(): TTSService {
  if (typeof window !== 'undefined' && process.env.NEXT_PUBLIC_USE_REAL_APIS === 'true') {
    // TODO(Phase 5): return new ElevenLabsTTSService()
    // Real service will call /api/tts route
    // Fallback chain: ElevenLabs -> FallbackTTS (pre-recorded) -> SpeechSynthesis
    if (isOnline()) {
      console.warn('[TTS] Real service not yet implemented, using fallback');
    }
    return new FallbackTTSService();
  }
  return new MockTTSService();
}
```

From src/services/stt/index.ts (current):
```typescript
export function createSTTService(): STTService {
  if (typeof window !== 'undefined' && process.env.NEXT_PUBLIC_USE_REAL_APIS === 'true') {
    // TODO(Phase 5): return new WhisperSTTService()
    // Real service will call /api/stt route
    console.warn('[STT] Real service not yet implemented, using mock');
  }
  return new MockSTTService();
}
```

From src/services/nlu/index.ts (current):
```typescript
export function createNLUService(): NLUService {
  if (typeof window !== 'undefined' && process.env.NEXT_PUBLIC_USE_REAL_APIS === 'true') {
    // TODO(Phase 5): return new ClaudeNLUService()
    // Real service will call /api/nlu route
    console.warn('[NLU] Real service not yet implemented, using mock');
  }
  return new MockNLUService();
}
```

From Plan 01 outputs (new service classes):
```typescript
// src/services/tts/elevenlabs.ts
export class ElevenLabsTTSService implements TTSService { ... }

// src/services/stt/whisper.ts
export class WhisperSTTService implements STTService { ... }

// src/services/nlu/claude.ts
export class ClaudeNLUService implements NLUService { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire real service classes into factory functions</name>
  <files>src/services/tts/index.ts, src/services/stt/index.ts, src/services/nlu/index.ts</files>
  <read_first>
    - src/services/tts/index.ts (Current factory with TODO placeholder)
    - src/services/stt/index.ts (Current factory with TODO placeholder)
    - src/services/nlu/index.ts (Current factory with TODO placeholder)
    - src/services/tts/elevenlabs.ts (New service class to import — verify it exists from Plan 01)
    - src/services/stt/whisper.ts (New service class to import — verify it exists from Plan 01)
    - src/services/nlu/claude.ts (New service class to import — verify it exists from Plan 01)
  </read_first>
  <action>
    **Update `src/services/tts/index.ts`:**

    1. Add import at top: `import { ElevenLabsTTSService } from './elevenlabs';`
    2. Replace the TODO block inside the `NEXT_PUBLIC_USE_REAL_APIS === 'true'` branch:

    BEFORE (lines 28-35):
    ```typescript
    if (typeof window !== 'undefined' && process.env.NEXT_PUBLIC_USE_REAL_APIS === 'true') {
      // TODO(Phase 5): return new ElevenLabsTTSService()
      // Real service will call /api/tts route
      // Fallback chain: ElevenLabs -> FallbackTTS (pre-recorded) -> SpeechSynthesis
      if (isOnline()) {
        console.warn('[TTS] Real service not yet implemented, using fallback');
      }
      return new FallbackTTSService();
    }
    ```

    AFTER:
    ```typescript
    if (typeof window !== 'undefined' && process.env.NEXT_PUBLIC_USE_REAL_APIS === 'true') {
      return new ElevenLabsTTSService();
    }
    ```

    Keep the rest of the file unchanged (VoiceSettings, PHASE_VOICE_SETTINGS, TTSService interface, createFallbackTTSService).

    **Update `src/services/stt/index.ts`:**

    1. Add import at top: `import { WhisperSTTService } from './whisper';`
    2. Replace the TODO block:

    BEFORE (lines 8-12):
    ```typescript
    if (typeof window !== 'undefined' && process.env.NEXT_PUBLIC_USE_REAL_APIS === 'true') {
      // TODO(Phase 5): return new WhisperSTTService()
      // Real service will call /api/stt route
      console.warn('[STT] Real service not yet implemented, using mock');
    }
    ```

    AFTER:
    ```typescript
    if (typeof window !== 'undefined' && process.env.NEXT_PUBLIC_USE_REAL_APIS === 'true') {
      return new WhisperSTTService();
    }
    ```

    **Update `src/services/nlu/index.ts`:**

    1. Add import at top: `import { ClaudeNLUService } from './claude';`
    2. Replace the TODO block:

    BEFORE (lines 18-22):
    ```typescript
    if (typeof window !== 'undefined' && process.env.NEXT_PUBLIC_USE_REAL_APIS === 'true') {
      // TODO(Phase 5): return new ClaudeNLUService()
      // Real service will call /api/nlu route
      console.warn('[NLU] Real service not yet implemented, using mock');
    }
    ```

    AFTER:
    ```typescript
    if (typeof window !== 'undefined' && process.env.NEXT_PUBLIC_USE_REAL_APIS === 'true') {
      return new ClaudeNLUService();
    }
    ```

    All three factory functions must:
    - Import the real service class
    - Return `new RealService()` when `NEXT_PUBLIC_USE_REAL_APIS === 'true'` and `typeof window !== 'undefined'`
    - Remove the TODO(Phase 5) comments
    - Remove the `console.warn` placeholder messages
    - Continue returning mock services when env var is not set
  </action>
  <verify>
    <automated>npx vitest run src/services/tts/__tests__/ src/services/stt/__tests__/ src/services/nlu/__tests__/ --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/services/tts/index.ts contains `import { ElevenLabsTTSService } from './elevenlabs'`
    - src/services/tts/index.ts contains `return new ElevenLabsTTSService()`
    - src/services/tts/index.ts does NOT contain `TODO(Phase 5)`
    - src/services/tts/index.ts does NOT contain `Real service not yet implemented`
    - src/services/stt/index.ts contains `import { WhisperSTTService } from './whisper'`
    - src/services/stt/index.ts contains `return new WhisperSTTService()`
    - src/services/stt/index.ts does NOT contain `TODO(Phase 5)`
    - src/services/stt/index.ts does NOT contain `Real service not yet implemented`
    - src/services/nlu/index.ts contains `import { ClaudeNLUService } from './claude'`
    - src/services/nlu/index.ts contains `return new ClaudeNLUService()`
    - src/services/nlu/index.ts does NOT contain `TODO(Phase 5)`
    - src/services/nlu/index.ts does NOT contain `Real service not yet implemented`
    - All existing service tests still pass
  </acceptance_criteria>
  <done>Factory functions import and return real service implementations when NEXT_PUBLIC_USE_REAL_APIS=true. TODO(Phase 5) placeholders removed. Mock services still returned when env var not set.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite verification and TypeScript compilation check</name>
  <files></files>
  <read_first>
    - src/services/tts/index.ts (Verify wiring from Task 1)
    - src/services/stt/index.ts (Verify wiring from Task 1)
    - src/services/nlu/index.ts (Verify wiring from Task 1)
  </read_first>
  <action>
    Run the complete verification battery to confirm nothing is broken:

    1. **TypeScript compilation:** `npx tsc --noEmit` must exit 0 with no errors. This verifies all imports resolve, interface implementations are correct, and type signatures match.

    2. **Full test suite:** `npx vitest run --reporter=verbose` must exit 0. Verify total test count is at least 192 (168 v1.0 + 24 Phase 4 API route tests) plus the new Phase 5 tests (approximately 20+ new tests from elevenlabs-tts.test.ts, whisper-stt.test.ts, claude-nlu.test.ts).

    3. **Grep verification — no remaining TODOs:**
       - `grep -r "TODO(Phase 5)" src/services/` must return empty (all placeholders replaced)
       - `grep -r "not yet implemented" src/services/` must return empty (all placeholder warnings removed)

    4. **Grep verification — real services wired:**
       - `grep "ElevenLabsTTSService" src/services/tts/index.ts` must return matches
       - `grep "WhisperSTTService" src/services/stt/index.ts` must return matches
       - `grep "ClaudeNLUService" src/services/nlu/index.ts` must return matches

    If any step fails, diagnose and fix the issue before proceeding.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0 with 200+ tests passing
    - No `TODO(Phase 5)` strings remain in src/services/ directory
    - No `not yet implemented` strings remain in src/services/ directory
    - src/services/tts/index.ts contains `ElevenLabsTTSService`
    - src/services/stt/index.ts contains `WhisperSTTService`
    - src/services/nlu/index.ts contains `ClaudeNLUService`
  </acceptance_criteria>
  <done>Full test suite green (200+ tests), TypeScript compiles clean, all TODO(Phase 5) placeholders resolved, all three factory functions wired to real implementations.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npx tsc --noEmit` — zero TypeScript errors
2. `npx vitest run --reporter=verbose` — 200+ tests, all green
3. `grep -r "TODO(Phase 5)" src/services/` — returns empty
4. `grep "ElevenLabsTTSService" src/services/tts/index.ts` — matches found
5. `grep "WhisperSTTService" src/services/stt/index.ts` — matches found
6. `grep "ClaudeNLUService" src/services/nlu/index.ts` — matches found
</verification>

<success_criteria>
- All three factory functions return real service classes when NEXT_PUBLIC_USE_REAL_APIS=true
- All three factory functions still return mock services when env var is not set
- All TODO(Phase 5) placeholders removed from codebase
- TypeScript compiles clean (all imports resolve, interfaces satisfied)
- Full test suite passes (200+ tests)
- The application is ready for end-to-end testing with real API keys
</success_criteria>

<output>
After completion, create `.planning/phases/05-real-voice-services/05-02-SUMMARY.md`
</output>
