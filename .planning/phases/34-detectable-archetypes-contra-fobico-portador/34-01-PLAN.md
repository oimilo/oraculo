---
phase: 34-detectable-archetypes-contra-fobico-portador
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/script.ts
  - src/types/index.ts
autonomous: true
requirements: [AR-02, AR-03]

must_haves:
  truths:
    - "SCRIPT.DEVOLUCAO_CONTRA_FOBICO exists as a top-level key in src/data/script.ts and is a SpeechSegment[] of 5-7 segments"
    - "SCRIPT.DEVOLUCAO_PORTADOR exists as a top-level key in src/data/script.ts and is a SpeechSegment[] of 5-7 segments"
    - "Both new devolucao scripts target ~22-25s spoken duration via the validate-timing.ts heuristic (~13 chars/sec + pauseAfter sums)"
    - "Both new scripts use only inflection tags from the established vocabulary: warm, gentle, thoughtful, serious, determined (NEVER whispering — voice ID PznTnBc8X6pvixs9UkQm does not render it per MEMORY.md)"
    - "Neither script names a framework (no Winnicott/Lacan/Bion), nor declares a diagnosis ('Você é...'), nor uses gamified language ('você desbloqueou...')"
    - "Each script has a closing line that mirrors the gesture without prescribing meaning to the visitor (open form, like ESPELHO_SILENCIOSO but with content)"
    - "DevolucaoArchetype union in src/types/index.ts now includes 'ESPELHO_SILENCIOSO' (Phase 33 type-gap fix), 'CONTRA_FOBICO' (Phase 34, AR-02), and 'PORTADOR' (Phase 34, AR-03)"
    - "TypeScript type-check passes (npx tsc --noEmit exits 0) — type extension does not break any existing reference"
    - "All previously-passing tests still pass (no regression — Wave 1 is data-only)"
  artifacts:
    - path: src/data/script.ts
      provides: "Two new top-level SCRIPT keys (DEVOLUCAO_CONTRA_FOBICO + DEVOLUCAO_PORTADOR) ready for MP3 generation in Wave 3"
      contains: ["DEVOLUCAO_CONTRA_FOBICO", "DEVOLUCAO_PORTADOR"]
    - path: src/types/index.ts
      provides: "Extended DevolucaoArchetype union to support ESPELHO_SILENCIOSO + CONTRA_FOBICO + PORTADOR"
      contains: ["ESPELHO_SILENCIOSO", "CONTRA_FOBICO", "PORTADOR"]
  key_links:
    - from: "src/data/script.ts SCRIPT export"
      to: "scripts/generate-audio-v3.ts auto-discovery"
      via: "Object.keys(SCRIPT) iteration in audio generator"
      pattern: "DEVOLUCAO_CONTRA_FOBICO|DEVOLUCAO_PORTADOR"
    - from: "DevolucaoArchetype union"
      to: "patternMatching.ts createArchetypeGuard signature (Wave 2 will rely on the type accepting new variants)"
      via: "type import in patternMatching.ts"
      pattern: "DevolucaoArchetype"
---

<objective>
Wave 1 of Phase 34: Add the two new devolução scripts (CONTRA_FOBICO + PORTADOR) and extend the DevolucaoArchetype type union. This is pure data work — no machine, no UI, no audio. The new SCRIPT keys are the foundation that Wave 2 (machine + guards + UI) and Wave 3 (audio + timing + roteiro) build on.

Purpose:
- Satisfy AR-02 and AR-03 at the script-data level (the artistic content visitors will hear)
- Backfill the Phase 33 type-gap: DevolucaoArchetype union currently does NOT include 'ESPELHO_SILENCIOSO' even though oracleMachine.ts already routes to a state by that name. Adding it now prevents type errors when patternMatching.ts is touched in Wave 2.
- Keep both scripts within the 22-25s target window so Wave 3's timing validator has slack against the 7:30 budget (currently 7:01.2 with 28.8s headroom)
- Honor user decision D-12 (Claude's discretion on wording) by drafting the actual segments inline so the executor does not have to invent content

Output:
- src/data/script.ts: 2 new SCRIPT keys appended after DEVOLUCAO_ESPELHO_SILENCIOSO (~line 587), each a SpeechSegment[] of 5-7 segments matching the 3-layer mirror tone of the existing 8 + ESPELHO archetypes
- src/types/index.ts: DevolucaoArchetype union extended from 8 variants to 11 (8 baseline + ESPELHO_SILENCIOSO + CONTRA_FOBICO + PORTADOR)
- Type-check green, no test regressions (data-only wave — no test additions yet, those land in Wave 2)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-detectable-archetypes-contra-fobico-portador/34-CONTEXT.md
@.planning/phases/34-detectable-archetypes-contra-fobico-portador/34-RESEARCH.md
@.planning/phases/34-detectable-archetypes-contra-fobico-portador/34-VALIDATION.md
@CLAUDE.md
@src/data/script.ts
@src/types/index.ts

<interfaces>
<!-- Key types and existing patterns Wave 1 must align with -->

From src/types/index.ts:
```typescript
export interface SpeechSegment {
  text: string;
  pauseAfter?: number; // milliseconds
  inflection?: string[]; // v3 audio tags, e.g. ["thoughtful"], ["whispers", "sad"]
}

export type DevolucaoArchetype =
  | 'SEEKER'
  | 'GUARDIAN'
  | 'CONTRADICTED'
  | 'PIVOT_EARLY'
  | 'PIVOT_LATE'
  | 'DEPTH_SEEKER'
  | 'SURFACE_KEEPER'
  | 'MIRROR';
// NOTE: 'ESPELHO_SILENCIOSO' is MISSING here even though Phase 33 ships a state with that name.
// Wave 1 MUST add ESPELHO_SILENCIOSO + CONTRA_FOBICO + PORTADOR (3 new variants total).
```

From src/data/script.ts (lines 580-587, template for new entries):
```typescript
DEVOLUCAO_ESPELHO_SILENCIOSO: [
  { text: "Tudo bem.", pauseAfter: 1500, inflection: ['warm'] },
  { text: "Você não vai sair daqui com um nome. Você vai sair com um silêncio que tem forma.", pauseAfter: 1500 },
  { text: "Eu não vou te dizer o que eu vi. Vou te dar o lugar onde isso poderia ser dito.", pauseAfter: 1500, inflection: ['thoughtful'] },
  { text: "Aqui:", pauseAfter: 2000 },
  { text: "O que você nunca pediu — mas que mesmo assim te falta?", pauseAfter: 1800, inflection: ['gentle'] },
  { text: "Esse é o seu espelho. Não tem moldura. Não tem reflexo. Só pergunta.", pauseAfter: 1400, inflection: ['warm'] },
],
```

From src/data/script.ts (lines 565-570, MIRROR template — 4-segment version showing 3-layer mirror compression):
```typescript
DEVOLUCAO_MIRROR: [
  { text: "Equilíbrio perfeito.", pauseAfter: 800, inflection: ['thoughtful'] },
  { text: "O equilíbrio perfeito pode ser sabedoria — ou a última forma de nunca se comprometer de verdade.", pauseAfter: 1000 },
  { text: "Balança que não pende nunca pesa nada. E o que não pesa também não transforma.", pauseAfter: 900 },
  { text: "Só você sabe se o centro é uma conquista ou um refúgio.", pauseAfter: 1000, inflection: ['warm'] },
],
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend DevolucaoArchetype union to add ESPELHO_SILENCIOSO + CONTRA_FOBICO + PORTADOR</name>
  <files>src/types/index.ts</files>
  <read_first>
    - src/types/index.ts (lines 1-50, focus on DevolucaoArchetype union at lines 24-32)
    - .planning/phases/34-detectable-archetypes-contra-fobico-portador/34-RESEARCH.md §"types/index.ts — Extend DevolucaoArchetype union" (insertion mechanic site #7)
    - .planning/phases/34-detectable-archetypes-contra-fobico-portador/34-CONTEXT.md decisions D-08 (script keys), D-31 (wave structure)
  </read_first>
  <action>
Open src/types/index.ts. Locate the `export type DevolucaoArchetype` declaration that currently spans lines 24-32. Replace it with the following expanded union. Preserve the inline comments format used by existing variants.

```typescript
/** Devolução archetype derived from pattern analysis */
export type DevolucaoArchetype =
  | 'SEEKER'              // mostly toward + movement
  | 'GUARDIAN'            // mostly away + stillness
  | 'CONTRADICTED'        // mixed pattern
  | 'PIVOT_EARLY'         // switches direction in first half
  | 'PIVOT_LATE'          // switches direction in second half
  | 'DEPTH_SEEKER'        // chose "deeper" option consistently
  | 'SURFACE_KEEPER'      // chose "safer" option consistently
  | 'MIRROR'              // alternating pattern — perfect balance
  | 'ESPELHO_SILENCIOSO'  // Phase 33, AR-01 — q6b='B', form-not-content devolução
  | 'CONTRA_FOBICO'       // Phase 34, AR-02 — q1='B' && q2='B' && q1b='A', courage walking toward what threatens
  | 'PORTADOR';           // Phase 34, AR-03 — q4='A' && q5='A' && q5b='A', the question as treasure
```

Save the file. Do not touch any other type or constant in this file. Do not modify ChoiceMap, QuestionMeta, QUESTION_META, PHASE_COLORS, or anything else.

Why this is the FIRST task in Wave 1: extending the union before the script keys ensures a TypeScript error cannot fire on the SCRIPT object even though SCRIPT keys are not type-constrained by DevolucaoArchetype directly (they are string keys), the union is consumed by patternMatching.ts and downstream test fixtures in Wave 2. Doing the type change first is safer.

Note: This adds ESPELHO_SILENCIOSO retroactively. Phase 33 shipped the state without adding it to the union (verified via grep — see acceptance criterion below). This is a Phase 33 type-gap that Wave 1 of Phase 34 backfills per CONTEXT D-31 / RESEARCH §"DevolucaoArchetype union" status note. No code currently references the literal 'ESPELHO_SILENCIOSO' as a DevolucaoArchetype value, so the addition is purely additive (zero breakage risk).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Command `grep -q "'ESPELHO_SILENCIOSO'" src/types/index.ts` exits 0
    - Command `grep -q "'CONTRA_FOBICO'" src/types/index.ts` exits 0
    - Command `grep -q "'PORTADOR'" src/types/index.ts` exits 0
    - Command `grep -c "  | '" src/types/index.ts` returns at least 11 (the union now has 11 variants)
    - Command `npx tsc --noEmit` exits 0 (no type errors anywhere in the project)
    - File src/types/index.ts is otherwise unchanged (only the DevolucaoArchetype union block was modified)
    - VALIDATION.md row "34-01-* | type-check | npx tsc --noEmit" status flips to green
  </acceptance_criteria>
  <done>
    DevolucaoArchetype union has 11 variants (was 8). Project type-checks green. No code changes outside the union declaration in src/types/index.ts.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add SCRIPT.DEVOLUCAO_CONTRA_FOBICO and SCRIPT.DEVOLUCAO_PORTADOR to script.ts</name>
  <files>src/data/script.ts</files>
  <read_first>
    - src/data/script.ts (lines 540-600 — DEVOLUCAO_PIVOT_LATE through DEVOLUCAO_ESPELHO_SILENCIOSO and ENCERRAMENTO; this is the insertion zone)
    - src/data/script.ts (lines 565-570 — DEVOLUCAO_MIRROR — tone reference for compression)
    - src/data/script.ts (lines 580-587 — DEVOLUCAO_ESPELHO_SILENCIOSO — tone reference for inflection tag inlining + closing-question structure)
    - .planning/phases/34-detectable-archetypes-contra-fobico-portador/34-CONTEXT.md decisions D-08 (key names), D-09 (5-7 segments / 22-25s target), D-10 (tone themes), D-11 (forbidden vocabulary), D-12 (Claude discretion)
    - .planning/phases/34-detectable-archetypes-contra-fobico-portador/34-CONTEXT.md <specifics> block (CONTRA_FOBICO content directions, PORTADOR content directions, contrasting profile notes)
    - C:/Users/USER/Oraculo/CLAUDE.md §"Change script text" workflow
    - .planning/MEMORY.md §"Patterns Learned" — voice ID PznTnBc8X6pvixs9UkQm + inflection tag compatibility (whispering does NOT render — never use it)
  </read_first>
  <action>
Open src/data/script.ts. Locate the closing bracket of the `DEVOLUCAO_ESPELHO_SILENCIOSO` array (currently around line 587). Insert the two new SCRIPT keys IMMEDIATELY after that closing bracket+comma and BEFORE the `ENCERRAMENTO` block comment (which currently starts around line 589).

Insert this exact block (do not modify the segment text — it has been pre-drafted to honor D-10/D-11/D-12 and the contrast cases in CONTEXT <specifics>):

```typescript
  // ═══════════════════════════════════════════════════════════════
  // DEVOLUCAO_CONTRA_FOBICO (Phase 34, AR-02)
  // Triggered when q1='B' && q2='B' && q1b='A':
  //   visitor procurou a porta, ficou olhando o que repugna, atravessou o vazio.
  // 3-layer mirror: recognition (sees the gesture) → reframing (refuses bravado)
  //                 → naming (a rare profile, not a hero). Closing line refuses
  //                 to label the visitor — preserves open form (like ESPELHO).
  // 6 segments, target ~23-25s. Echoes Q1B "porta no fundo" / "câmara queimando"
  // imagery without recapping. Distinguishes from PIVOT_EARLY (which pivots LATER).
  // ═══════════════════════════════════════════════════════════════
  DEVOLUCAO_CONTRA_FOBICO: [
    { text: "Você não fugiu do que arde.", pauseAfter: 1000, inflection: ['warm'] },
    { text: "Quase ninguém fica na sala quando a saída aparece. Quase ninguém olha o que repugna. E quase ninguém atravessa quando o vazio convida.", pauseAfter: 1000 },
    { text: "Isso não é coragem. Coragem ainda tem um nome — e o nome serve de muleta.", pauseAfter: 900, inflection: ['thoughtful'] },
    { text: "O que você fez é mais raro: caminhar em direção ao que ameaça, sem precisar chamar de heroísmo.", pauseAfter: 1000 },
    { text: "Quem anda para dentro do fogo não está procurando queimar. Está procurando o que só o fogo mostra.", pauseAfter: 1000, inflection: ['serious'] },
    { text: "Eu não vou te dar um nome para isso. Esse gesto é seu — e ele já te conhece.", pauseAfter: 1800, inflection: ['warm'] },
  ],

  // ═══════════════════════════════════════════════════════════════
  // DEVOLUCAO_PORTADOR (Phase 34, AR-03)
  // Triggered when q4='A' && q5='A' && q5b='A':
  //   visitor lembrou tudo, carrega a pergunta, fundiu memória e pergunta numa só carga.
  // 3-layer mirror: recognition (some questions are not problems) → reframing
  //                 (cargo, not burden) → distinguishing from GUARDIAN (holds FOR
  //                 vs holds AGAINST). Closing line refuses prescription — the
  //                 carrier IS the inheritance.
  // 6 segments, target ~23-25s. Echoes Q5B "memória que pesa" / "tesouro" without
  // repeating words. Distinguishes from GUARDIAN (protective) and DEPTH_SEEKER (compulsive).
  // ═══════════════════════════════════════════════════════════════
  DEVOLUCAO_PORTADOR: [
    { text: "Você lembrou de tudo. E mesmo assim escolheu carregar.", pauseAfter: 1000, inflection: ['warm'] },
    { text: "Tem perguntas que não são problemas. Não pedem solução, não esperam alívio.", pauseAfter: 900, inflection: ['thoughtful'] },
    { text: "Elas são cargas. E quem carrega não está preso — está sendo herdeiro de alguma coisa que ainda não tem nome.", pauseAfter: 1000 },
    { text: "Guardião segura para que nada entre. Você segura para que algo continue existindo. É outro gesto.", pauseAfter: 1000, inflection: ['gentle'] },
    { text: "A pergunta que pesa em você não é um peso a tirar. É um tesouro que pede um corpo para continuar fazendo sentido.", pauseAfter: 1000, inflection: ['serious'] },
    { text: "Eu não vou te dizer o que ela quer dizer. Mas ela está bem onde está — e você também.", pauseAfter: 1800, inflection: ['warm'] },
  ],

```

Then save the file. Do not touch any existing SCRIPT entry, comment, or constant. The block goes BETWEEN DEVOLUCAO_ESPELHO_SILENCIOSO and the ENCERRAMENTO comment header.

Tone constraints satisfied (verify by re-reading aloud after insertion):
- D-11 forbidden words check: no "Winnicott", "Lacan", "Bion", no "Você é", no "desbloqueou" — confirmed in drafted text
- D-11 required: at least one warm/gentle inflection per script — confirmed (warm appears 3× in CONTRA_FOBICO, warm 2× + gentle 1× in PORTADOR)
- D-09 segment count: 6 each (within 5-7 range)
- D-09 target duration: ~22-25s — verified in Wave 3 by validate-timing.ts; if any path overshoots 7:30 budget, Wave 3 trims one segment
- D-10 closing line refuses to label visitor: "Esse gesto é seu — e ele já te conhece" / "Eu não vou te dizer o que ela quer dizer. Mas ela está bem onde está — e você também" — confirmed open form
- D-10 echoes branch imagery without recapping: "anda para dentro do fogo" (echoes Q1B câmara queimando) / "tesouro que pede um corpo" (echoes Q5B "como tesouro") — confirmed
- D-10 distinguishes from neighbor archetypes: CONTRA_FOBICO explicitly contrasts with "coragem" (PIVOT_EARLY), PORTADOR explicitly contrasts with GUARDIAN — confirmed
- Inflection tag list check: only `warm`, `thoughtful`, `serious`, `gentle` used (per MEMORY.md compatibility list with voice ID PznTnBc8X6pvixs9UkQm). No `whispering`. No experimental tags.
- pauseAfter convention check: most segments 900-1000ms, closing segments 1800ms (matches Phase 33 ESPELHO_SILENCIOSO pattern)

After insertion, the SCRIPT object should have exactly 82 top-level keys (was 80 after Phase 33: 61 base + 6 Q1B + 6 Q5B + 7 Q6B/ESPELHO = 80; +2 new = 82).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Command `grep -n "DEVOLUCAO_CONTRA_FOBICO:" src/data/script.ts` exits 0 and shows exactly one occurrence
    - Command `grep -n "DEVOLUCAO_PORTADOR:" src/data/script.ts` exits 0 and shows exactly one occurrence
    - Command `grep -c "DEVOLUCAO_CONTRA_FOBICO" src/data/script.ts` returns at least 1 (key declaration)
    - Command `grep -c "DEVOLUCAO_PORTADOR" src/data/script.ts` returns at least 1 (key declaration)
    - Command `grep -q "Você não fugiu do que arde" src/data/script.ts` exits 0 (CONTRA_FOBICO opening line)
    - Command `grep -q "tem perguntas que não são problemas" src/data/script.ts` exits 0 (PORTADOR core line, case-insensitive via -i if needed; otherwise grep -qi)
    - Command `grep -c "DEVOLUCAO_" src/data/script.ts` returns at least 11 entries (was 9 — 8 baseline + ESPELHO; now 11 including CONTRA_FOBICO + PORTADOR)
    - Both new arrays contain between 5 and 7 elements (verify visually + by counting `text:` occurrences inside each block)
    - Neither new block contains the substring `whispering` (forbidden inflection per MEMORY.md)
    - Neither new block contains the substring `Winnicott`, `Lacan`, `Bion`
    - Neither new block contains the substring `Você é` (declarative diagnosis forbidden per D-11)
    - Neither new block contains the substring `desbloqueou` (gamified language forbidden per D-11)
    - Command `npx tsc --noEmit` exits 0 (no new type errors introduced by SCRIPT extension)
    - Quick test command from VALIDATION.md row "34-01-* | unit (script keys) | npm test src/data/__tests__ -- --run" exits 0 (existing data tests still pass)
  </acceptance_criteria>
  <done>
    SCRIPT object has 82 top-level keys including DEVOLUCAO_CONTRA_FOBICO and DEVOLUCAO_PORTADOR. Each new key is a SpeechSegment[] of 6 segments using only allowed inflection tags. Type-check green. Existing src/data/__tests__ tests still pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run full regression suite to confirm Wave 1 introduced zero breakage</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - .planning/phases/34-detectable-archetypes-contra-fobico-portador/34-VALIDATION.md §"Sampling Rate" + per-task verification map
    - .planning/phases/33-q6b-espelho-silencioso/33-VERIFICATION.md (Phase 33 baseline: 698/714 passing, 16 known failures)
  </read_first>
  <action>
Run the full regression suite in one-shot mode to confirm Wave 1 (script + type changes only) introduced zero behavioral regressions. This task does NOT modify any files — it is a Wave 1 close-out gate before Wave 2 begins.

Command:
```
npm test -- --run 2>&1 | tail -50
```

Inspect the output. The expected baseline after Wave 1 is identical to the pre-Phase-34 baseline:
- Test Files: 41 passed, 2 failed (43 total)
- Tests: 698 passed, 16 failed, 3 skipped (717 total)
- Known failures (do NOT count as regressions, do NOT fix):
  - voice-flow-integration.test.ts: 15 failures (v1.0 PURGATORIO_A/B states obsolete)
  - ambient-player.test.ts: ~2 failures (Phase 30 modifications)

If passing count < 698 OR failing count > 16: STOP. The Wave 1 changes broke something. Roll back Task 1 or Task 2 changes (likely a typo in the script segment text causing a parse error, or a stray edit elsewhere in src/types/index.ts), re-run, and only proceed when the baseline holds.

If passing count == 698 (or higher — should NOT be higher since Wave 1 adds no tests) and failing count == 16: Wave 1 is green. Proceed to commit.

Note: fallback-tts.test.ts SCRIPT key count assertion (currently 80) WILL fail after Task 2 because SCRIPT now has 82 keys. THIS IS EXPECTED — the assertion is updated in Wave 3 (per CONTEXT D-29). Wave 1 acknowledges this as a known transient regression: it is the ONLY new failure permitted in Wave 1's intermediate state. Confirm by checking that the only new failure in the output is `fallback-tts.test.ts > FallbackTTSService > should have a URL entry for every key in SCRIPT (80 keys)` and that the failure message says "expected 82 to be 80" or similar.

Therefore the Wave 1 acceptance is:
- Tests: 697 passed, 17 failed, 3 skipped (717 total) — exactly ONE new failure, the SCRIPT count assertion in fallback-tts.test.ts.
- All other previously-passing tests still pass.
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - npm test exit code is 1 (the ONE expected fallback-tts.test.ts SCRIPT count assertion fails — Wave 3 will fix it)
    - Output line "Tests" shows exactly 17 failed (was 16; +1 from fallback-tts SCRIPT count assertion)
    - Output line "Tests" shows at least 697 passed
    - The ONLY new failure is `fallback-tts.test.ts` failing on the assertion `expect(scriptKeyCount).toBe(80)` (the new value is 82)
    - No new failures appear in voice-flow-integration.test.ts (count stays at 15)
    - No new failures appear in ambient-player.test.ts (count stays at 2)
    - No new failures in patternMatching.test.ts, oracleMachine.test.ts, OracleExperience-helpers.test.ts, or any other suite
    - VALIDATION.md row "34-01-* | unit (script keys) | npm test src/data/__tests__ -- --run" exits 0 (data tests intact)
  </acceptance_criteria>
  <done>
    Full suite run shows 697 passing / 17 failing / 3 skipped. The single new failure is the EXPECTED fallback-tts.test.ts SCRIPT count assertion (80 → must be 82, fixed in Wave 3). All other test files unchanged from Phase 33 baseline. Wave 1 is ready to commit.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. Type system: `npx tsc --noEmit` exits 0
2. Script keys: `node -e "const {SCRIPT}=require('./src/data/script.ts'); console.log(Object.keys(SCRIPT).length)"` is not directly runnable (TS file) — instead use `grep -c "^  [A-Z_]\+: \[" src/data/script.ts` to count top-level array keys (should be ≥82)
3. Test baseline: 697 passing / 17 failing / 3 skipped — the +1 failure is the EXPECTED fallback-tts SCRIPT count assertion that Wave 3 will fix
4. Forbidden vocabulary check: `grep -E "(Winnicott|Lacan|Bion|whispering|Você é|desbloqueou)" src/data/script.ts` should return ZERO matches inside the two new SCRIPT entries (a few hits in pre-existing entries are fine — only check the new blocks visually)
5. DevolucaoArchetype union: contains 'ESPELHO_SILENCIOSO', 'CONTRA_FOBICO', 'PORTADOR' (verified by grep)

Commit:
```
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "feat(34-01): add CONTRA_FOBICO + PORTADOR scripts and extend DevolucaoArchetype" --files src/data/script.ts src/types/index.ts
```
</verification>

<success_criteria>
- src/data/script.ts has exactly 2 new top-level keys: DEVOLUCAO_CONTRA_FOBICO and DEVOLUCAO_PORTADOR
- Each new SCRIPT entry is a SpeechSegment[] of 6 segments (within the 5-7 range from D-09)
- Each new SCRIPT entry uses only inflection tags from the allowed set: warm, gentle, thoughtful, serious, determined
- Neither new SCRIPT entry contains forbidden vocabulary (Winnicott, Lacan, Bion, "Você é", "desbloqueou", "whispering")
- Each new SCRIPT entry has a closing line that mirrors the gesture without prescribing meaning (open form)
- src/types/index.ts DevolucaoArchetype union now has 11 variants (was 8: +ESPELHO_SILENCIOSO +CONTRA_FOBICO +PORTADOR)
- `npx tsc --noEmit` exits 0
- Full test suite: 697 passing, 17 failing (the +1 failure is the EXPECTED fallback-tts SCRIPT count assertion which Wave 3 fixes), 3 skipped
- Wave 2 (34-02) can begin: it has the SCRIPT keys + types it needs to write the guards, machine states, helper extensions, and tests
</success_criteria>

<output>
After completion, create `.planning/phases/34-detectable-archetypes-contra-fobico-portador/34-01-SUMMARY.md` documenting:
- Final segment text for both new SCRIPT keys (so future phases can reference what shipped)
- Test baseline after Wave 1 (697 passing / 17 failing / 3 skipped — note the EXPECTED transient failure)
- Type union state (8 → 11 variants)
- Hand-off to Wave 2: machine + guards + UI + tests next
</output>
