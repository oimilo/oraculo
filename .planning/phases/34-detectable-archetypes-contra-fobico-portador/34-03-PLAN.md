---
phase: 34-detectable-archetypes-contra-fobico-portador
plan: 03
type: execute
wave: 3
depends_on: [34-02]
files_modified:
  - public/audio/oracle/devolucao_contra_fobico.mp3
  - public/audio/oracle/devolucao_portador.mp3
  - scripts/validate-timing.ts
  - public/roteiro.html
  - src/services/tts/__tests__/fallback-tts.test.ts
autonomous: false
requirements: [AR-02, AR-03]

must_haves:
  truths:
    - "public/audio/oracle/devolucao_contra_fobico.mp3 exists as a non-empty MP3 file generated via scripts/generate-audio-v3.ts using voice ID PznTnBc8X6pvixs9UkQm + eleven_v3 model"
    - "public/audio/oracle/devolucao_portador.mp3 exists as a non-empty MP3 file generated with the same parameters"
    - "Both new MP3 file sizes fall in the ~0.4-0.9 MB range (comparable to DEVOLUCAO_ESPELHO_SILENCIOSO at ~0.64 MB)"
    - "scripts/validate-timing.ts pickLongestDevolucao() includes DEVOLUCAO_CONTRA_FOBICO and DEVOLUCAO_PORTADOR in its devolucoes array (now 10 candidates instead of 8)"
    - "scripts/validate-timing.ts PathConfig interface and ALL_PATHS array include new permutations exercising CONTRA_FOBICO + PORTADOR triggers"
    - "scripts/validate-timing.ts exits 0 after Wave 3 — max-path ≤ 450 seconds (7:30 min) across all paths including the new ones"
    - "If max-path exceeds 7:30 after MP3 generation, Wave 3 trims ONE segment from the longest new devolução script, regenerates the affected MP3, and re-runs the validator until it exits 0"
    - "src/services/tts/__tests__/fallback-tts.test.ts SCRIPT key count assertion is updated from 80 to 82 and the test passes"
    - "public/roteiro.html contains two new devolucao-card entries for CONTRA_FOBICO and PORTADOR, matching the existing card layout (h4 + criteria div + text div)"
    - "public/roteiro.html archetype explanation list (around line 961) is updated to mention CONTRA_FOBICO and PORTADOR and lists 11 archetypes instead of 9"
    - "Full test suite: ≥712 passing / 16 failing (the fallback-tts assertion is now fixed so total failures drop from 17 to 16 — matching the pre-Phase-34 baseline of known failures) / 3 skipped"
    - "Zero new regressions introduced by Wave 3 changes"
  artifacts:
    - path: public/audio/oracle/devolucao_contra_fobico.mp3
      provides: "Generated MP3 for DEVOLUCAO_CONTRA_FOBICO archetype — plays when isContraFobico guard fires"
    - path: public/audio/oracle/devolucao_portador.mp3
      provides: "Generated MP3 for DEVOLUCAO_PORTADOR archetype — plays when isPortador guard fires"
    - path: scripts/validate-timing.ts
      provides: "Extended validator supporting 10 devolução candidates + new path permutations for CONTRA_FOBICO and PORTADOR triggers"
      contains: ["DEVOLUCAO_CONTRA_FOBICO", "DEVOLUCAO_PORTADOR", "hasContraFobico", "hasPortador"]
    - path: public/roteiro.html
      provides: "Two new archetype cards + updated priority list reflecting 11 total archetypes"
      contains: ["CONTRA_FOBICO", "PORTADOR", "Contra-F", "Portador"]
    - path: src/services/tts/__tests__/fallback-tts.test.ts
      provides: "SCRIPT key count assertion updated from 80 to 82 — test now green"
      contains: ["82"]
  key_links:
    - from: "scripts/generate-audio-v3.ts auto-discovery"
      to: "public/audio/oracle/devolucao_contra_fobico.mp3 + devolucao_portador.mp3"
      via: "Object.keys(SCRIPT) iteration"
      pattern: "devolucao_contra_fobico\\.mp3|devolucao_portador\\.mp3"
    - from: "scripts/validate-timing.ts PathConfig"
      to: "PORTADOR/CONTRA_FOBICO path permutations"
      via: "hasContraFobico / hasPortador PathConfig flags + matching pickLongestDevolucao logic"
      pattern: "hasContraFobico|hasPortador"
    - from: "public/roteiro.html devolucao-grid"
      to: "new archetype cards"
      via: "div.devolucao-card layout with h4 + criteria + text"
      pattern: "Contra-F.bico|Portador"
---

<objective>
Wave 3 of Phase 34: generate the two new MP3s, extend the timing validator with new path permutations, update the timing validator's pickLongestDevolucao to consider the new archetypes, fix the fallback-tts SCRIPT count assertion, and add the new archetype cards to the roteiro.html client-facing document. This is the polish wave — it closes Phase 34 by making the runtime actually AUDIBLE (not just logically routable) and restores the test suite to a fully-green baseline.

Purpose:
- Satisfy AR-02 and AR-03 at the audio level (visitors actually HEAR the new devolutions via ElevenLabs v3)
- Restore POL-01 timing invariant: validator exits 0 with max-path ≤ 7:30 min, even with the new archetype candidates in play
- Fix the Wave 1/2 transient test failure (fallback-tts SCRIPT count assertion: 80 → 82)
- Update public/roteiro.html (client-facing script + devolução catalog) to reflect the 11 archetypes now in play (partial POL-03 sync — full POL-03 coherence pass remains Phase 35's job)
- Leave the repository in a fully-clean state: 16 known failures exactly (same as pre-Phase-34 baseline), ≥712 passing

Output:
- public/audio/oracle/devolucao_contra_fobico.mp3 (new MP3, ~0.5-0.8 MB)
- public/audio/oracle/devolucao_portador.mp3 (new MP3, ~0.5-0.8 MB)
- scripts/validate-timing.ts extended with 4 new path permutations + pickLongestDevolucao extended to 10 candidates (with trigger-aware selection)
- public/roteiro.html with 2 new devolucao-card entries and updated priority list
- src/services/tts/__tests__/fallback-tts.test.ts with SCRIPT count assertion bumped to 82
- Full regression suite clean: ≥712 passing / 16 failing / 3 skipped
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-detectable-archetypes-contra-fobico-portador/34-CONTEXT.md
@.planning/phases/34-detectable-archetypes-contra-fobico-portador/34-RESEARCH.md
@.planning/phases/34-detectable-archetypes-contra-fobico-portador/34-VALIDATION.md
@.planning/phases/34-detectable-archetypes-contra-fobico-portador/34-01-SUMMARY.md
@.planning/phases/34-detectable-archetypes-contra-fobico-portador/34-02-SUMMARY.md
@CLAUDE.md
@scripts/generate-audio-v3.ts
@scripts/validate-timing.ts
@public/roteiro.html
@src/services/tts/__tests__/fallback-tts.test.ts

<interfaces>
<!-- Key existing patterns Wave 3 must align with -->

From scripts/validate-timing.ts (line 110-140, pickLongestDevolucao — current signature):
```typescript
function pickLongestDevolucao(hasEspelhoSilencioso: boolean): { segments: SpeechSegment[], key: string } {
  if (hasEspelhoSilencioso) {
    return { key: 'DEVOLUCAO_ESPELHO_SILENCIOSO', segments: SCRIPT.DEVOLUCAO_ESPELHO_SILENCIOSO };
  }
  const devolucoes = [
    { key: 'DEVOLUCAO_SEEKER', segments: SCRIPT.DEVOLUCAO_SEEKER },
    // ... 7 more (8 baseline)
  ];
  // ... loop to find longest
}
```

Wave 3 extends this signature to accept trigger booleans for the Phase 34 archetypes:
```typescript
function pickLongestDevolucao(
  hasEspelhoSilencioso: boolean,
  hasContraFobico: boolean,
  hasPortador: boolean,
): { segments: SpeechSegment[], key: string } {
  // ESPELHO wins first (priority [0] in machine)
  if (hasEspelhoSilencioso) { return { key: 'DEVOLUCAO_ESPELHO_SILENCIOSO', segments: SCRIPT.DEVOLUCAO_ESPELHO_SILENCIOSO }; }
  // CONTRA_FOBICO wins second (priority [1])
  if (hasContraFobico) { return { key: 'DEVOLUCAO_CONTRA_FOBICO', segments: SCRIPT.DEVOLUCAO_CONTRA_FOBICO }; }
  // PORTADOR wins third (priority [2])
  if (hasPortador) { return { key: 'DEVOLUCAO_PORTADOR', segments: SCRIPT.DEVOLUCAO_PORTADOR }; }
  // Otherwise fall through to longest of 8 baseline archetypes (existing logic)
  const devolucoes = [ ... 8 baseline ... ];
  // ... loop
}
```

From scripts/validate-timing.ts PathConfig (line 156-165):
```typescript
interface PathConfig {
  name: string;
  hasQ1B: boolean;
  hasQ2B: boolean;
  hasQ4B: boolean;
  hasQ5B: boolean;
  hasQ6B: boolean;
  hasEspelhoSilencioso: boolean;
  questionCount: number;
}
```

Wave 3 extends this with:
```typescript
  hasContraFobico: boolean;  // Phase 34, AR-02 — fires when hasQ1B && (q1b response = 'A')
  hasPortador: boolean;      // Phase 34, AR-03 — fires when hasQ5B && (q5b response = 'A')
```

From scripts/validate-timing.ts assertValidPath (line 168-178) — add mutual exclusion rules:
- hasContraFobico implies hasQ1B (CONTRA_FOBICO only reachable via Q1B branch)
- hasPortador implies hasQ5B (PORTADOR only reachable via Q5B branch)
- hasContraFobico and hasEspelhoSilencioso can BOTH be true (machine priority: ESPELHO wins; validator must reflect that)

From public/roteiro.html card template (lines 989-995 — SEEKER card, simplest example):
```html
    <div class="devolucao-card">
      <h4>O Buscador <span style="font-size:0.7rem;color:var(--muted);">(SEEKER)</span></h4>
      <div class="criteria">&ge;66% A &mdash; movimento, abertura, confronto</div>
      <div class="text">
        [full devolução text with &amp; entities]
      </div>
    </div>
```

From public/roteiro.html ESPELHO card (lines 975-987 — first card, has a color border override for highest priority):
```html
    <div class="devolucao-card" style="border-left:4px solid #8b7530;">
      <h4>Espelho Silencioso <span style="font-size:0.7rem;color:var(--muted);">(ESPELHO_SILENCIOSO)</span></h4>
      <div class="criteria">Q6B=B — Highest priority (devolve forma, não conteúdo)</div>
      <div class="text">
        [text]
      </div>
    </div>
```

From src/services/tts/__tests__/fallback-tts.test.ts (lines 134-145, the assertion to update):
```typescript
it('should have a URL entry for every key in SCRIPT (80 keys)', () => {
  const scriptKeyCount = Object.keys(SCRIPT).length;
  expect(scriptKeyCount).toBe(80);
  // ...
});
```

Wave 3 updates:
- Test name: `'should have a URL entry for every key in SCRIPT (82 keys)'`
- Assertion: `expect(scriptKeyCount).toBe(82);`
- Comment on the line above the assertion to mention Phase 34: `// 80 from Phase 33 + 2 Phase 34 (CONTRA_FOBICO + PORTADOR) = 82`

From CLAUDE.md §Environment Variables:
```
ELEVENLABS_API_KEY=sk_...
ELEVENLABS_VOICE_ID=PznTnBc8X6pvixs9UkQm
USE_V3_MODEL=true
```
These env vars must be present (they're in .env.local) for generate-audio-v3.ts to work.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0 (pre-check): Confirm ELEVENLABS_API_KEY is set before audio generation</name>
  <files>(no files modified — environment pre-check only)</files>
  <read_first>
    - scripts/generate-audio-v3.ts (lines 1-40 — confirm env var usage)
    - CLAUDE.md §Environment Variables
  </read_first>
  <action>
No code changes. This is a lightweight pre-check before running `generate-audio-v3.ts`, which makes paid API calls to ElevenLabs.

Run this command to confirm the API key is set in the environment:
```
node -e "console.log('API key set:', !!process.env.ELEVENLABS_API_KEY); console.log('Voice ID:', process.env.ELEVENLABS_VOICE_ID); console.log('V3 model:', process.env.USE_V3_MODEL);"
```

Expected output:
```
API key set: true
Voice ID: PznTnBc8X6pvixs9UkQm
V3 model: true
```

If API key is NOT set or the command reports `false`:
1. Check .env.local exists in project root
2. Confirm ELEVENLABS_API_KEY=sk_... is present
3. If running via npm script, env vars are loaded automatically by Next.js tooling; for ts-node direct runs, you may need to prefix the command with `dotenv -e .env.local -- npx ts-node scripts/generate-audio-v3.ts` or source the env file first

This task pauses Wave 3 only if the env check reveals a misconfiguration that would cause Task 1 to fail. If the env check passes, approve immediately and continue to Task 1.

Resume signal: Type "approved" or "env ok" to proceed. If env is broken, fix .env.local first then approve.
  </action>
  <verify>
    <automated>node -e "if (!process.env.ELEVENLABS_API_KEY) { console.error('ELEVENLABS_API_KEY not set'); process.exit(1); } else { console.log('ok'); }"</automated>
  </verify>
  <acceptance_criteria>
    - Command `node -e "console.log(!!process.env.ELEVENLABS_API_KEY)"` prints `true`
    - User confirms the environment is ready for MP3 generation
    - Resume signal received ("approved" or "env ok")
  </acceptance_criteria>
  <done>
    ELEVENLABS_API_KEY confirmed present in environment. User has approved proceeding to audio generation. Task 1 can safely run generate-audio-v3.ts without wasting API calls on a misconfigured environment.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Generate DEVOLUCAO_CONTRA_FOBICO.mp3 + DEVOLUCAO_PORTADOR.mp3 via generate-audio-v3.ts</name>
  <files>public/audio/oracle/devolucao_contra_fobico.mp3, public/audio/oracle/devolucao_portador.mp3</files>
  <read_first>
    - scripts/generate-audio-v3.ts (full file — understand how it walks SCRIPT keys and writes .mp3 files)
    - CLAUDE.md §"Change script text" workflow (MP3 generation step)
    - .planning/phases/33-q6b-espelho-silencioso/33-03-SUMMARY.md if present (for file-size expectations and generation time)
  </read_first>
  <action>
Run the audio generation script. It auto-discovers ALL keys in SCRIPT and generates one MP3 per key IF the target file does not already exist. This means the script will SKIP all 80 already-generated MP3s and ONLY create the 2 new ones (devolucao_contra_fobico.mp3 + devolucao_portador.mp3).

Command:
```
npx ts-node scripts/generate-audio-v3.ts 2>&1 | tee /tmp/generate-audio-34.log
```

Expected behavior:
- Script reads src/data/script.ts, enumerates all 82 SCRIPT keys
- For each key, checks if public/audio/oracle/{key.toLowerCase()}.mp3 exists
- Skips files that already exist (80 skips)
- Generates MP3 via ElevenLabs v3 API for the 2 new keys (DEVOLUCAO_CONTRA_FOBICO, DEVOLUCAO_PORTADOR)
- Writes to public/audio/oracle/devolucao_contra_fobico.mp3 and public/audio/oracle/devolucao_portador.mp3
- Each new MP3 should be ~0.4-0.9 MB (comparable to DEVOLUCAO_ESPELHO_SILENCIOSO's 0.64 MB)
- Total cost: ~$0.10-0.20 (2 calls to eleven_v3, ~250 chars each)

After the script completes, verify:
```
ls -la public/audio/oracle/devolucao_contra_fobico.mp3
ls -la public/audio/oracle/devolucao_portador.mp3
```
Both files should exist and have non-zero size.

Listen to both MP3s briefly (optional but recommended):
```
# On Windows:
start public/audio/oracle/devolucao_contra_fobico.mp3
start public/audio/oracle/devolucao_portador.mp3
```
This is a spot-check — does the voice match the Oracle? Are the inflection tags rendering? (Formal tone quality QA is a Manual-Only Verification from 34-VALIDATION.md; this is just a smoke-listen.)

If the script FAILS to generate one of the files:
- Check /tmp/generate-audio-34.log for the error message
- Common causes: API rate limit (wait 1 min and retry), API key expired (rotate and re-run), script text has special chars that break the v3 model (unlikely — both scripts use only ASCII + Portuguese accents which have been validated in Phase 33)
- Manual retry via single-key mode: if generate-audio-v3.ts supports a `--key` flag, use it; otherwise temporarily rename the OTHER MP3 and re-run the script to force regeneration of just the missing one

If the script reports "already exists" for a new file that should NOT exist: the file was created in an earlier attempt. Delete it and re-run.
  </action>
  <verify>
    <automated>test -f public/audio/oracle/devolucao_contra_fobico.mp3 && test -f public/audio/oracle/devolucao_portador.mp3 && test -s public/audio/oracle/devolucao_contra_fobico.mp3 && test -s public/audio/oracle/devolucao_portador.mp3</automated>
  </verify>
  <acceptance_criteria>
    - File `public/audio/oracle/devolucao_contra_fobico.mp3` exists
    - File `public/audio/oracle/devolucao_portador.mp3` exists
    - Both files have size > 100 KB (non-empty, containing actual audio data — expected ~400-900 KB each)
    - Both files have size < 2 MB (upper sanity bound — anything larger suggests a generation problem)
    - Command `file public/audio/oracle/devolucao_contra_fobico.mp3` (if `file` is available) reports "Audio file" or similar MP3 signature; otherwise the `test -s` check is sufficient
    - generate-audio-v3.ts exited 0
    - Generation log shows 2 new files created (look for lines containing "devolucao_contra_fobico" and "devolucao_portador")
    - VALIDATION.md row "34-03-* | file existence | test -f ..." flips to green
  </acceptance_criteria>
  <done>
    Both new MP3 files exist in public/audio/oracle/ with non-zero size. generate-audio-v3.ts completed successfully. Two new MP3s generated, 80 existing MP3s skipped.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend scripts/validate-timing.ts with Phase 34 path permutations + trigger-aware devolução selection</name>
  <files>scripts/validate-timing.ts</files>
  <read_first>
    - scripts/validate-timing.ts (full file — 422 lines; pay special attention to pickLongestDevolucao at line 110, PathConfig at line 156, ALL_PATHS at line 184, assertValidPath at line 168, calculatePath at line 225)
    - .planning/phases/34-detectable-archetypes-contra-fobico-portador/34-RESEARCH.md §"Insertion Mechanics" section 6 (validate-timing.ts extension)
    - .planning/phases/34-detectable-archetypes-contra-fobico-portador/34-CONTEXT.md decisions D-21 (new paths), D-22 (budget tolerance), D-23 (must exit 0)
  </read_first>
  <action>
Open scripts/validate-timing.ts. Make these changes in order:

**Change 1: Extend pickLongestDevolucao signature (lines 110-140).**

Replace the function with:
```typescript
/**
 * Pick the longest DEVOLUCAO for max-path calculation (Phase 34 extended from Phase 33)
 *
 * Priority order (mirrors oracleMachine.ts DEVOLUCAO.always):
 * - [0] ESPELHO_SILENCIOSO (Phase 33) — fires when q6b='B'
 * - [1] CONTRA_FOBICO (Phase 34) — fires when q1=B && q2=B && q1b=A
 * - [2] PORTADOR (Phase 34) — fires when q4=A && q5=A && q5b=A
 * - [3-10] 8 baseline archetypes (longest picked via loop)
 *
 * When multiple priority-0/1/2 guards could fire, the HIGHEST priority wins
 * (first-match-wins in always[]). This mirrors runtime behavior exactly.
 */
function pickLongestDevolucao(
  hasEspelhoSilencioso: boolean,
  hasContraFobico: boolean,
  hasPortador: boolean,
): { segments: SpeechSegment[], key: string } {
  // Phase 33 — ESPELHO_SILENCIOSO pre-empts all other archetypes
  if (hasEspelhoSilencioso) {
    return { key: 'DEVOLUCAO_ESPELHO_SILENCIOSO', segments: SCRIPT.DEVOLUCAO_ESPELHO_SILENCIOSO };
  }

  // Phase 34 — CONTRA_FOBICO pre-empts PORTADOR and baseline archetypes
  if (hasContraFobico) {
    return { key: 'DEVOLUCAO_CONTRA_FOBICO', segments: SCRIPT.DEVOLUCAO_CONTRA_FOBICO };
  }

  // Phase 34 — PORTADOR pre-empts baseline archetypes
  if (hasPortador) {
    return { key: 'DEVOLUCAO_PORTADOR', segments: SCRIPT.DEVOLUCAO_PORTADOR };
  }

  // Existing 8 baseline archetypes (longest wins)
  const devolucoes = [
    { key: 'DEVOLUCAO_SEEKER', segments: SCRIPT.DEVOLUCAO_SEEKER },
    { key: 'DEVOLUCAO_GUARDIAN', segments: SCRIPT.DEVOLUCAO_GUARDIAN },
    { key: 'DEVOLUCAO_CONTRADICTED', segments: SCRIPT.DEVOLUCAO_CONTRADICTED },
    { key: 'DEVOLUCAO_PIVOT_EARLY', segments: SCRIPT.DEVOLUCAO_PIVOT_EARLY },
    { key: 'DEVOLUCAO_PIVOT_LATE', segments: SCRIPT.DEVOLUCAO_PIVOT_LATE },
    { key: 'DEVOLUCAO_DEPTH_SEEKER', segments: SCRIPT.DEVOLUCAO_DEPTH_SEEKER },
    { key: 'DEVOLUCAO_SURFACE_KEEPER', segments: SCRIPT.DEVOLUCAO_SURFACE_KEEPER },
    { key: 'DEVOLUCAO_MIRROR', segments: SCRIPT.DEVOLUCAO_MIRROR },
  ];

  let longest = devolucoes[0];
  let longestDuration = calculateSectionDuration(longest.segments);

  for (const devolucao of devolucoes.slice(1)) {
    const duration = calculateSectionDuration(devolucao.segments);
    if (duration > longestDuration) {
      longest = devolucao;
      longestDuration = duration;
    }
  }

  return longest;
}
```

**Change 2: Extend PathConfig interface (lines 156-165).**

Replace with:
```typescript
interface PathConfig {
  name: string;
  hasQ1B: boolean;
  hasQ2B: boolean;
  hasQ4B: boolean;
  hasQ5B: boolean;
  hasQ6B: boolean;
  hasEspelhoSilencioso: boolean;
  hasContraFobico: boolean;  // Phase 34, AR-02 — implies hasQ1B
  hasPortador: boolean;      // Phase 34, AR-03 — implies hasQ5B
  questionCount: number;
}
```

**Change 3: Extend assertValidPath (lines 168-178) with 2 new invariants.**

Replace with:
```typescript
function assertValidPath(p: PathConfig): void {
  if (p.hasQ5B && p.hasQ6B) {
    throw new Error(`Path "${p.name}": Q5B and Q6B are mutually exclusive (q5 cannot be both 'A' and 'B')`);
  }
  if (p.hasEspelhoSilencioso && !p.hasQ6B) {
    throw new Error(`Path "${p.name}": hasEspelhoSilencioso requires hasQ6B (ESPELHO only reachable via Q6B)`);
  }
  if (p.hasQ1B && p.hasQ2B) {
    throw new Error(`Path "${p.name}": Q1B and Q2B are mutually exclusive (q1 cannot be both 'A' and 'B')`);
  }
  // Phase 34 — CONTRA_FOBICO requires Q1B branch (only reachable via q1b response)
  if (p.hasContraFobico && !p.hasQ1B) {
    throw new Error(`Path "${p.name}": hasContraFobico requires hasQ1B (CONTRA_FOBICO only reachable via Q1B branch)`);
  }
  // Phase 34 — PORTADOR requires Q5B branch (only reachable via q5b response)
  if (p.hasPortador && !p.hasQ5B) {
    throw new Error(`Path "${p.name}": hasPortador requires hasQ5B (PORTADOR only reachable via Q5B branch)`);
  }
}
```

**Change 4: Add 4 new paths to ALL_PATHS array (around line 184-220).**

Every existing entry in ALL_PATHS must have `hasContraFobico: false, hasPortador: false` appended to preserve the interface. Append them to each existing entry. Then add 4 new paths at the end of the array (before the closing `];`):

```typescript
  // ========== Phase 34 — CONTRA_FOBICO paths (AR-02) ==========
  // CONTRA_FOBICO fires when hasQ1B AND q1b_response='A' (visitor atravessou o vazio).
  // Since validator picks longest RESPOSTA, we can't force q1b='A' directly,
  // but the CONTRA_FOBICO devolução is SELECTED by pickLongestDevolucao regardless.
  { name: 'Q1B + CONTRA_FOBICO (7Q)', hasQ1B: true, hasQ2B: false, hasQ4B: false, hasQ5B: false, hasQ6B: false, hasEspelhoSilencioso: false, hasContraFobico: true, hasPortador: false, questionCount: 7 },
  { name: 'Q1B + Q4B + CONTRA_FOBICO (8Q)', hasQ1B: true, hasQ2B: false, hasQ4B: true, hasQ5B: false, hasQ6B: false, hasEspelhoSilencioso: false, hasContraFobico: true, hasPortador: false, questionCount: 8 },

  // ========== Phase 34 — PORTADOR paths (AR-03) ==========
  // PORTADOR fires when hasQ5B AND q5b_response='A' (visitor fundiu pergunta e memória).
  { name: 'Q5B + PORTADOR (7Q)', hasQ1B: false, hasQ2B: false, hasQ4B: false, hasQ5B: true, hasQ6B: false, hasEspelhoSilencioso: false, hasContraFobico: false, hasPortador: true, questionCount: 7 },
  { name: 'Q1B + Q4B + Q5B + PORTADOR + CONTRA_FOBICO (9Q — worst case)', hasQ1B: true, hasQ2B: false, hasQ4B: true, hasQ5B: true, hasQ6B: false, hasEspelhoSilencioso: false, hasContraFobico: true, hasPortador: true, questionCount: 9 },
```

Note on the last path: both CONTRA_FOBICO and PORTADOR fire, but CONTRA_FOBICO wins (priority [1] beats [2]). The validator's `pickLongestDevolucao` returns CONTRA_FOBICO here, which matches the runtime behavior. This is the worst-case-stacking test and verifies that maximum branch nesting + highest-priority Phase 34 devolução still fits in the budget.

Expected final count: 20 existing + 4 new = **24 paths**.

Also update the summary comment at the top of the file (around lines 1-56) to reflect "V6.0 TIMING VALIDATION (BRANCH-AWARE) — PHASE 34 (24 PATHS)" and add the 4 new path entries to the numbered list in the header comment.

**Change 5: Update the call to pickLongestDevolucao inside calculatePath (around line 311-313).**

Current:
```typescript
const longestDevolucao = pickLongestDevolucao(config.hasEspelhoSilencioso);
```

Change to:
```typescript
const longestDevolucao = pickLongestDevolucao(
  config.hasEspelhoSilencioso,
  config.hasContraFobico,
  config.hasPortador,
);
```

**Change 6: Update the main() console header.**

Find `console.log('\n=== V6.0 TIMING VALIDATION (BRANCH-AWARE) — PHASE 33 (20 PATHS) ===\n');` (around line 335) and change to:
```typescript
console.log('\n=== V6.0 TIMING VALIDATION (BRANCH-AWARE) — PHASE 34 (24 PATHS) ===\n');
```

Find `console.log('\nAll 20 paths fall within acceptable range.');` (around line 399) and change to:
```typescript
console.log('\nAll 24 paths fall within acceptable range.');
```

Save the file. Run it:
```
npx ts-node scripts/validate-timing.ts
```

Expected exit 0 with max-path ≤ 450s. Currently 7:01.2 (421.2s) with 28.8s headroom. The new CONTRA_FOBICO and PORTADOR scripts target 22-25s each, which is comparable to or shorter than DEPTH_SEEKER (28s) and MIRROR (24s). Substituting them into the worst-case paths should not push total beyond 7:30.

**If max-path > 450s (exceeds budget):**
1. Identify the longest-offending path from the validator output
2. Identify which new devolução script is causing the overflow (DEVOLUCAO_CONTRA_FOBICO or DEVOLUCAO_PORTADOR) — compute its duration manually using calculateSectionDuration
3. Edit src/data/script.ts to trim ONE segment from that script (prefer trimming a middle segment, not the opening or closing; do not touch the inflection-tagged segments if possible)
4. Re-run generate-audio-v3.ts (but first `rm public/audio/oracle/devolucao_{trimmed}.mp3` to force regeneration)
5. Re-run validate-timing.ts
6. Repeat until exit 0

Record which script was trimmed (if any) in the Wave 3 summary for Phase 35 to reference.
  </action>
  <verify>
    <automated>npx ts-node scripts/validate-timing.ts</automated>
  </verify>
  <acceptance_criteria>
    - Command `npx ts-node scripts/validate-timing.ts` exits 0
    - Output header shows "PHASE 34 (24 PATHS)"
    - Output shows 24 path blocks processed (20 existing + 4 new)
    - Output shows "STATUS: PASS" and "All 24 paths fall within acceptable range."
    - Output shows max-path ≤ 450.0 seconds (7:30 min)
    - Command `grep -q "hasContraFobico" scripts/validate-timing.ts` exits 0
    - Command `grep -q "hasPortador" scripts/validate-timing.ts` exits 0
    - Command `grep -q "DEVOLUCAO_CONTRA_FOBICO" scripts/validate-timing.ts` exits 0
    - Command `grep -q "DEVOLUCAO_PORTADOR" scripts/validate-timing.ts` exits 0
    - Command `grep -c "^  { name:" scripts/validate-timing.ts` returns exactly 24 (path entries, all on single lines in ALL_PATHS)
    - VALIDATION.md row "34-03-* | timing | npx ts-node scripts/validate-timing.ts" flips to green
    - If trimming was required: src/data/script.ts has one segment removed and the corresponding MP3 has been regenerated (file mtime is newer than Task 1's generation)
  </acceptance_criteria>
  <done>
    validate-timing.ts extended with trigger-aware pickLongestDevolucao, PathConfig fields, 4 new path permutations, and updated console output. Runs exit 0 with max-path ≤ 7:30. All 24 paths within budget.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix fallback-tts SCRIPT count assertion (80 → 82) + update public/roteiro.html with 2 new archetype cards</name>
  <files>src/services/tts/__tests__/fallback-tts.test.ts, public/roteiro.html</files>
  <read_first>
    - src/services/tts/__tests__/fallback-tts.test.ts (lines 134-145 — the assertion to fix)
    - public/roteiro.html (lines 955-1055 — existing devolucao-card markup for the 9 archetypes; use line 989-995 SEEKER card as the cleanest template)
    - public/roteiro.html (lines 957-972 — "Como funciona a classificação" explanation list)
    - .planning/phases/34-detectable-archetypes-contra-fobico-portador/34-CONTEXT.md decisions D-24 (card layout), D-25 (card content), D-29 (assertion update)
    - src/data/script.ts (DEVOLUCAO_CONTRA_FOBICO + DEVOLUCAO_PORTADOR — the source text to excerpt into the cards)
  </read_first>
  <action>
**Part A: Update fallback-tts.test.ts SCRIPT count assertion.**

Open src/services/tts/__tests__/fallback-tts.test.ts. Locate the test named `'should have a URL entry for every key in SCRIPT (80 keys)'` (around line 134-145). Replace that test block with:

```typescript
  it('should have a URL entry for every key in SCRIPT (82 keys)', () => {
    // SCRIPT imported at top of file
    // 61 base keys (v4.0) + 6 Q1B (Phase 31) + 6 Q5B (Phase 32) + 7 Q6B + ESPELHO (Phase 33) + 2 Phase 34 (CONTRA_FOBICO + PORTADOR) = 82
    const scriptKeyCount = Object.keys(SCRIPT).length;
    expect(scriptKeyCount).toBe(82);

    // Each script key should produce a valid URL
    for (const key of Object.keys(SCRIPT)) {
      const expectedUrl = `/audio/prerecorded/${key.toLowerCase()}.mp3`;
      expect(expectedUrl).toMatch(/^\/audio\/prerecorded\/[a-z0-9_]+\.mp3$/);
    }
  });
```

Save. Quick verify:
```
npm test src/services/tts/__tests__/fallback-tts.test.ts -- --run
```
Expect: all 7 tests passing (the SCRIPT count assertion is now correct).

**Part B: Update public/roteiro.html — add 2 new devolução cards + update the priority list.**

Open public/roteiro.html.

**B.1: Update the "Como funciona a classificação" priority list (around lines 960-970).**

Current state shows 9 archetypes in the priority list (ESPELHO + 8 baseline). Update it to reflect the 11 archetypes after Phase 34:

Replace lines 960-970 (`<p>O sistema analisa...</p><ul>...</ul>`) with:
```html
    <p style="margin-bottom:0.5rem;">O sistema analisa o <em>padr&atilde;o total</em> de escolhas (6 a 9, dependendo das ramifica&ccedil;&otilde;es) usando porcentagens. S&atilde;o <strong>11 arqu&eacute;tipos</strong> no total, ordenados por prioridade:</p>
    <ul style="padding-left:1.2rem; font-size:0.85rem; line-height:1.8;">
      <li><strong>ESPELHO SILENCIOSO</strong> &mdash; PRIORIDADE 1: Q6B=B (devolve forma, n&atilde;o conte&uacute;do)</li>
      <li><strong>CONTRA-F&Oacute;BICO</strong> &mdash; PRIORIDADE 2: Q1=B &amp; Q2=B &amp; Q1B=A (atravessou o vazio)</li>
      <li><strong>PORTADOR</strong> &mdash; PRIORIDADE 3: Q4=A &amp; Q5=A &amp; Q5B=A (carrega a pergunta como tesouro)</li>
      <li><strong>MIRROR</strong> &mdash; Altern&acirc;ncia perfeita (A-B-A-B-A-B...)</li>
      <li><strong>DEPTH SEEKER</strong> &mdash; 100% Op&ccedil;&atilde;o A</li>
      <li><strong>SURFACE KEEPER</strong> &mdash; 100% Op&ccedil;&atilde;o B</li>
      <li><strong>PIVOT LATE</strong> &mdash; Primeiros 1/3 &ge;66% A, &uacute;ltimos 1/3 &le;33% A, com &ge;40% B no total</li>
      <li><strong>PIVOT EARLY</strong> &mdash; Primeiros 1/3 &le;33% A, &uacute;ltimos 1/3 &ge;66% A, com &ge;40% A no total</li>
      <li><strong>SEEKER</strong> &mdash; &ge;66% Op&ccedil;&atilde;o A (sem pivot)</li>
      <li><strong>GUARDIAN</strong> &mdash; &ge;66% Op&ccedil;&atilde;o B (sem pivot)</li>
      <li><strong>CONTRADICTED</strong> &mdash; Nenhum padr&atilde;o claro (default)</li>
    </ul>
```

**B.2: Insert 2 new archetype cards in the devolucao-grid.**

Locate the ESPELHO card (lines 975-987, the card with `style="border-left:4px solid #8b7530;"`). Immediately AFTER its closing `</div>` (end of the ESPELHO card block, around line 987) and BEFORE the SEEKER card (`<div class="devolucao-card">` for "O Buscador"), insert these two new cards. They should have distinctive left-border colors to mark them as "Phase 34 / high priority" (mirroring the ESPELHO color convention but with different hues):

```html
    <div class="devolucao-card" style="border-left:4px solid #a85030;">
      <h4>Contra-F&oacute;bico <span style="font-size:0.7rem;color:var(--muted);">(CONTRA_FOBICO)</span></h4>
      <div class="criteria">Q1=B &amp; Q2=B &amp; Q1B=A &mdash; ficou, olhou o que repugna, atravessou o vazio</div>
      <div class="text">
        Voc&ecirc; n&atilde;o fugiu do que arde.
        <br><br>
        Quase ningu&eacute;m fica na sala quando a sa&iacute;da aparece. Quase ningu&eacute;m olha o que repugna. E quase ningu&eacute;m atravessa quando o vazio convida.
        <br><br>
        Isso n&atilde;o &eacute; coragem. Coragem ainda tem um nome &mdash; e o nome serve de muleta. O que voc&ecirc; fez &eacute; mais raro: caminhar em dire&ccedil;&atilde;o ao que amea&ccedil;a, sem precisar chamar de hero&iacute;smo.
        <br><br>
        <em>Eu n&atilde;o vou te dar um nome para isso. Esse gesto &eacute; seu &mdash; e ele j&aacute; te conhece.</em>
      </div>
    </div>

    <div class="devolucao-card" style="border-left:4px solid #6b5a30;">
      <h4>Portador <span style="font-size:0.7rem;color:var(--muted);">(PORTADOR)</span></h4>
      <div class="criteria">Q4=A &amp; Q5=A &amp; Q5B=A &mdash; lembrou de tudo, escolheu carregar, fundiu pergunta e mem&oacute;ria</div>
      <div class="text">
        Voc&ecirc; lembrou de tudo. E mesmo assim escolheu carregar.
        <br><br>
        Tem perguntas que n&atilde;o s&atilde;o problemas. N&atilde;o pedem solu&ccedil;&atilde;o, n&atilde;o esperam al&iacute;vio. Elas s&atilde;o cargas. E quem carrega n&atilde;o est&aacute; preso &mdash; est&aacute; sendo herdeiro de alguma coisa que ainda n&atilde;o tem nome.
        <br><br>
        Guardi&atilde;o segura para que nada entre. Voc&ecirc; segura para que algo continue existindo. &Eacute; outro gesto. A pergunta que pesa em voc&ecirc; n&atilde;o &eacute; um peso a tirar. &Eacute; um tesouro que pede um corpo para continuar fazendo sentido.
        <br><br>
        <em>Eu n&atilde;o vou te dizer o que ela quer dizer. Mas ela est&aacute; bem onde est&aacute; &mdash; e voc&ecirc; tamb&eacute;m.</em>
      </div>
    </div>
```

Note the use of HTML entities (&ccedil;, &eacute;, &atilde;, &mdash;, &amp;) consistent with the rest of roteiro.html. Do not use raw Portuguese accents — the existing file uses entity escaping throughout.

**B.3: Check the Mermaid flowchart section (around lines 420-450).**

Per CONTEXT D-24: "No Mermaid flowchart changes — devoluções are not nodes in the flowchart, only branches are."

Confirm by reading lines 420-450 of roteiro.html: the Mermaid diagram should show branches (Q1B, Q2B, Q4B, Q5B, Q6B) and the ESPELHO devolução (because ESPELHO was added as a node in Phase 33, apparently). Do NOT add CONTRA_FOBICO or PORTADOR as flowchart nodes — they are regular devoluções like SEEKER, which also do not appear in the flowchart. Leave this section alone.

Save public/roteiro.html. Open in a browser (optional visual spot-check):
```
start public/roteiro.html
```
Scroll to the devoluções section. Verify the 2 new cards appear between ESPELHO and SEEKER and match the card layout (title, criteria, text). Verify the priority list shows 11 archetypes.

Per 34-VALIDATION.md §Manual-Only Verifications, a formal visual QA of roteiro.html card consistency is deferred to Phase 35 POL-03. This task just ensures the content is present and the HTML is well-formed.
  </action>
  <verify>
    <automated>npm test src/services/tts/__tests__/fallback-tts.test.ts -- --run</automated>
  </verify>
  <acceptance_criteria>
    - Command `npm test src/services/tts/__tests__/fallback-tts.test.ts -- --run` exits 0
    - fallback-tts.test.ts SCRIPT key count assertion now says `expect(scriptKeyCount).toBe(82)`
    - Command `grep -q "toBe(82)" src/services/tts/__tests__/fallback-tts.test.ts` exits 0
    - Command `grep -q "CONTRA_FOBICO" public/roteiro.html` exits 0
    - Command `grep -q "PORTADOR" public/roteiro.html` exits 0
    - Command `grep -q "Contra-F" public/roteiro.html` exits 0 (the human-readable card title "Contra-Fóbico" via entity)
    - Command `grep -q "11 arqu" public/roteiro.html` exits 0 (the "11 arquétipos" text in the priority list)
    - Command `grep -c "devolucao-card" public/roteiro.html` returns at least 11 (was 9: ESPELHO + 8 baseline; now 11)
    - The Mermaid flowchart section (lines 420-450) is UNCHANGED — verify by checking that ESPELHO is still the only devolução node in the diagram
    - VALIDATION.md row "34-03-* | SCRIPT count update | npm test ... fallback-tts" flips to green
  </acceptance_criteria>
  <done>
    fallback-tts SCRIPT count assertion updated to 82 and passing. public/roteiro.html has 2 new devolução cards (CONTRA_FOBICO + PORTADOR) and an updated priority list showing 11 archetypes. Mermaid flowchart unchanged per D-24.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Final regression run — confirm Phase 34 closes cleanly with no new failures</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - .planning/phases/34-detectable-archetypes-contra-fobico-portador/34-VALIDATION.md §"Validation Sign-Off"
    - .planning/phases/33-q6b-espelho-silencioso/33-VERIFICATION.md (Phase 33 final test baseline for comparison)
  </read_first>
  <action>
Run the full regression suite to confirm Phase 34 closes cleanly.

Command:
```
npm test -- --run 2>&1 | tail -80
```

Expected baseline after Wave 3:
- Test Files: 41 passed, 2 failed (43 total) — matches Phase 33 baseline
- Tests: ≥712 passing (697 pre-34 + ~14 new from Wave 2 + 0 new from Wave 3; the fallback-tts assertion is fixed which nets ZERO change in failed count)
- Failed: 16 (matches Phase 33 baseline: 15 voice-flow-integration + ~1-2 ambient-player — the fallback-tts assertion that failed in Wave 1 + 2 is now green)
- Skipped: 3 (unchanged)

**Critical check:** the failed count dropped from 17 (after Wave 1 + 2) back down to 16 (Phase 33 baseline). This is the signal that Wave 3 successfully closed the transient test regression introduced by Wave 1.

Also run the timing validator one more time to confirm nothing Wave 3 did regressed timing:
```
npx ts-node scripts/validate-timing.ts
```
Exit 0, 24 paths all pass.

Also spot-check by running the full quick command from VALIDATION.md:
```
npm test src/machines/guards/__tests__/patternMatching.test.ts src/machines/oracleMachine.test.ts src/components/experience/__tests__/OracleExperience-helpers.test.ts -- --run
```
Should exit 0 with all new Phase 34 tests green.

If any test file regressed compared to Phase 33 baseline:
1. Identify which test and which suite
2. Trace back to the Wave 3 change that caused it:
   - SCRIPT count assertion expecting 80 (but script already says 82) → Task 3 Part A edit was incomplete or wrong
   - validate-timing.ts throwing → Task 2 path entry malformed
   - OracleExperience test failing → Wave 3 shouldn't have touched OracleExperience.tsx; check for accidental edits
3. Fix, re-run, do not commit until clean
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -80</automated>
  </verify>
  <acceptance_criteria>
    - Command `npm test -- --run` exits with code 1 (16 known failures remain — 15 voice-flow-integration + 1-2 ambient-player — matching Phase 33 baseline exactly)
    - Output line "Tests" shows at least 712 passed
    - Output line "Tests" shows exactly 16 failed (NOT 17 — the fallback-tts assertion is now fixed)
    - Output line "Tests" shows 3 skipped
    - NO new failures compared to Phase 33 baseline: the ONLY failing test files should be voice-flow-integration.test.ts and ambient-player.test.ts — exactly matching the documented known-failure set from CLAUDE.md and 34-RESEARCH.md §"Test Baseline"
    - Command `npx ts-node scripts/validate-timing.ts` exits 0
    - Command `npm test src/machines/guards/__tests__/patternMatching.test.ts src/machines/oracleMachine.test.ts src/components/experience/__tests__/OracleExperience-helpers.test.ts -- --run` exits 0
    - VALIDATION.md row "34-03-* | full regression" flips to green
    - All VALIDATION.md per-task verification map rows for Phase 34 are now green (or skipped where explicitly noted as manual-only)
  </acceptance_criteria>
  <done>
    Full regression suite matches Phase 33 baseline + 14 new passing Phase 34 tests. Zero new failures. Timing validator exits 0. All Phase 34 must-haves satisfied at runtime level. Phase 34 is ready for /gsd:verify-work.
  </done>
</task>

</tasks>

<verification>
After all 4 tasks complete:

1. MP3s exist: `test -f public/audio/oracle/devolucao_contra_fobico.mp3 && test -f public/audio/oracle/devolucao_portador.mp3` exits 0
2. MP3 sizes: both files between 100 KB and 2 MB
3. Validator: `npx ts-node scripts/validate-timing.ts` exits 0 with max-path ≤ 450s and 24 paths processed
4. fallback-tts: SCRIPT count assertion is 82 and test passes
5. Roteiro: `grep -c "devolucao-card" public/roteiro.html` ≥ 11
6. Full suite: ≥712 passing / 16 failing / 3 skipped — no NEW failures compared to Phase 33 baseline
7. Quick command from VALIDATION.md exits 0

Commit:
```
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "feat(34-03): generate new MP3s, extend timing validator, sync roteiro.html, fix SCRIPT count" --files public/audio/oracle/devolucao_contra_fobico.mp3 public/audio/oracle/devolucao_portador.mp3 scripts/validate-timing.ts public/roteiro.html src/services/tts/__tests__/fallback-tts.test.ts
```

If the script trimmed segments in Task 2 to stay under budget, also include `src/data/script.ts` in the commit --files list and mention the trim in the commit message body.
</verification>

<success_criteria>
- public/audio/oracle/devolucao_contra_fobico.mp3 exists, non-empty, generated by ElevenLabs v3 with voice ID PznTnBc8X6pvixs9UkQm
- public/audio/oracle/devolucao_portador.mp3 exists, non-empty, same voice/model
- scripts/validate-timing.ts has trigger-aware pickLongestDevolucao supporting 10 candidates (ESPELHO + CONTRA_FOBICO + PORTADOR + 8 baseline)
- scripts/validate-timing.ts has 24 path permutations (20 existing + 4 new)
- scripts/validate-timing.ts exits 0 with max-path ≤ 450 seconds
- src/services/tts/__tests__/fallback-tts.test.ts SCRIPT count assertion is `expect(scriptKeyCount).toBe(82)` and passes
- public/roteiro.html has 2 new devolucao-card entries for CONTRA_FOBICO + PORTADOR (title + criteria + text)
- public/roteiro.html priority list shows 11 archetypes (was 9 — +CONTRA_FOBICO +PORTADOR)
- public/roteiro.html Mermaid flowchart is unchanged (devoluções are not flowchart nodes per D-24)
- Full regression suite: ≥712 passing / 16 failing / 3 skipped — matching the Phase 33 baseline exactly (no NEW failures)
- Phase 34 is complete: AR-02 and AR-03 fully satisfied at code + audio + docs levels
- Ready for /gsd:verify-work to close Phase 34
</success_criteria>

<output>
After completion, create `.planning/phases/34-detectable-archetypes-contra-fobico-portador/34-03-SUMMARY.md` documenting:
- MP3 file sizes (devolucao_contra_fobico.mp3, devolucao_portador.mp3)
- Validator final max-path after Wave 3 (and whether any trimming was needed in Task 2)
- Final test count (passing / failing / skipped) matching Phase 33 baseline + 14 new Phase 34 tests
- Roteiro.html changes (2 new cards + priority list update)
- Phase 34 closing status: AR-02 + AR-03 satisfied, ready for /gsd:verify-work
- Hand-off to Phase 35: POL-01 full 96-path audit + POL-03 comprehensive roteiro coherence + UAT-01 browser testing
</output>
