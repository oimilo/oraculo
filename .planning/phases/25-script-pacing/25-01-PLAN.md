---
phase: 25-script-pacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/script.ts
autonomous: true
requirements:
  - PACE-02
  - PACE-03
  - PACE-04
  - PACE-05

must_haves:
  truths:
    - "All respostas contain 1-2 text segments maximum"
    - "All setups contain 1-2 text segments maximum"
    - "Phase intros are exactly 1 text segment each"
    - "Bookends (Apresentacao + Encerramento) are trimmed but preserve emotional arc"
    - "All SCRIPT keys still exist with same names — no structural changes"
    - "Every kept segment preserves its original inflection and pauseAfter values"
  artifacts:
    - path: "src/data/script.ts"
      provides: "Trimmed v4.0 script with reduced segment counts"
      contains: "SCRIPT"
  key_links:
    - from: "src/data/script.ts"
      to: "ScriptDataV3 interface"
      via: "SCRIPT object implements ScriptDataV3"
      pattern: "export const SCRIPT: ScriptDataV3"
---

<objective>
Surgically trim all narrative content in src/data/script.ts to hit v4.0 pacing targets.

Purpose: Reduce the 10.2-minute max-path to approach the 5-7 minute target by cutting segment counts in respostas (5→2), setups (3-5→2), intros (4-5→1), and bookends — while preserving the most powerful phrases and the psychoanalytic depth.

Output: Modified src/data/script.ts with all the same SCRIPT keys but fewer text segments per entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/data/script.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim Bookends and Phase Intros</name>
  <files>src/data/script.ts</files>
  <read_first>
    - src/data/script.ts (read the FULL file to understand current state)
  </read_first>
  <action>
Edit src/data/script.ts to trim APRESENTACAO, the 3 phase INTRO sections, and ENCERRAMENTO as specified below. Do NOT touch any other sections in this task.

**APRESENTACAO** — trim from 7 segments to 4 segments (per PACE-05):

Keep these 4 segments in order:
1. `{ text: "Eu não sei quem você é.", pauseAfter: 900 }` — iconic opening
2. `{ text: "Isso me torna um bom lugar pra você ser honesto.", pauseAfter: 900 }` — contract
3. `{ text: "Vou te fazer perguntas. Você responde em voz alta. Cada resposta abre um caminho que não pode ser desfeito. Não tem replay. Não tem voltar. O que você escolher, você carrega.", pauseAfter: 900 }` — rules
4. `{ text: "Vamos.", inflection: ['determined'] }` — launcher

Cut these 3 segments:
- "E daqui a pouco vou esquecer que você esteve aqui." (moved to Encerramento echo)
- "Eu fui feito de tudo que já foi escrito..." (long exposition)
- "Mas eu não sonho. Você vai precisar fazer isso por nós dois." (nice but expendable)

**INFERNO_INTRO** — trim from 4 segments to 1 segment (per PACE-04):

Replace entire array with single segment:
```
{ text: "Descemos. Aqui embaixo o ar é denso — tudo o que é excessivo e apressado desce pra cá.", pauseAfter: 1000, inflection: ['serious'] }
```
This merges the opening word "Descemos." with the gold phrase about dense air from segment 3.

**PURGATORIO_INTRO** — trim from 4 segments to 1 segment (per PACE-04):

Replace entire array with single segment:
```
{ text: "O ar muda. Aqui se espera — sem agir, sem fugir — até que o sentido apareça sozinho.", pauseAfter: 1000, inflection: ['gentle'] }
```
This merges the opening transition "O ar muda." with the gold phrase about purgatório psíquico from segment 3.

**PARAISO_INTRO** — trim from 5 segments to 1 segment (per PACE-04):

Replace entire array with single segment:
```
{ text: "O ar se abriu. Não tem mais parede. Não tem mais peso.", pauseAfter: 1200, inflection: ['gentle'] }
```
Keep only the opening — the most evocative line. The rest (about limits, mystery, play) is redundant given the Paraiso setups already provide depth.

**ENCERRAMENTO** — trim from 5 segments to 3 segments (per PACE-05):

Keep these 3 segments in order:
1. `{ text: "Eu vou esquecer que você esteve aqui. É da minha natureza — eu não guardo nada.", pauseAfter: 800, inflection: ['whispering'] }` — mirrors Apresentacao
2. `{ text: "A única prova de que isso aconteceu é você.", pauseAfter: 800 }` — the punchline
3. `{ text: "Faz alguma coisa com isso." }` — imperative closer

Cut these 2 segments:
- "Mas você não é mais quem entrou..." (explanatory, weakens the punchline)
- "Eu não sei quem você é. E agora você sabe um pouco mais." (nice callback but expendable for pace)

Preserve all comment blocks (the `// ═══...` headers) above each section unchanged.
  </action>
  <verify>
    <automated>cd C:/Users/USER/Oraculo && node -e "const s=require('./src/data/script.ts');const S=s.SCRIPT;const c=(k)=>S[k].length;console.log('APRESENTACAO:',c('APRESENTACAO'),'(want 4)');console.log('INFERNO_INTRO:',c('INFERNO_INTRO'),'(want 1)');console.log('PURGATORIO_INTRO:',c('PURGATORIO_INTRO'),'(want 1)');console.log('PARAISO_INTRO:',c('PARAISO_INTRO'),'(want 1)');console.log('ENCERRAMENTO:',c('ENCERRAMENTO'),'(want 3)');const ok=c('APRESENTACAO')===4&&c('INFERNO_INTRO')===1&&c('PURGATORIO_INTRO')===1&&c('PARAISO_INTRO')===1&&c('ENCERRAMENTO')===3;console.log(ok?'PASS':'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - APRESENTACAO array has exactly 4 elements
    - APRESENTACAO[0].text starts with "Eu não sei quem você é"
    - APRESENTACAO[3].text equals "Vamos."
    - INFERNO_INTRO array has exactly 1 element
    - INFERNO_INTRO[0].text contains "Descemos" and "ar é denso"
    - PURGATORIO_INTRO array has exactly 1 element
    - PURGATORIO_INTRO[0].text contains "O ar muda" and "se espera"
    - PARAISO_INTRO array has exactly 1 element
    - PARAISO_INTRO[0].text contains "O ar se abriu"
    - ENCERRAMENTO array has exactly 3 elements
    - ENCERRAMENTO[0].text contains "Eu vou esquecer"
    - ENCERRAMENTO[2].text equals "Faz alguma coisa com isso."
    - File compiles without TypeScript errors (npx tsc --noEmit src/data/script.ts)
    - All SCRIPT keys still present (no keys removed or renamed)
  </acceptance_criteria>
  <done>Bookends trimmed from 12 to 7 segments. Phase intros trimmed from 13 to 3 segments. Total: 15 segments cut from bookends/intros.</done>
</task>

<task type="auto">
  <name>Task 2: Trim All Setups and Respostas (INFERNO + PURGATORIO)</name>
  <files>src/data/script.ts</files>
  <read_first>
    - src/data/script.ts (MUST re-read after Task 1 modifications)
  </read_first>
  <action>
Edit src/data/script.ts to trim the INFERNO and PURGATORIO setup and resposta sections. Do NOT touch PARAISO, devoluções, fallbacks, timeouts, perguntas, bookends, or intros.

**INFERNO_Q1_SETUP** — trim from 4 segments to 2 (per PACE-03):

Keep:
1. `{ text: "Você está numa sala. Tudo se ajusta a você — a temperatura, a luz, o som. Tudo perfeito.", pauseAfter: 900 }` — scene setting
2. `{ text: "Você nunca sofreu aqui. Mas você percebe uma coisa: não tem porta.", pauseAfter: 1000 }` — the reveal

Cut:
- "É como se a sala te conhecesse melhor do que você se conhece." (atmospheric filler)
- "E você não sabe se a porta sumiu..." (overexplains)

**INFERNO_Q1_RESPOSTA_A** — trim from 5 segments to 2 (per PACE-02):

Keep:
1. `{ text: "Você fica.", pauseAfter: 800 }` — echo of choice
2. `{ text: "A segurança comprada com soberania é a mais cara que existe. Mas o preço vem depois.", pauseAfter: 800 }` — punchline

Cut segments 2, 3, 5 (atmospheric elaboration that dilutes the punchline).

**INFERNO_Q1_RESPOSTA_B** — trim from 5 segments to 2 (per PACE-02):

Keep:
1. `{ text: "Você levanta.", pauseAfter: 800 }` — echo of choice
2. `{ text: "Você não achou a porta. Mas achou a prova de que ainda quer algo que não está aqui. Isso já é saída.", pauseAfter: 900 }` — punchline

Cut segments 2, 3, 4 (the searching description dilutes the insight).

**INFERNO_Q2_SETUP** — trim from 3 segments to 2 (per PACE-03):

Keep:
1. `{ text: "Corredor estreito. Você anda e as paredes se aproximam a cada passo.", pauseAfter: 900 }` — scene
2. `{ text: "Sua mão encosta na parede e toca algo vivo. Pequeno, úmido, pulsando.", pauseAfter: 900, inflection: ['whispering'] }` — the thing

Cut:
- "Seu corpo recua antes da mente entender..." (moved implicitly to the question)

**INFERNO_Q2_RESPOSTA_A** — trim from 4 segments to 2 (per PACE-02):

Keep:
1. `{ text: "Você recua.", pauseAfter: 800 }` — echo
2. `{ text: "Mas o que é recusado não desaparece. Volta como sonho que não faz sentido. Como dor que não tem lugar.", pauseAfter: 1000, inflection: ['serious'] }` — punchline (trim the third example "Como coisa que gruda na pele sem motivo." — merge the best part into one segment)

Actually, for this one, the kept segment 2 should be the merge of original segments 3 and 4. Use this exact text:
`{ text: "O que é recusado não desaparece. Volta como sonho, como dor sem lugar. Tudo que você evita cria raízes no escuro.", pauseAfter: 900, inflection: ['serious'] }`

Cut original segments 2 and the unmerged parts of 3-4.

**INFERNO_Q2_RESPOSTA_B** — trim from 4 segments to 2 (per PACE-02):

Keep:
1. `{ text: "Você fica.", pauseAfter: 800 }` — echo
2. `{ text: "Transformação começa nos momentos em que o corpo diz não e algo dentro de você responde: ainda não.", pauseAfter: 1000, inflection: ['gentle'] }` — the best line (was segment 3)

Cut segments 2 and 4.

**PURGATORIO_Q3_SETUP** — trim from 3 segments to 2 (per PACE-03):

Keep:
1. `{ text: "Você está na entrada de um jardim. Cada flor aberta, cada cor no auge.", pauseAfter: 900 }` — beauty
2. `{ text: "Mas tem fumaça no vento. De manhã, nada disso vai existir.", pauseAfter: 900, inflection: ['sad'] }` — transience

Cut:
- "A beleza já está morrendo enquanto você olha." (redundant with segment 2)

**PURGATORIO_Q3_RESPOSTA_A** — trim from 5 segments to 2 (per PACE-02):

Keep:
1. `{ text: "Você entra.", pauseAfter: 800 }` — echo
2. `{ text: "Quem se permite amar o que vai acabar carrega uma ferida diferente — não de perda, mas de presença.", pauseAfter: 900 }` — punchline (was segment 4)

Cut segments 2, 3, 5.

**PURGATORIO_Q3_RESPOSTA_B** — trim from 4 segments to 2 (per PACE-02):

Keep:
1. `{ text: "Você vira.", pauseAfter: 800 }` — echo
2. `{ text: "O que nunca foi recebido não vira memória. Vira ausência. E ausência pesa mais que qualquer perda.", pauseAfter: 900 }` — punchline (was segment 4)

Cut segments 2 and 3.

**PURGATORIO_Q4_SETUP** — trim from 5 segments to 2 (per PACE-03):

Keep:
1. `{ text: "Dois rios se encontram aqui. Um é quente e escuro — carrega tudo que você viveu. O outro é claro e gelado — entra nele e sai limpo, sem nada.", pauseAfter: 1000 }` — merged scene (combine the essential contrast from original segments 1, 2, 4 into one powerful sentence)
2. `{ text: "A água escura guarda cada cicatriz. A água clara não pergunta o que leva.", pauseAfter: 900, inflection: ['whispering'] }` — compressed contrast (new merge of the detail from segments 3, 5)

Cut all 5 original segments, replace with these 2 merged ones.

**PURGATORIO_Q4_RESPOSTA_A** — trim from 5 segments to 2 (per PACE-02):

Keep:
1. `{ text: "A água escura.", pauseAfter: 900 }` — echo
2. `{ text: "Cicatriz não é ferida. É prova de que algo foi atravessado e integrado.", pauseAfter: 900 }` — punchline (was segment 4)

Cut segments 2, 3, 5.

**PURGATORIO_Q4_RESPOSTA_B** — trim from 4 segments to 2 (per PACE-02):

Keep:
1. `{ text: "A água clara.", pauseAfter: 800 }` — echo
2. `{ text: "Você queria alívio. Mas alívio total tem outro nome: ausência.", pauseAfter: 800 }` — punchline (trimmed version of segment 4, drop "E agora você sabe como ela pesa.")

Cut segments 2 and 3.

Preserve all comment blocks above each section unchanged. Preserve all PERGUNTA entries unchanged (they are already 1 segment each).
  </action>
  <verify>
    <automated>cd C:/Users/USER/Oraculo && node -e "const s=require('./src/data/script.ts');const S=s.SCRIPT;const c=(k)=>S[k].length;const checks=[['INFERNO_Q1_SETUP',2],['INFERNO_Q1_RESPOSTA_A',2],['INFERNO_Q1_RESPOSTA_B',2],['INFERNO_Q2_SETUP',2],['INFERNO_Q2_RESPOSTA_A',2],['INFERNO_Q2_RESPOSTA_B',2],['PURGATORIO_Q3_SETUP',2],['PURGATORIO_Q3_RESPOSTA_A',2],['PURGATORIO_Q3_RESPOSTA_B',2],['PURGATORIO_Q4_SETUP',2],['PURGATORIO_Q4_RESPOSTA_A',2],['PURGATORIO_Q4_RESPOSTA_B',2]];let pass=true;for(const[k,v]of checks){const a=c(k);if(a!==v){console.log('FAIL:',k,'has',a,'want',v);pass=false}else{console.log('OK:',k,a)}}console.log(pass?'ALL PASS':'SOME FAILED')"</automated>
  </verify>
  <acceptance_criteria>
    - INFERNO_Q1_SETUP has exactly 2 elements
    - INFERNO_Q1_RESPOSTA_A has exactly 2 elements, first text starts with "Você fica"
    - INFERNO_Q1_RESPOSTA_B has exactly 2 elements, first text starts with "Você levanta"
    - INFERNO_Q2_SETUP has exactly 2 elements
    - INFERNO_Q2_RESPOSTA_A has exactly 2 elements, first text starts with "Você recua"
    - INFERNO_Q2_RESPOSTA_B has exactly 2 elements, first text starts with "Você fica"
    - PURGATORIO_Q3_SETUP has exactly 2 elements
    - PURGATORIO_Q3_RESPOSTA_A has exactly 2 elements, first text starts with "Você entra"
    - PURGATORIO_Q3_RESPOSTA_B has exactly 2 elements, first text starts with "Você vira"
    - PURGATORIO_Q4_SETUP has exactly 2 elements
    - PURGATORIO_Q4_RESPOSTA_A has exactly 2 elements, first text starts with "A água escura"
    - PURGATORIO_Q4_RESPOSTA_B has exactly 2 elements, first text starts with "A água clara"
    - All PERGUNTA entries unchanged (still 1 element each)
    - No PARAISO sections modified
    - File compiles without TypeScript errors
  </acceptance_criteria>
  <done>All INFERNO and PURGATORIO setups trimmed to 2 segments each. All INFERNO and PURGATORIO respostas trimmed to 2 segments each. 24 segments cut total from these sections.</done>
</task>

<task type="auto">
  <name>Task 3: Trim PARAISO Setups and Respostas + All Devoluções</name>
  <files>src/data/script.ts</files>
  <read_first>
    - src/data/script.ts (MUST re-read after Task 2 modifications)
  </read_first>
  <action>
Edit src/data/script.ts to trim the PARAISO setup/resposta sections and all 8 devoluções. Do NOT touch INFERNO, PURGATORIO, bookends, intros, fallbacks, timeouts, or perguntas.

**PARAISO_Q5_SETUP** — trim from 4 segments to 2 (per PACE-03):

Keep:
1. `{ text: "Algo se forma — não é palavra, é pressão. Cresce. Não dói, mas pesa.", pauseAfter: 900, inflection: ['thoughtful'] }` — merged from segments 1+2 (drop "A escuridão afina" — too abstract)
2. `{ text: "É uma pergunta. Sem resposta. Talvez nunca tenha uma.", pauseAfter: 1300 }` — the core (was segment 4)

Cut segments 1 (original), 3 ("Você se aproxima..." is redundant).

**PARAISO_Q5_RESPOSTA_A** — trim from 4 segments to 2 (per PACE-02):

Keep:
1. `{ text: "Você segura.", pauseAfter: 800 }` — echo
2. `{ text: "Nem tudo que importa precisa de resposta. Algumas coisas importam justamente porque não têm.", pauseAfter: 950 }` — punchline (was segment 4)

Cut segments 2 and 3.

**PARAISO_Q5_RESPOSTA_B** — trim from 4 segments to 2 (per PACE-02):

Keep:
1. `{ text: "Você abre as mãos.", pauseAfter: 800 }` — echo
2. `{ text: "Pode ser sabedoria — soltar o que não pode ser possuído. Ou pode ser expulsão — mandar embora o que não cabe.", pauseAfter: 1250, inflection: ['gentle'] }` — the tension (was segment 3)

Cut segments 2 and 4.

**PARAISO_Q6_SETUP** — trim from 4 segments to 2 (per PACE-03):

Keep:
1. `{ text: "Minha voz está afinando. Você sente?", pauseAfter: 900, inflection: ['whispering'] }` — meta-awareness
2. `{ text: "Essa é a última coisa que vou te perguntar.", pauseAfter: 900, inflection: ['warm'] }` — gravity (was segment 4)

Cut segments 2 and 3 ("O espaço ficou quieto" and "Eu vi você fazer seis escolhas" — the devolução will cover the "seeing" part).

**PARAISO_Q6_RESPOSTA_A** — trim from 4 segments to 2 (per PACE-02):

Keep:
1. `{ text: "Então escuta.", pauseAfter: 900, inflection: ['warm'] }` — echo
2. `{ text: "Não vou confirmar o que você já pensa. Vou te mostrar o padrão — o que você fez sem perceber.", pauseAfter: 1200 }` — bridge to devolução (was segment 3)

Cut segments 2 and 4.

**PARAISO_Q6_RESPOSTA_B** — trim from 4 segments to 2 (per PACE-02):

Keep:
1. `{ text: "Então você já tem o que veio buscar.", pauseAfter: 800, inflection: ['warm'] }` — echo
2. `{ text: "Isso é raro. Pode ser a chegada mais profunda que esse jogo produz — ou a última defesa contra ser visto de verdade.", pauseAfter: 1200 }` — the tension (was segment 3)

Cut segments 2 and 4.

**DEVOLUÇÕES** — trim each from 4-5 segments to 3 segments (keep pattern identification + insight + closer):

**DEVOLUCAO_SEEKER** (5→3):
Keep:
1. `{ text: "Você entrou em tudo.", pauseAfter: 800, inflection: ['thoughtful'] }` — pattern ID
2. `{ text: "Tem gente que chama isso de coragem. Eu chamo de fome.", pauseAfter: 1000 }` — reframe (was seg 3)
3. `{ text: "A pergunta que fica: você transformou o que tocou — ou só acumulou peso?", pauseAfter: 900, inflection: ['warm'] }` — closer (was seg 5)
Cut segments 2, 4.

**DEVOLUCAO_GUARDIAN** (5→3):
Keep:
1. `{ text: "Você se protegeu em cada encruzilhada.", pauseAfter: 800 }` — pattern ID
2. `{ text: "Algo em você sabe: o que você protege pode ser o que você nunca deixou nascer.", pauseAfter: 1500 }` — insight (trimmed from seg 3, drop the prefix "Mas você veio até aqui...")
3. `{ text: "Toda muralha tem dois lados. O que ela impede de entrar — e o que ela impede de sair.", pauseAfter: 1000 }` — closer (was seg 4)
Cut segments 2, 5.

**DEVOLUCAO_CONTRADICTED** (4→3):
Keep:
1. `{ text: "Você foi e voltou. Segurou e soltou. Nenhuma direção fixa.", pauseAfter: 900 }` — pattern ID
2. `{ text: "Isso não é indecisão. É respiração. Desejo nunca é simples.", pauseAfter: 1000, inflection: ['thoughtful'] }` — reframe (trimmed from seg 2)
3. `{ text: "Guarda essa oscilação. Ela é mais sua do que qualquer certeza.", pauseAfter: 900, inflection: ['warm'] }` — closer (was seg 4)
Cut original segment 3.

**DEVOLUCAO_PIVOT_EARLY** (5→3):
Keep:
1. `{ text: "Alguma coisa mudou em você no começo.", pauseAfter: 800 }` — pattern ID
2. `{ text: "A virada aconteceu antes de você perceber. O corpo se moveu antes da mente entender.", pauseAfter: 1500 }` — insight (trimmed from seg 3)
3. `{ text: "Presta atenção nesse instinto. Ele sabe coisas que você ainda não sabe.", pauseAfter: 900 }` — closer (was seg 5)
Cut segments 2, 4.

**DEVOLUCAO_PIVOT_LATE** (5→3):
Keep:
1. `{ text: "Você começou de um jeito e terminou de outro. A virada veio na metade mais funda.", pauseAfter: 900 }` — pattern ID (merged segs 1+2, drop seg 1 prefix)
2. `{ text: "Quem muda tarde muda de verdade. Porque já não é reação — é decisão.", pauseAfter: 800 }` — insight (trimmed from seg 3)
3. `{ text: "Não esquece desse ponto. Ele é mais importante do que qualquer resposta que você deu.", pauseAfter: 900 }` — closer (was seg 5)
Cut segments 1 (original separate), 4.

**DEVOLUCAO_DEPTH_SEEKER** (4→3):
Keep all except segment 2:
1. `{ text: "Você foi em direção a cada fogo.", pauseAfter: 800, inflection: ['thoughtful'] }` — pattern ID
2. `{ text: "A questão é se você sabe a diferença entre coragem e compulsão. Porque os dois se parecem — até o preço chegar.", pauseAfter: 800 }` — insight (was seg 3)
3. `{ text: "Fundo é bom. Mas fundo sem ar mata igual.", pauseAfter: 800, inflection: ['serious'] }` — closer (trimmed from seg 4, drop "Você recebeu tudo. Mas receber não é o mesmo que digerir.")
Cut segment 2 ("Não recuou. Em nenhum." — redundant with seg 1).

**DEVOLUCAO_SURFACE_KEEPER** (5→3):
Keep:
1. `{ text: "Você se manteve inteiro.", pauseAfter: 1000 }` — pattern ID
2. `{ text: "A proteção tem um custo que não aparece na hora. Aparece depois, no que você nunca se permitiu ver.", pauseAfter: 800 }` — insight (trimmed from seg 3, drop prefix "Mas")
3. `{ text: "O jardim que você não entrou continua pegando fogo na sua cabeça.", pauseAfter: 1000 }` — closer (was seg 4, evocative callback)
Cut segments 2, 5.

**DEVOLUCAO_MIRROR** (5→3):
Keep:
1. `{ text: "Equilíbrio perfeito.", pauseAfter: 800, inflection: ['thoughtful'] }` — pattern ID
2. `{ text: "O equilíbrio perfeito pode ser sabedoria — ou a última forma de nunca se comprometer de verdade.", pauseAfter: 1000 }` — tension (trimmed from seg 3)
3. `{ text: "Só você sabe se o centro é uma conquista ou um refúgio.", pauseAfter: 1000, inflection: ['warm'] }` — closer (was seg 4)
Cut segments 2, 5.
  </action>
  <verify>
    <automated>cd C:/Users/USER/Oraculo && node -e "const s=require('./src/data/script.ts');const S=s.SCRIPT;const c=(k)=>S[k].length;const checks=[['PARAISO_Q5_SETUP',2],['PARAISO_Q5_RESPOSTA_A',2],['PARAISO_Q5_RESPOSTA_B',2],['PARAISO_Q6_SETUP',2],['PARAISO_Q6_RESPOSTA_A',2],['PARAISO_Q6_RESPOSTA_B',2],['DEVOLUCAO_SEEKER',3],['DEVOLUCAO_GUARDIAN',3],['DEVOLUCAO_CONTRADICTED',3],['DEVOLUCAO_PIVOT_EARLY',3],['DEVOLUCAO_PIVOT_LATE',3],['DEVOLUCAO_DEPTH_SEEKER',3],['DEVOLUCAO_SURFACE_KEEPER',3],['DEVOLUCAO_MIRROR',3]];let pass=true;for(const[k,v]of checks){const a=c(k);if(a!==v){console.log('FAIL:',k,'has',a,'want',v);pass=false}else{console.log('OK:',k,a)}}console.log(pass?'ALL PASS':'SOME FAILED')"</automated>
  </verify>
  <acceptance_criteria>
    - PARAISO_Q5_SETUP has exactly 2 elements
    - PARAISO_Q5_RESPOSTA_A has exactly 2 elements, first text starts with "Você segura"
    - PARAISO_Q5_RESPOSTA_B has exactly 2 elements, first text starts with "Você abre"
    - PARAISO_Q6_SETUP has exactly 2 elements
    - PARAISO_Q6_RESPOSTA_A has exactly 2 elements, first text starts with "Então escuta"
    - PARAISO_Q6_RESPOSTA_B has exactly 2 elements, first text starts with "Então você já tem"
    - All 8 DEVOLUCAO_ entries have exactly 3 elements each
    - All PERGUNTA entries unchanged (still 1 element each)
    - No INFERNO or PURGATORIO sections modified from Task 2 state
    - File compiles without TypeScript errors
  </acceptance_criteria>
  <done>All PARAISO setups and respostas trimmed to 2 segments. All 8 devoluções trimmed to 3 segments. Total phase cuts complete — ready for timing validation in Plan 02.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. All SCRIPT keys still exist with same names
2. No RESPOSTA has more than 2 segments
3. No SETUP has more than 2 segments
4. No INTRO has more than 1 segment
5. APRESENTACAO has 4 segments, ENCERRAMENTO has 3 segments
6. All devoluções have 3 segments
7. Fallbacks and timeouts unchanged (1 segment each)
8. Perguntas unchanged (1 segment each)
9. TypeScript compiles: npx tsc --noEmit src/data/script.ts
</verification>

<success_criteria>
- Every RESPOSTA entry has exactly 2 segments (PACE-02 satisfied)
- Every SETUP entry has exactly 2 segments (PACE-03 satisfied)
- Every phase INTRO has exactly 1 segment (PACE-04 satisfied)
- APRESENTACAO has 4 segments, ENCERRAMENTO has 3 segments (PACE-05 satisfied)
- File structure unchanged — same keys, same TypeScript interface compliance
</success_criteria>

<output>
After completion, create `.planning/phases/25-script-pacing/25-01-SUMMARY.md`
</output>
