---
phase: 28-audio-regeneration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/audio/prerecorded/*.mp3
autonomous: false
requirements:
  - AUDI-01
  - AUDI-02

user_setup:
  - service: elevenlabs
    why: "MP3 generation via ElevenLabs TTS API"
    env_vars:
      - name: ELEVENLABS_API_KEY
        source: "ElevenLabs Dashboard -> Profile -> API Keys"
      - name: ELEVENLABS_VOICE_ID
        source: "Already set: PznTnBc8X6pvixs9UkQm"

must_haves:
  truths:
    - "All 61 script keys have corresponding MP3 files in public/audio/prerecorded/"
    - "All MP3s contain audio matching the TRIMMED v4 script text (not old v3.1 text)"
    - "New Q2B/Q4B branch keys have MP3s generated with correct phase voice settings"
    - "FallbackTTS PRERECORDED_URLS dynamically covers all 61 keys without code changes"
    - "Total audio payload is reasonable for browser loading (<50MB)"
  artifacts:
    - path: "public/audio/prerecorded/"
      provides: "61 MP3 files for all script keys"
      contains: "61 .mp3 files"
    - path: "src/services/tts/fallback.ts"
      provides: "Dynamic PRERECORDED_URLS derivation"
      contains: "Object.keys(SCRIPT).map"
  key_links:
    - from: "scripts/generate-audio-v3.ts"
      to: "src/data/script.ts"
      via: "import { SCRIPT } from '@/data/script'"
      pattern: "import.*SCRIPT.*from.*script"
    - from: "src/services/tts/fallback.ts"
      to: "src/data/script.ts"
      via: "import { SCRIPT } from '@/data/script'"
      pattern: "Object\\.keys\\(SCRIPT\\)"
    - from: "src/services/tts/fallback.ts"
      to: "public/audio/prerecorded/*.mp3"
      via: "PRERECORDED_URLS maps key to /audio/prerecorded/{key}.mp3"
      pattern: "key\\.toLowerCase.*\\.mp3"
---

<objective>
Generate all 61 MP3 audio files for the v4.0 trimmed/branching script using ElevenLabs v3 API.

Purpose: Phase 25 trimmed all script segments and Phase 26 added 12 new branch keys (Q2B, Q4B). The existing 49 MP3s contain OLD v3.1 text and 12 new keys have no audio at all. This plan regenerates ALL 61 files with `--force` to ensure consistent voice quality across the entire experience.

Output: 61 MP3 files in public/audio/prerecorded/, verified FallbackTTS coverage
</objective>

<execution_context>
@C:/Users/USER/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/USER/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@scripts/generate-audio-v3.ts
@src/services/tts/fallback.ts
@src/data/script.ts

<interfaces>
<!-- Key contracts the executor needs -->

From scripts/generate-audio-v3.ts:
- Reads all keys from `SCRIPT` object via `Object.keys(SCRIPT)`
- Maps each key to phase voice settings via `getPhaseForKey(key)`:
  - FALLBACK_* -> FALLBACK settings (stability: 0.70)
  - TIMEOUT_* -> TIMEOUT settings (stability: 0.70)
  - DEVOLUCAO_* -> DEVOLUCAO settings (stability: 0.50)
  - ENCERRAMENTO* -> ENCERRAMENTO settings (stability: 0.55)
  - PARAISO_* -> PARAISO settings (stability: 0.40)
  - PURGATORIO_* -> PURGATORIO settings (stability: 0.45)
  - INFERNO_* -> INFERNO settings (stability: 0.65)
  - Default -> APRESENTACAO settings (stability: 0.50)
- Output: `public/audio/prerecorded/{key_lowercase}.mp3`
- Flags: `--dry-run` (preview), `--force` (overwrite existing), `--clean` (remove orphans)
- API: ElevenLabs v1 TTS, model eleven_v3, format mp3_44100_192
- Rate limit: 1 second between API calls

From src/services/tts/fallback.ts:
- PRERECORDED_URLS derived dynamically: `Object.keys(SCRIPT).map(key => [key, `/audio/prerecorded/${key.toLowerCase()}.mp3`])`
- No code changes needed for new keys -- dynamic derivation handles it automatically
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify generation script handles all 61 keys and run dry-run</name>
  <read_first>
    - scripts/generate-audio-v3.ts
    - src/data/script.ts (first 120 lines for ScriptDataV4 type definition)
  </read_first>
  <files>scripts/generate-audio-v3.ts</files>
  <action>
    1. Run the generation script in dry-run mode to verify all 61 keys are detected:
       `npx tsx scripts/generate-audio-v3.ts --dry-run`

    2. Verify the output shows exactly 61 keys -- 49 existing (SKIP) + 12 new (GEN):
       - New keys that should show GEN: inferno_q2b_setup, inferno_q2b_pergunta, inferno_q2b_resposta_a, inferno_q2b_resposta_b, purgatorio_q4b_setup, purgatorio_q4b_pergunta, purgatorio_q4b_resposta_a, purgatorio_q4b_resposta_b, fallback_q2b, fallback_q4b, timeout_q2b, timeout_q4b

    3. Verify phase detection is correct for new keys by checking dry-run output:
       - inferno_q2b_* should show phase "INFERNO"
       - purgatorio_q4b_* should show phase "PURGATORIO"
       - fallback_q2b, fallback_q4b should show phase "FALLBACK"
       - timeout_q2b, timeout_q4b should show phase "TIMEOUT"

    4. If dry-run shows correct 61 keys with correct phases, the script needs NO modifications.
       If any key is missing or has wrong phase, fix `getPhaseForKey()` in generate-audio-v3.ts.

    NOTE: Do NOT run the actual generation yet -- that is Task 2 (checkpoint).
  </action>
  <verify>
    <automated>npx tsx scripts/generate-audio-v3.ts --dry-run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Dry-run output lists exactly 61 keys (grep for "Total keys: 61")
    - All 12 new Q2B/Q4B keys appear with "GEN" prefix (not "SKIP")
    - All 49 existing keys appear with "SKIP" prefix
    - Phase assignments correct: INFERNO_Q2B -> INFERNO, PURGATORIO_Q4B -> PURGATORIO, FALLBACK_Q2B -> FALLBACK, TIMEOUT_Q2B -> TIMEOUT
  </acceptance_criteria>
  <done>Dry-run confirms all 61 keys detected with correct phase voice settings; generation script ready to execute</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Generate all 61 MP3s with --force --clean</name>
  <read_first>
    - scripts/generate-audio-v3.ts
  </read_first>
  <files>public/audio/prerecorded/*.mp3</files>
  <action>
    Run the ElevenLabs generation command to produce all 61 MP3 files:
    `npx tsx scripts/generate-audio-v3.ts --force --clean`

    This calls the REAL ElevenLabs API (~$0.15/segment x 61 = ~$9.15 total).
    The --force flag overwrites existing 49 MP3s with trimmed v4 text.
    The --clean flag removes any orphaned MP3s from old v3.1 script.
    Rate limiting (1s between calls) is built into the script.

    If any calls fail with 429 (rate limit), re-run without --force to retry only failed files.
  </action>
  <what-built>
    Task 1 verified the generation script correctly handles all 61 script keys with proper phase voice settings. Now the actual ElevenLabs API must be called to generate/regenerate all audio files.

    This step costs real money (~$0.15/segment x 61 = ~$9.15 total) and takes ~2 minutes (61 API calls with 1s rate limiting).
  </what-built>
  <how-to-verify>
    Run the generation command:
    ```
    npx tsx scripts/generate-audio-v3.ts --force --clean
    ```

    Expected behavior:
    1. Script shows "Total keys: 61"
    2. Each key generates with "OK" status and file size
    3. Summary shows "Generated: 61, Skipped: 0, Failed: 0"
    4. If any fail due to rate limiting (429), re-run without --force (skips already-generated files)

    After generation, verify:
    1. Count files: `ls public/audio/prerecorded/*.mp3 | wc -l` should show 61
    2. Check total size: `du -sh public/audio/prerecorded/` should be ~25-40MB
    3. Spot-check a new file exists: `ls -la public/audio/prerecorded/inferno_q2b_setup.mp3`
  </how-to-verify>
  <verify>
    <automated>ls public/audio/prerecorded/*.mp3 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Generation summary shows "Generated: 61" (or close, with re-runs for failures)
    - `ls public/audio/prerecorded/*.mp3 | wc -l` returns 61
    - All 12 new Q2B/Q4B MP3 files exist (e.g., inferno_q2b_setup.mp3)
    - Total directory size between 20-50MB
  </acceptance_criteria>
  <done>All 61 MP3 files generated in public/audio/prerecorded/ with consistent voice quality</done>
  <resume-signal>Type "generated" after all 61 MP3s are confirmed, or describe any failures</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Validate FallbackTTS coverage and audio inventory</name>
  <read_first>
    - src/services/tts/fallback.ts
    - src/data/script.ts (first 115 lines for key list)
  </read_first>
  <files>No files modified -- validation only</files>
  <action>
    1. Verify AUDI-02 (FallbackTTS coverage) is already satisfied:
       - Read src/services/tts/fallback.ts and confirm PRERECORDED_URLS is derived dynamically via `Object.keys(SCRIPT).map(...)` (line ~10-15)
       - This means ALL 61 keys (including Q2B/Q4B) automatically get URL entries -- no code change needed

    2. Cross-check: Run a Node script to verify every SCRIPT key has a matching MP3 file:
       ```
       npx tsx -e "
         import {SCRIPT} from './src/data/script';
         import {existsSync} from 'fs';
         const keys = Object.keys(SCRIPT);
         let missing = 0;
         for (const key of keys) {
           const path = 'public/audio/prerecorded/' + key.toLowerCase() + '.mp3';
           if (!existsSync(path)) { console.log('MISSING:', path); missing++; }
         }
         console.log('Total keys:', keys.length, '| Missing files:', missing);
         if (missing === 0) console.log('ALL AUDIO FILES PRESENT');
       "
       ```

    3. Check total audio size is reasonable for browser loading (should be <50MB):
       `du -sh public/audio/prerecorded/`

    4. Verify no orphaned MP3 files exist (old keys from v3.1 that no longer have SCRIPT entries):
       - The --clean flag in Task 2 should have removed them
       - Count files in directory should equal 61
  </action>
  <verify>
    <automated>npx tsx -e "import {SCRIPT} from './src/data/script'; import {existsSync} from 'fs'; const keys = Object.keys(SCRIPT); let m=0; for (const k of keys) { if (!existsSync('public/audio/prerecorded/'+k.toLowerCase()+'.mp3')) m++; } console.log(m===0?'PASS: All '+keys.length+' MP3s present':'FAIL: '+m+' missing')"</automated>
  </verify>
  <acceptance_criteria>
    - Verification script outputs "PASS: All 61 MP3s present"
    - FallbackTTS fallback.ts contains `Object.keys(SCRIPT).map` for dynamic URL derivation (no hardcoded key list)
    - Total audio directory size is under 50MB
    - File count in public/audio/prerecorded/ is exactly 61
    - No orphaned MP3 files (every .mp3 corresponds to a SCRIPT key)
  </acceptance_criteria>
  <done>All 61 MP3s confirmed present, FallbackTTS covers all keys dynamically, total size under 50MB, no orphaned files</done>
</task>

</tasks>

<verification>
Phase 28 verification checklist:
1. `ls public/audio/prerecorded/*.mp3 | wc -l` returns 61
2. Cross-check script confirms 0 missing files
3. FallbackTTS uses dynamic Object.keys(SCRIPT) derivation (no hardcoded URLs)
4. Audio format is mp3_44100_192 (192kbps) -- confirmed by generation script parameters
5. Voice ID PznTnBc8X6pvixs9UkQm used for all files -- confirmed by generation script
6. Total size under 50MB for reasonable browser loading
</verification>

<success_criteria>
- AUDI-01: All 61 MP3s regenerated via ElevenLabs v3 with voice ID PznTnBc8X6pvixs9UkQm, format mp3_44100_192
- AUDI-02: FallbackTTS PRERECORDED_URLS dynamically derived from SCRIPT keys -- already covers all 61 keys including Q2B/Q4B without any code changes
- All branch keys (Q2B, Q4B) use correct phase voice settings (INFERNO, PURGATORIO, FALLBACK, TIMEOUT)
- No orphaned audio files from old v3.1 script
</success_criteria>

<output>
After completion, create `.planning/phases/28-audio-regeneration/28-01-SUMMARY.md`
</output>
